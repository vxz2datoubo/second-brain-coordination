# Offline browser surface

Future HTML, CSS, SVG and JavaScript sources in this directory are admitted
only after the canonical-floor public-safe verifier passes. Runtime networking
is intentionally absent.
