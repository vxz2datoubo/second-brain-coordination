# build / dependencies — never commit
node_modules/
dist/
.vite/
*.tsbuildinfo

# python
__pycache__/
*.pyc
.venv/
venv/

# local runtime / telemetry / cache — never commit
runtime/
.cache/
*.log

# credentials / secrets — NEVER commit
.env
.env.*
*.pem
*.key
secrets/

# editor
.DS_Store
.idea/
.vscode/
