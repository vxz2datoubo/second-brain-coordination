---
id: P065
type: source-page
title: "贡献指南"
source: "https://docs.mai-mai.org/develop/contributing.md"
snapshot: "2026-06-22"
tags: [maibot, 官方文档, 来源页]
---

# 贡献指南

## 原子用途
这是一张来源页笔记，用来在以后研究 MaiBot 时快速定位官方文档页面、标题结构和主要信息点。

## 来源
- 官方页面：[https://docs.mai-mai.org/develop/contributing.md](https://docs.mai-mai.org/develop/contributing.md)
- 本地快照：[maibot-docs-llms-full-2026-06-22.md](../../sources/maibot-docs-llms-full-2026-06-22.md)

## 页面摘要
欢迎参与 MaiBot 开发！在提交代码之前，请仔细阅读以下规范。
1. 标准库和第三方库使用 `from ... import ...` 形式在前，`import ...` 形式在后；同组按字母序排列
2. 本地模块：同目录使用相对导入，跨目录使用 `from src.xxx` 绝对导入
3. 标准库/第三方库导入放在本地模块导入之前，块间用空行分隔
4. 重构时调整不符合规范的导入顺序
1. 保持良好注释；重构时保留原注释（可修改内容）
2. 原无注释的重构时，较长或复杂功能块应添加注释
3. 首选语言为简体中文（注释、日志、WebUI 文案）
1. 重构时保留原类型注解；无注解的复杂函数/多参数函数应添加
2. 参数化泛型使用 Python 3.10+ 内置语法（如 `dict[K, V]`、`list[T]`、`T | None`），避免使用旧式 typing 模块写法
3. 变量类型确定后不必使用 `or` 回退
以下行为在项目中**严格禁止**：
* **修改 `dashboard/` 下任何内容** — 前端由独立仓库构建
* **直接编辑 `bot_config.toml` / `model_config.t

## 快速要点
- 标准库和第三方库使用 `from ... import ...` 形式在前，`import ...` 形式在后；同组按字母序排列
- 本地模块：同目录使用相对导入，跨目录使用 `from src.xxx` 绝对导入
- 标准库/第三方库导入放在本地模块导入之前，块间用空行分隔
- 重构时调整不符合规范的导入顺序
- 保持良好注释；重构时保留原注释（可修改内容）
- 原无注释的重构时，较长或复杂功能块应添加注释
- 首选语言为简体中文（注释、日志、WebUI 文案）
- 重构时保留原类型注解；无注解的复杂函数/多参数函数应添加

## 标题树
- 贡献指南
  - 导入规范
- 标准库
- 第三方库
- 本地模块
  - 注释规范
  - 类型注解规范
- 推荐
- 不推荐
  - 反模式列表
  - 变量与属性规范
  - 开发命令
    - 安装依赖
    - 运行项目
    - 运行测试
    - 代码检查
- Lint
- 自动格式化
  - PR 流程
    - 注意事项
