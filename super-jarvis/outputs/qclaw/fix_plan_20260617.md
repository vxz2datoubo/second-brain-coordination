# Super Jarvis 系统修复方案

**生成时间**: 2026-06-17
**来源**: Codex 审查报告 (F:/ai/super-jarvis/outputs/codex/系统审查报告.md)
**执行人分工**:
- **Codex** = 工程代码修改（.py / .json schema / 数据文件）
- **WorkBuddy** = 配置文件/文档/MCP 注册表

---

## 📊 修复优先级总览

| 优先级 | 数量 | 预计工时 | 阻塞业务 |
|--------|------|----------|----------|
| **P0** | 1 | 10 分钟 | GET 请求 100% 报错 |
| **P1** | 8 | 2-3 小时 | 健康检查/搜索/Codex 调用全部异常 |
| **P2** | 3 | 1 小时 | 鲁棒性/扩展性 |

---

## 🔴 P0 — 立即修复

### P0-1: `server.py:118` GET 路由 `NameError`

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/server.py:118` |
| **症状** | `"/api/events/record": lambda: self.record_event(data)` 中 `data` 未定义，GET 请求 100% 触发 `NameError: name 'data' is not defined` |
| **修复步骤** | 1. 删除该 GET 路由（事件记录本就是写入操作，GET 不合理）<br>2. 确认 POST 版本存在并保留<br>3. 启动 server.py，curl 测试 `GET /api/events/record` 返回 404（而非 500）<br>4. curl 测试 `POST /api/events/record` 仍然可用 |
| **执行者** | Codex |
| **前置条件** | 无 |
| **风险等级** | 🟢 极低（删除错误路由，POST 通路不受影响） |
| **回滚** | `git checkout server.py` 或从备份恢复 |

**具体代码改动**:
```python
# 找到这一行（server.py 第 118 行附近）:
"/api/events/record": lambda: self.record_event(data),

# 直接删除（POST 版本在 do_POST 里已经存在）
```

---

## 🟡 P1 — 尽快修复

### P1-1: 知识图谱残留问题（inbox 节点 + 孤立节点 + 非标准 relation）

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/data/knowledge-graph.json` |
| **症状** | 95 节点中 11 个 category=inbox 未归类；23 个孤立节点无关联边；12 条 `cross_category:...` 长文本 relation 不符合枚举 |
| **修复步骤** | 1. **Phase A — 备份**：`cp knowledge-graph.json knowledge-graph-backup-pre-cleanup-20260617.json`<br>2. **Phase B — 写脚本** `tools/cleanup_kg.py`：<br>&nbsp;&nbsp;&nbsp;a. 扫描 category=inbox 的节点，读取 title/tags，调用 `_graph.auto_link()` 重新跑关联（jaccard > 0.2 自动建边）<br>&nbsp;&nbsp;&nbsp;b. 关联后 category 仍未匹配的，写入 `data/pending/` 等待人工 review<br>&nbsp;&nbsp;&nbsp;c. 孤立节点（度=0）执行一次 `auto_link(threshold=0.15)` 降低阈值补救<br>&nbsp;&nbsp;&nbsp;d. `cross_category:xxx` 关系 → 映射到标准关系（`references`/`related_to`/`derived_from`）<br>3. **Phase C — 跑脚本 + 验证**：`python tools/cleanup_kg.py`；输出 `cleanup_report.json`（前后对比）<br>4. **Phase D — 手动 review**：在浏览器打开 `http://localhost:8766/app/graph.html` 可视化确认 |
| **执行者** | Codex（写脚本）+ WorkBuddy（人工 review 11 个 inbox 节点归类） |
| **前置条件** | P0 修完；graph.html 可视化页面可用 |
| **风险等级** | 🟡 中（修改知识图谱核心数据，可能影响搜索召回） |
| **回滚** | 备份文件直接覆盖即可 |

### P1-2: `core/graph.py` 重复方法定义 `get_full_graph()`

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/core/graph.py:317` 和 `:358`（后者覆盖前者） |
| **症状** | `:317` 版本只 `return self.data`；`:358` 版本是 `reload + return {nodes, edges}`。后定义覆盖前者，`:317` 是死代码 |
| **修复步骤** | 1. 读取 `:317` 上下文，确认第一个版本的实现逻辑<br>2. **判断**：对比两个版本对调用方的影响<br>&nbsp;&nbsp;&nbsp;- 检查 `server.py` 中 `get_full_graph()` 的调用方（`/api/knowledge-graph`、`get_graph`）<br>&nbsp;&nbsp;&nbsp;- 确认实际生效的是 `:358` 版本（reload + 结构化）<br>3. **删除 `:317` 版本**（保留 `:358`）<br>4. 用 grep 确认 `get_full_graph` 在全代码库只有一处定义<br>5. 启动 server.py 测试 `GET /api/knowledge-graph` 返回 200 |
| **执行者** | Codex |
| **前置条件** | 无 |
| **风险等级** | 🟢 极低（删除死代码） |
| **回滚** | git checkout |

**具体代码改动**:
```python
# core/graph.py:317 附近删除整个第一个 get_full_graph() 方法
# 保留 :358 附近的版本（带 reload + 结构化返回）
```

### P1-3: `core/qclaw.py` 健康检查与 token 解析分叉

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/core/qclaw.py` `_ping_qclaw()` vs `_resolve_token()` |
| **症状** | `_ping_qclaw()` 只读 `qclaw.json`（端口信息）；`_resolve_token()` 还会回退到 `openclaw.json`。当 QClaw gateway 的 token 真的在 `openclaw.json` 时，健康检查用错 token 会误判失败 |
| **修复步骤** | 1. **重构 token 解析**：把 `_resolve_token()` 提取为独立函数 `_get_token()`，优先级链：`openclaw.json.gateway.auth.token` → `qclaw.json.token` → `FALLBACK_TOKEN`<br>2. `_ping_qclaw()` 改为调用 `_get_token()` 而不是内部重复实现<br>3. 在 `_ping_qclaw()` 返回 False 时增加日志：`logger.warning(f"QClaw health check failed on port {port}, tried token from {source}")`<br>4. 加单元测试 `tests/test_qclaw_token.py`：mock 两个配置文件，验证 token 解析优先级<br>5. 启动 server 跑一次健康检查，确认 200 |
| **执行者** | Codex |
| **前置条件** | 无 |
| **风险等级** | 🟢 低（纯重构） |
| **回滚** | git checkout |

### P1-4: `mcp/qclaw_bridge.py` 硬编码 token

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/mcp/qclaw_bridge.py:9` |
| **症状** | `QCLAW_TOKEN = "67421131a067a461..."` 写死，与 `core.qclaw.QClawBridge` 的动态端口/token 解析分叉。QClaw 重启换 token 后 MCP 失联 |
| **修复步骤** | 1. 删除硬编码的 `QCLAW_TOKEN` 和 `_get_port()` 函数<br>2. `mcp/qclaw_bridge.py` 改为 `from core.qclaw import QClawBridge, QCLAW_CONFIG_PATH` 复用现有逻辑<br>3. `call_qclaw()` 函数改为：<br>&nbsp;&nbsp;```python<br>&nbsp;&nbsp;def call_qclaw(prompt, max_tokens=1500):<br>&nbsp;&nbsp;&nbsp;&nbsp;qc = QClawBridge()<br>&nbsp;&nbsp;&nbsp;&nbsp;return qc.ask(prompt, max_tokens=max_tokens)[0]<br>&nbsp;&nbsp;```<br>4. 启动 WorkBuddy，调用 `qclaw_ask` 工具测试返回结果 |
| **执行者** | Codex |
| **前置条件** | P1-3 修完（`_get_token()` 提取出来） |
| **风险等级** | 🟡 中（修改 MCP 通信层，WorkBuddy 集成受影响） |
| **回滚** | git checkout |

### P1-5: Codex 默认模型/沙箱三处配置不一致

| 字段 | 内容 |
|------|------|
| **位置** | `core/codex_bridge.py` / `mcp/codex_bridge.py` / `CODEBUDDY.md` |
| **症状** | 三处配置分叉：<br>- `core/codex_bridge.py`: sandbox=read-only, model=gpt-5.5<br>- `mcp/codex_bridge.py`: sandbox=workspace-write, model=gpt-5.5<br>- `CODEBUDDY.md` 文档: sandbox=workspace-write, **model=gpt-5-codex** ❌ |
| **修复步骤** | 1. **WorkBuddy 决策**：在群里确认统一标准（建议 `workspace-write + gpt-5.5`，因为 WorkBuddy 调用场景需要写入）<br>2. **Codex 修改** `core/codex_bridge.py:60` 附近：`sandbox: str = "workspace-write"`<br>3. **Codex 修改** `CODEBUDDY.md`：把 `model="gpt-5-codex"` 改为 `model="gpt-5.5"`<br>4. **Codex 添加** `config/codex_defaults.json`：<br>&nbsp;&nbsp;```json<br>&nbsp;&nbsp;{"sandbox": "workspace-write", "model": "gpt-5.5", "updated": "2026-06-17"}<br>&nbsp;&nbsp;```<br>&nbsp;&nbsp;两个 codex_bridge.py 都从该文件读取<br>5. 跑 `codex_doctor` MCP 工具验证默认配置生效 |
| **执行者** | Codex（修 .py）+ WorkBuddy（改 .md + 拍板统一标准） |
| **前置条件** | WorkBuddy 先决策 |
| **风险等级** | 🟡 中（影响所有 Codex 调用的默认行为） |
| **回滚** | git checkout |

### P1-6: `decision_consult.py` category 参数被 server 静默忽略

| 字段 | 内容 |
|------|------|
| **位置** | `_task3_decision.py` / `server.py:241` `search()` 方法 |
| **症状** | 决策咨询调用传 `category` 字段想缩小范围，但 `search()` 只读 `query` 和 `top_k`，category 被丢弃 |
| **修复步骤** | 1. **修改** `server.py:241` `search()` 方法：<br>&nbsp;&nbsp;```python<br>&nbsp;&nbsp;def search(self, data):<br>&nbsp;&nbsp;&nbsp;&nbsp;q = data.get("query", "").strip()<br>&nbsp;&nbsp;&nbsp;&nbsp;cat = data.get("category")  # 新增<br>&nbsp;&nbsp;&nbsp;&nbsp;if not q: return self.send_err("query required")<br>&nbsp;&nbsp;&nbsp;&nbsp;if cat: results = _search.search(q, data.get("top_k", 10), category=cat)<br>&nbsp;&nbsp;&nbsp;&nbsp;else: results = _search.search(q, data.get("top_k", 10))<br>&nbsp;&nbsp;&nbsp;&nbsp;self.send_json({"query": q, "category": cat, "results": results, "count": len(results)})<br>&nbsp;&nbsp;```<br>2. **确认** `core/tfidf.py` 的 `SearchEngine.search()` 是否支持 `category` 参数；若不支持则同步扩展（加 category 过滤逻辑）<br>3. 跑决策咨询测试：传 `category="investment"` 应只返回投资类结果 |
| **执行者** | Codex |
| **前置条件** | 无（需先读 `core/tfidf.py` 确认接口） |
| **风险等级** | 🟢 低（向后兼容，不传 category 时行为不变） |
| **回滚** | git checkout |

### P1-7: `mcp/fs_tools.py search_brain()` GET/POST 不匹配

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/mcp/fs_tools.py` `search_brain()` 函数 |
| **症状** | `search_brain` 用 `urllib.request.urlopen` 发起 **GET** 请求到 `/api/retrieve/search?q=...&top_k=...`；但 server.py 只在 **POST** `do_POST` 里注册该路由，GET 调用必然 404 |
| **修复步骤** | 1. **方案 A（推荐）— 双路由注册**：在 server.py `do_GET` 也加上 `/api/retrieve/search` 路由，从 query params 构造 data dict<br>&nbsp;&nbsp;```python<br>&nbsp;&nbsp;# server.py do_GET 的 r 字典里加:<br>&nbsp;&nbsp;"/api/retrieve/search": lambda: self.search({"query": params.get("q", [""])[0], "top_k": int(params.get("top_k", ["10"])[0])}),<br>&nbsp;&nbsp;```<br>2. **方案 B — 改 fs_tools.py**：把 `search_brain` 改用 POST + JSON body<br>3. 选择 A（影响面小，GET 语义对纯查询更合理）<br>4. 测试：MCP 调 `brain_search("量化策略")` 返回结果 |
| **执行者** | Codex |
| **前置条件** | 无 |
| **风险等级** | 🟢 低（只新增路由） |
| **回滚** | git checkout |

### P1-8: `qclaw-output/codex-runs/test-mcp.json` 扩展名错误

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/qclaw-output/codex-runs/test-mcp.json` |
| **症状** | 文件名是 `.json` 但内容是中文 Markdown 文本（Codex 输出），其他工具按 JSON 解析会报错 |
| **修复步骤** | 1. `mv test-mcp.json test-mcp.md`<br>2. 扫描 `qclaw-output/` 全部 `.json` 文件，验证是否都能被 `json.load` 解析（Codex 任务应输出一份 `summary.json` 单独存）<br>3. 在 `core/codex_bridge.py` 添加输出格式约定：人类可读摘要 → `.md`，结构化指标 → `.json` |
| **执行者** | Codex（一次性清理 + 加规范） |
| **前置条件** | 无 |
| **风险等级** | 🟢 极低（只是文件重命名） |
| **回滚** | 重命名回来 |

---

## 🟢 P2 — 建议优化

### P2-1: schema 偏宽松

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/data/schema.md` + 各业务 schema 文件 |
| **症状** | 缺 `additionalProperties` 策略；缺 `user_request.schema.json`、`output.schema.json`；task schema 缺 `started_at/completed_at/output_path/error` |
| **修复步骤** | 1. **新建** `schemas/user_request.schema.json`：定义调用三方协作的入参（prompt/context/timeout/model）<br>2. **新建** `schemas/output.schema.json`：定义 Codex/QClaw 输出标准（markdown_body/summary/key_points/duration_sec/exit_code）<br>3. **新建** `schemas/task.schema.json`：包含 `id/created_at/started_at/completed_at/prompt/output_path/error/exit_code`<br>4. **更新** `data/schema.md`：在所有 schema 加 `"additionalProperties": false`<br>5. 用 `jsonschema` 库在 server.py 启动时 `validate()` 所有 task JSON |
| **执行者** | Codex（写 schema + 验证代码） |
| **前置条件** | 无 |
| **风险等级** | 🟡 中（加严 schema 可能让旧 task 校验失败，需先做向后兼容） |
| **回滚** | 关闭 `validate()` 调用即可 |

### P2-2: MCP 协议实现偏最小化

| 字段 | 内容 |
|------|------|
| **位置** | `mcp/qclaw_bridge.py` / `mcp/fs_tools.py` / `mcp/codex_bridge.py` |
| **症状** | JSON-RPC 错误处理不完整（缺 `code/message/data` 标准错误响应）；通知（无 id 的请求）未处理；缺 `notifications/initialized` 握手 |
| **修复步骤** | 1. **新建** `mcp/base_mcp_server.py`：抽象基类，统一实现：<br>&nbsp;&nbsp;&nbsp;- `handle_request()` 入口<br>&nbsp;&nbsp;&nbsp;- 标准错误响应 `{"code": -32601, "message": "Method not found"}`<br>&nbsp;&nbsp;&nbsp;- 通知（无 id）不响应<br>&nbsp;&nbsp;&nbsp;- `notifications/initialized` 握手<br>2. **重构** 三个 bridge 继承基类，去掉重复代码<br>3. 写集成测试 `tests/test_mcp_protocol.py`：覆盖正常调用/方法未找到/参数错误/通知 |
| **执行者** | Codex |
| **前置条件** | 无 |
| **风险等级** | 🟢 低（基类重构，向后兼容） |
| **回滚** | git checkout |

### P2-3: `server.py stats()` 缺兜底

| 字段 | 内容 |
|------|------|
| **位置** | `F:/ai/server.py:201` `stats()` 方法 |
| **症状** | `mem["meta"]["total_interactions"]` 直接下标访问，若 `memory-index.json` 损坏或缺 `meta` 字段，整数 500 |
| **修复步骤** | 1. 把 stats() 改成 `.get()` 链：<br>&nbsp;&nbsp;```python<br>&nbsp;&nbsp;"memory": {<br>&nbsp;&nbsp;&nbsp;&nbsp;"total_interactions": mem.get("meta", {}).get("total_interactions", 0),<br>&nbsp;&nbsp;&nbsp;&nbsp;"total_feedback": mem.get("meta", {}).get("total_feedback", 0),<br>&nbsp;&nbsp;&nbsp;&nbsp;"good_rate": mem.get("meta", {}).get("good_rate", 0.0),<br>&nbsp;&nbsp;}<br>&nbsp;&nbsp;```<br>2. 同样处理 `sync` 字段<br>3. 测试：临时把 `memory-index.json` 改名，确认 `GET /api/stats` 仍返回 200（默认值） |
| **执行者** | Codex |
| **前置条件** | 无 |
| **风险等级** | 🟢 极低（增强兜底） |
| **回滚** | git checkout |

---

## 🎯 建议执行顺序

```
Phase 1（P0，10分钟）:
  → Codex 修 P0-1（server.py 删 GET 路由）

Phase 2（P1 高优先级，1.5小时）:
  → Codex 修 P1-2（graph.py 重复方法）— 5分钟
  → Codex 修 P1-3 + P1-4（qclaw token 重构 + MCP 复用）— 30分钟
  → WorkBuddy 拍板 P1-5（默认模型/沙箱统一标准）
  → Codex 改 P1-5（.py + .md）— 15分钟
  → Codex 修 P1-7（fs_tools GET 路由）— 10分钟

Phase 3（P1 中优先级，1小时）:
  → Codex 修 P1-6（search 接受 category）— 20分钟
  → Codex 修 P1-8（重命名 test-mcp.json）— 5分钟
  → Codex 修 P1-1（知识图谱清理脚本）— 30分钟
  → WorkBuddy 人工 review 11 个 inbox 节点 — 20分钟

Phase 4（P2，1小时，可延后）:
  → Codex 修 P2-3（stats 兜底）— 10分钟
  → Codex 修 P2-1（schema 完善）— 30分钟
  → Codex 修 P2-2（MCP 基类重构）— 30分钟
```

---

## 📋 验收清单

每个修复完成后，必须跑以下验证：

| 修复 | 验证命令 | 预期结果 |
|------|----------|----------|
| P0-1 | `curl localhost:8766/api/events/record` | 404（不是 500） |
| P1-1 | 浏览器打开 `/app/graph.html` | 无 inbox 节点；孤立节点 < 5 |
| P1-2 | `grep -n "def get_full_graph" core/graph.py` | 只 1 行 |
| P1-3 | `python -c "from core.qclaw import _ping_qclaw; print(_ping_qclaw(8234))"` | True |
| P1-4 | WorkBuddy 调 `qclaw_ask` 工具 | 返回正常分析结果 |
| P1-5 | `codex_doctor` MCP 工具 | 显示统一默认配置 |
| P1-6 | `curl -X POST localhost:8766/api/retrieve/search -d '{"query":"VWAP","category":"investment"}'` | 只返回 investment 类 |
| P1-7 | `qclaw_ask("调 brain_search 测试")` | 返回搜索结果 |
| P1-8 | `python -c "import json; json.load(open('test-mcp.md'))"` | 报错（确认已不是 JSON） |
| P2-1 | `python -c "import jsonschema; jsonschema.validate(...)"` | 通过 |
| P2-2 | `pytest tests/test_mcp_protocol.py` | 全部通过 |
| P2-3 | `mv data/memory-index.json /tmp/ && curl localhost:8766/api/stats` | 200（默认值） |

---

**总结**: 12 个问题中 11 个由 Codex 执行，1 个需要 WorkBuddy 决策拍板（P1-5）。总预计工时 3-4 小时，建议分两个 session 完成：先 P0 + Phase 2 高优先级，剩余 P1+P2 一次性清完。
