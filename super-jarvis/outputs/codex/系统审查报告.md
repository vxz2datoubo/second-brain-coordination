# Super Jarvis 系统审查报告

生成时间: 2026-06-17
审查: Codex (read-only, 扫描 263 个文件)
报告位置: outputs/codex/系统审查报告.md

---

## P0 — 立即修复

### 1. ❌ server.py GET 路由运行时错误
**文件**: server.py:118
**问题**: `do_GET()` 中绑定了 `GET /api/events/record` → `self.record_event(data)`，但 `data` 未定义，访问会触发 `NameError`。
**修复**: 删除 GET 版本的 `/api/events/record`，只保留 POST；或从 query params 构造 data。

---

## P1 — 尽快修复

### 1. ⚠️ 知识图谱残留问题
**文件**: data/knowledge-graph.json
- 95 节点、129 边、0 断边、0 重复 ID — 结构 ✅
- 仍有 11 个 `inbox` 残留节点未归类
- 23 个孤立节点需要补边或归档
- 12 条 `cross_category:...` 长文本 relation 不符合标准关系枚举

### 2. ⚠️ core/graph.py 重复方法定义
**文件**: core/graph.py:317 和 :358
- `get_full_graph()` 定义了两次，后者覆盖前者

### 3. ⚠️ core/qclaw.py 健康检查与实际 token 解析不一致
- `_resolve_token()` 读 `openclaw.json`，`_ping_qclaw()` 只读 `qclaw.json`
- 健康检查可能误判失败

### 4. ⚠️ mcp/qclaw_bridge.py 硬编码 token
- 写死 `QCLAW_TOKEN`，与 core/qclaw.py 动态端口/token 分叉
- 应直接复用 `core.qclaw.QClawBridge`

### 5. ⚠️ Codex 默认模型/沙箱约定不一致
| 位置 | 默认 sandbox | 默认模型 |
|------|-------------|---------|
| core/codex_bridge.py | read-only | gpt-5.5 |
| mcp/codex_bridge.py | workspace-write | gpt-5.5 |
| CODEBUDDY.md | workspace-write | gpt-5-codex ❌ |

### 6. ⚠️ decision_consult.py category 参数可能无效
- POST 时传 `category`，但 server.py 的 search() 只读 `query` 和 `top_k`

### 7. ⚠️ mcp/fs_tools.py search_brain() 与服务端不匹配
- fs_tools 用 `GET /api/retrieve/search?q=...`
- server.py 只在 POST 中注册该端点

### 8. ⚠️ qclaw-output/codex-runs/test-mcp.json 扩展名与内容不符
- 内容是普通中文文本（Codex 输出），不是 JSON

---

## P2 — 建议优化

### 1. schema 偏宽松
- 缺 `additionalProperties` 策略
- 缺 `user_request.schema.json`、`output.schema.json`
- task schema 缺 `started_at/completed_at/output_path/error`

### 2. MCP 协议实现偏最小化
- qclaw_bridge.py 和 fs_tools.py 对 JSON-RPC 错误/通知处理不够完整
- 建议统一 MCP server 基类

### 3. server.py stats() 缺少兜底
- 直接访问 `mem["meta"]`，若 `memory-index.json` 损坏会 500
- 建议用 `.get()` 兜底

---

## ✅ 通过项
- Python 语法: 全部 .py 文件通过语法检测
- JSON 合法性: 除 test-mcp.json 外全部可解析
- JSONL 格式: 全部可解析
- 知识图谱: 无断边、无重复节点、无空标题/内容
- super-jarvis 目录完整性: 核心目录全部就位
- 任务队列: pending 0 / running 0 / done 1 / failed 0
- 决策记录与记忆文件: 格式有效
- CODEBUDDY.md: 与实际结构基本一致
