未能写入 `F:/ai/super-jarvis/outputs/codex/系统审查报告.md`：当前会话是只读沙箱，`Set-Content` 返回“访问被拒绝”。我已完成审查，扫描到 `263` 个文件，并做了 Python 语法、JSON/JSONL、目录、队列、知识图谱完整性检查。报告如下。

**P0 立即修复**

1. ❌ `server.py` 的 GET 路由存在运行时错误  
   `server.py:118` 把 `GET /api/events/record` 绑定到 `self.record_event(data)`，但 `do_GET()` 中没有定义 `data`。访问该 GET 端点会触发 `NameError`。  
   修复：删除 GET 版本的 `/api/events/record`，只保留 POST；或从 query params 构造 data。

**P1 尽快修复**

1. ⚠️ 知识图谱 `data/knowledge-graph.json`  
   结果：95 节点、129 边、0 条断边、0 个重复 ID、0 个空标题/空内容。  
   问题：仍有 11 个 `inbox` 残留节点；23 个孤立节点；边关系里有 12 条长文本 `cross_category: ...`，不符合 `core/graph.py` 的标准关系枚举。  
   建议：批量归类 inbox；为孤立节点补边或降级归档；把长文本 relation 拆成 `relation="related_to"` + `metadata.reason`。

2. ⚠️ `core/graph.py` 有重复方法定义  
   `get_full_graph()` 在 `core/graph.py:317` 和 `core/graph.py:358` 定义了两次，后者覆盖前者。  
   修复：删除前一个定义，统一返回结构，并确保 `edges` 类型始终是 dict，而不是默认 `[]`。

3. ⚠️ `core/qclaw.py` 健康检查与实际 token 解析不一致  
   `_resolve_token()` 会读 `openclaw.json`，但 `_ping_qclaw()` 只读 `qclaw.json` 或 fallback token。若真实 token 只在 `openclaw.json`，健康检查可能误判失败。  
   修复：让 `_ping_qclaw()` 复用 `_resolve_token(_read_qclaw_config())`。

4. ⚠️ `mcp/qclaw_bridge.py` 硬编码 token，且未复用 core 桥接  
   `mcp/qclaw_bridge.py:11` 写死 `QCLAW_TOKEN`，与 `core/qclaw.py` 的动态端口/token 逻辑分叉。  
   修复：MCP 层直接调用 `core.qclaw.QClawBridge`，并补齐 HTTP/JSON 错误处理。

5. ⚠️ Codex 默认模型/沙箱约定不一致  
   `core/codex_bridge.py` 默认 `read-only + gpt-5.5`，`mcp/codex_bridge.py` 默认 `workspace-write + gpt-5.5`，但 `CODEBUDDY.md:128` 写的是 `workspace-write + gpt-5-codex`。  
   修复：选一个源头配置，例如 `super-jarvis/protocols/agent-defaults.json`，代码和文档都从它同步。

6. ⚠️ `DecisionConsult` 的 category 参数可能无效  
   `core/decision_consult.py` POST 到 `/api/retrieve/search` 并传 `category`，但 `server.py` 的 `search()` 只读取 `query` 和 `top_k`。  
   修复：服务端 search 增加 category 过滤，或客户端不要假设 category 生效。

7. ⚠️ `mcp/fs_tools.py` 的 `search_brain()` 调用方式与服务端不匹配  
   fs_tools 使用 GET `/api/retrieve/search?q=...`，但 `server.py` 只在 POST 中注册 `/api/retrieve/search`。  
   修复：改成 POST JSON：`{"query": query, "top_k": max_results}`。

8. ⚠️ `qclaw-output/codex-runs/test-mcp.json` 扩展名与内容不符  
   JSON 校验失败，文件内容是普通中文文本，不是 JSON。  
   修复：改名为 `.txt`/`.md`，或改成合法 JSON。

**P2 建议优化**

1. ⚠️ `super-jarvis/` 目录完整性基本合格  
   已有 `inbox/`、`tasks/{pending,running,done,failed}`、`memory/`、`decisions/`、`outputs/{codex,qclaw,workbuddy}`、`protocols/`、`second_brain/{sources,notes,indexes}`。  
   当前队列：pending 0、running 0、done 1、failed 0，无任务残留。

2. ⚠️ schema 可用但偏宽松  
   `task.schema.json`、`memory.schema.json`、`decision.schema.json` 都是合法 JSON。  
   建议：增加 `additionalProperties` 策略；补 `user_request.schema.json`、`output.schema.json`；给 task 增加 `started_at/completed_at/output_path/error`。

3. ✅ 决策记录与记忆文件当前格式有效  
   `decision_records.jsonl` 1 条，`episodic.jsonl` 1 条，`profile.json` 为合法 profile memory。  
   小问题：`inbox/user_requests.jsonl` 没有 schema，当前字段无法自动校验。

4. ⚠️ MCP 协议实现偏最小化  
   `mcp/codex_bridge.py` 对 notification 有处理，`mcp/qclaw_bridge.py` 和 `mcp/fs_tools.py` 对 JSON-RPC 错误、通知、异常响应不够完整。  
   建议：统一 MCP server 基类或公共 helper。

5. ✅ `server.py` 基本健康面完整  
   具备 digest/search/memory/evolve/events/export 等核心端点。  
   小问题：`stats()` 直接访问 `mem["meta"]`，若 `memory-index.json` 损坏或缺字段会 500，建议使用 `.get()` 兜底。

**验证结果**

- Python 语法：通过，未发现 `.py` 语法错误。
- JSON：除 `qclaw-output/codex-runs/test-mcp.json` 外均可解析。
- JSONL：全部可解析。
- 知识图谱：无断边、无重复节点、无空标题/内容。
- 报告写入：失败，原因是当前文件系统只读。