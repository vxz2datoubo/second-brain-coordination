# R128 P2.4 contract remediation WPDCR

agent_id: CODEX

Planned/actual difficulty: D2/D2. GPT resolved the semantic-weight decision: no numeric score in v1. The remediation therefore specifies semantic discovery through the existing index only to find IDs, then uses `score=None` and existing supplemental placement; provider terms cannot concatenate into the lexical scoring query.

The second blocking discovery is a potential hidden-neighbor oracle in relation-type features. The contract now permits relation types only after independent source and target observability/admission and requires zero/one/many hidden-neighbor projection/telemetry parity. No runtime code was changed. GPT remains owner of later runtime acceptance; all private, formal, live, production, permission, trading and merge gates remain locked. LOCAL_EXECUTION_ISSUES = NONE_OBSERVED.
