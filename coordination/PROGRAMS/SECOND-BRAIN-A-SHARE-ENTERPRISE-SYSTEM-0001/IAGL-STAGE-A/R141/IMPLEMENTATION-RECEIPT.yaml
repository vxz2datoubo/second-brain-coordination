schema_version: "R141-IMPLEMENTATION-RECEIPT-4.0"
task_id: CODEX-IAGL-R141-STAGE-A-SYNTHETIC-SUPERVISOR
issue: 389
mode: project_plan
status: B22_REMEDIATION_LOCAL_VERIFIED__FINAL_EXACT_HEAD_AND_CI_BOUND_EXTERNALLY
provenance:
  historical_initial_executor: CODEX
  current_remediation_executor:
    executor_role: GPT_ENGINEERING_WORKER
    model_id: "GPT-5.6 Sol"
    harness: "ChatGPT engineering-worker window"
    repository_io: "OpenAI GitHub connector / GitHub App chatgpt-codex-connector"
    local_validation_harness: "isolated container Python 3.13.5"
  independent_reviewer: "separate GPT reviewer window; this executor does not self-review"
implementation:
  canonical_runtime: "src/iagl_synthetic_supervisor.py"
  runtime_structure: "single instantiable Supervisor/Store entrypoint plus pure-contract and unbound-operation helper modules"
  shadow_runtime: ABSENT
  persistence: "Python sqlite3 task-local non-authoritative working state"
  process_model: "synchronous single worker; no subprocess, pool, daemon, scheduler, or network"
mechanism_results:
  expected_exact_head_test_count: 56
  expected_partition:
    prior_suite: 43
    b16_b17_suite: 7
    b19_b21_suite: 4
    b22_suite: 2
  retained:
    B19: "one current instantiable Supervisor/Store; no old core bypass; incompatible store injection fails closed"
    B20: "authoritative active P2 blocks even with zero prior transport event"
    B21: "genuine renewed high-risk occurrence re-enters P0 and requires a new disposition"
  B22:
    occurrence_identity: "synthetic semantic_key + supplied idempotency_key"
    exact_duplicate: "same semantic target plus same idempotency is duplicate/redelivery; it preserves trace/history and does not reopen P0"
    genuine_new_occurrence: "same semantic risky request plus new idempotency is a new Stage-A synthetic occurrence; it reopens P0 and requires fresh reconciliation plus a new disposition"
    durability: "event_occurrences is durable task-local state; pre-B22 event rows are backfilled as already-seen occurrences"
    history: "event traces and user disposition history remain append-only"
    live_attestation: "not claimed; UNKNOWN-007 remains open"
local_validation:
  b22_paired_adversarial: "2/2 PASS"
  b16_b17: "7/7 PASS"
  b19_b21: "4/4 PASS"
  retained_combined_mechanisms: "50/50 PASS on cached local regression copy; final authority is exact-head GitHub CI"
  py_compile: PASS
authority_boundary:
  secret_permission_scheduler_production_domain_w3_trading_expansion: false
  autonomous_merge: false
unknown_preservation:
  rule: "synthetic PASS does not close live provider, scheduler, privacy, production, authority, calibration, occurrence-attestation, or merge UNKNOWNs"
remote_evidence_binding:
  exact_head: EXTERNAL_POST_COMMIT_BINDING_REQUIRED
  direct_python_3_11_3_13_ci: EXTERNAL_POST_COMMIT_BINDING_REQUIRED
  retained_phase_3_ci: EXTERNAL_POST_COMMIT_BINDING_REQUIRED
completion_signal: R141_IAGL_STAGE_A_SYNTHETIC_SUPERVISOR_READY_FOR_GPT_REVIEW
