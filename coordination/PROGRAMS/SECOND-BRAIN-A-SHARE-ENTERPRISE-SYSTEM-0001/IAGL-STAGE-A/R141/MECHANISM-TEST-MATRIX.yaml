schema_version: "R141-MATRIX-8.0"
task_id: CODEX-IAGL-R141-STAGE-A-SYNTHETIC-SUPERVISOR
issue: 389
mode: project_plan
current_executor:
  executor_role: GPT_ENGINEERING_WORKER
  model_id: "GPT-5.6 Sol"
expected_exact_head_tests:
  total: 60
  prior_suite: 43
  b16_b17_suite: 7
  b19_b21_suite: 4
  b22_b24_suite: 5
  b25_suite: 1
retained_mechanisms:
  - "canonical IAGL-E001..E018 and B09R2-B15 hardening"
  - "B16 resolved-P2 recurrence"
  - "B17 bounded starvation/fairness below P0/P1/P2"
  - "B19 single current instantiable Supervisor/Store with no shadow core"
  - "B20 reconciliation-derived authoritative active-P2 gate"
  - "B21 genuine renewed P0 recurrence"
  - "B22 duplicate/redelivery versus genuine occurrence"
  - "B23 occurrence+approval_epoch-bound P0 disposition and coalescing"
  - "B24 evidence precision and narrow event-row upgrade regression"
B25_regression:
  fixture_reachability:
    - "pre-B23 reconciliation row persists old-schema p0_dispositions"
    - "matching P0 event is already P0_DISPOSITION_TRACE"
    - "legacy event_traces, event_occurrences, and p0_disposition_history rows are present"
  assertions:
    - "current WorkingStateStore reopen does not crash"
    - "current_snapshot() does not crash and returns no runtime-active legacy unbound disposition"
    - "dropped legacy unbound disposition is recorded in reconciliation_migration_audit"
    - "legacy event and accepted disposition history remain preserved"
    - "genuine new occurrence reopens the P0 gate with a new current occurrence and higher approval_epoch"
    - "fresh reconcile without a current bound disposition remains USER_GATE"
    - "legacy unbound approval never becomes a current approval"
    - "current occurrence_key + approval_epoch disposition resolves normally"
B25_contract:
  legacy_shape: "event_key + decision + decision_ref + optional authority_source, with no occurrence_key and no approval_epoch"
  canonicalization: "drop only the recognized old unbound shape from runtime deserialization"
  no_guessing: "never synthesize occurrence_key or approval_epoch for persisted legacy approval"
  malformed_current_behavior: "non-legacy partially bound/invalid serialized dispositions fail deserialization closed"
  audit_outcome: LEGACY_UNBOUND_P0_DISPOSITION_DROPPED_FAIL_CLOSED
  raw_history: "existing reconciliation identity/generation and event/disposition history are not rewritten to fake a current binding"
B24_evidence_precision:
  direct_run_32031788981:
    python_3_11: 95393222924
    python_3_13: 95393222926
  phase3_run_32031788861:
    python_3_11: 95393222638
    python_3_13: 95393222476
shadow_guard:
  only_instantiable_supervisor_module: iagl_synthetic_supervisor.py
  alternate_instantiable_runtime_allowed: false
  helper_modules: "contracts/dataclasses/constants, migration helper, or unbound operation functions only"
  workflow_static_guard_required: true
truthfulness:
  - "Stage-A legacy compatibility is a task-local SQLite migration/deserialization guarantee, not live-provider attestation"
  - "UNKNOWN-007 remains open"
  - "final PASS/count is bound only by exact-head CI"
  - "synthetic PASS is not production readiness, authority expansion, or independent GPT acceptance"
