schema_version: "R141-MATRIX-5.0"
task_id: CODEX-IAGL-R141-STAGE-A-SYNTHETIC-SUPERVISOR
issue: 389
mode: project_plan
current_executor:
  executor_role: GPT_ENGINEERING_WORKER
  model_id: "GPT-5.6 Sol"
historical_executor:
  executor_role: CODEX
  scope: "initial R141 implementation; provenance retained"
expected_exact_head_tests:
  total: 54
  prior_suite: 43
  b16_b17_suite: 7
  b19_b21_suite: 4
retained_mechanisms:
  - "canonical IAGL-E001..E018"
  - "B09R2-B15 authority/checkpoint/retrieval/reconciliation hardening"
  - "B16 resolved P2 recurrence"
  - "B17 starvation visibility and bounded P4-to-P3 fairness"
remediation_regressions:
  B19:
    - "only one AST SyntheticSupervisor and one WorkingStateStore exist across Stage-A src"
    - "iagl_synthetic_supervisor_core.py is absent and import discovery returns no module"
    - "canonical Supervisor rejects an incompatible WorkingStateStore subclass"
  B20:
    - "database starts with zero P2 event rows"
    - "fresh authoritative active ROUTE_DRIFT creates reconciliation-derived P2 state"
    - "P3/P4 stays blocked"
    - "AUTHORITATIVE_COMPLETE plus exact P2Resolution moves the derived blocker to RESOLVED_TRACE and safe lower work resumes"
  B21:
    - "first high-risk request reaches P0 USER_GATE"
    - "fresh user disposition allows unrelated safe lower work"
    - "identical semantic high-risk recurrence returns event to unadjudicated P0"
    - "reusing the old disposition reference does not clear the renewed request"
    - "new current disposition clears the renewed occurrence while preserving both historical decisions"
shadow_guard:
  canonical_runtime: iagl_synthetic_supervisor.py
  alternate_instantiable_runtime_allowed: false
  workflow_static_guard_required: true
  incompatible_store_injection_allowed: false
truthfulness:
  - "expected count is reconciled from the committed test layout; final PASS/count is bound only by exact-head CI"
  - "synthetic PASS is not production readiness, authority expansion, or independent GPT acceptance"
