# Unplanned Improvement Ledger

| ID | Improvement | AMED class | Evidence | Rollback |
| --- | --- | --- | --- | --- |
| S0C-B-001 | Escalate local SQLite resource warnings to errors during tests. | B | A leaked connection warning appeared during the first scenario pass; explicit cleanup made the strict run pass. | Revert the test cleanup only if a future framework supplies lifecycle ownership. |
| S0C-B-002 | Make the public safety scan ignore generated binary cache while inspecting versioned text. | B | Initial scan attempted UTF-8 decoding of generated bytecode. The revised scan restricts itself to controlled textual extensions. | Revert scanner rule and replace with a repository-wide scanner only under a future gate. |
| S0C-B-003 | Make pre-commit PowerShell validation fail on every native interpreter nonzero exit. | B | The first workflow-YAML path was wrong and PowerShell continued after Python failed. The final validation checks `$LASTEXITCODE` after each tool. | Revert only if a repository-wide checked command wrapper supersedes it. |
| S0C-B-004 | Make CI diff validation use the event base SHA rather than a shallow-checkout remote name. | B | CI unit, scenario, and safety steps passed; only `origin/main...HEAD` was unavailable after shallow checkout. | Revert if a repository-standard checkout helper provides an equivalent base ref. |

No C/D expansion was implemented. Candidate C: versioned compaction snapshots with retention policy; owner GPT/future S0D route; gate requires retention and privacy authorization.
