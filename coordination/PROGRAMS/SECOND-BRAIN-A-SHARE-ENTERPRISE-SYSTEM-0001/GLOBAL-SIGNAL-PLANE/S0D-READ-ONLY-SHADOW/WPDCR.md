# R135 work-process evidence

agent_id: CODEX

- Planned/actual difficulty: medium; actual complexity was bounded by exact commit binding, metadata-only projection, and a 20-case mechanism matrix.
- Discovery: the real source receipt observed ten approved paths and six unresolved status surfaces; bootstrap-required is reducer output, not fixture text.
- Negative result: an initial local test module was not collected by `unittest discover`; it was converted to `unittest.TestCase` and rerun.
- Local execution: generated bytecode was accidentally staged in the first checkpoint, then removed in a separate additive cleanup commit and ignored thereafter.
- Authority: AI Film remains source authority; this adapter cannot write a domain or authorize a successor.
- Postflight gate: exact-head Python 3.11/3.13 CI plus GPT review only; no automatic next-stage unlock.
