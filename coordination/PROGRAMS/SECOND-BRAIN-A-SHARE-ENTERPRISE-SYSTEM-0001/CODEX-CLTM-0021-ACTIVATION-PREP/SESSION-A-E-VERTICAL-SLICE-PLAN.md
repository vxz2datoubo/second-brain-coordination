# Session A-E vertical-slice plan (not implemented)

| Session | Candidate-mode demonstration | Required assertion |
| --- | --- | --- |
| A | A synthetic important user statement becomes a `ConversationEpisode` pointer plus W3 candidate packet. | Deterministic source/candidate identity; no raw body in public artifact. |
| B | A later scoped query invokes the existing W3 plan/bundle path. | Relevant current candidate is recalled with provenance. |
| C | A synthetic user correction produces a linked correction/supersession event. | Prior episode remains historical evidence. |
| D | A current-state query is executed. | Only corrected, valid, in-scope state can pass the Trust Gate. |
| E | An explicit historical query is executed. | Old superseded state is returned only with valid-time and provenance. |

Additional mandatory cases: duplicate ingestion, unknown/conflict, abstention, project bleed, user-scope leak, secret-negative, injection-negative, stale record, rollback/rebuild and deterministic replay. This plan creates no implementation authorization.
