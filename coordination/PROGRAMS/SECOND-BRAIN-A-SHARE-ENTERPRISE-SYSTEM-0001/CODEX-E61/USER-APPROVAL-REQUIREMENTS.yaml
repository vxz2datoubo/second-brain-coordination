schema_version: "1.0"
record_type: "UserApprovalRequirements"
task_id: "CODEX-REAL-PRODUCTION-DURABLE-AUTHORITY-EXTERNAL-ISSUANCE-VERIFICATION-KNOWLEDGE-WRITE-GATE-0057-E61"
status: "USER_APPROVAL_REQUIRED"
reason: "No observable protected external authority control plane currently exists; configuration would change repository/service authority boundaries."
approval_items:
  - approval_id: "E61-APPROVAL-001"
    requested_decision: "Approve or reject Option A as the production authority target: protected GitHub environment plus OIDC federation to an externally operated certification issuer/verifier."
    why_needed: "Without an issuer outside ordinary caller code, a repository-local verifier cannot establish durable production authority."
    requested_change: "Architecture choice only; no configuration action is requested by this item."
    credential_request: "NONE"
    irreversible_effect: "NONE until later configuration is separately approved."
  - approval_id: "E61-APPROVAL-002"
    requested_decision: "Approve creation and protection of a knowledge-certification deployment environment."
    minimum_configuration: "Required independent reviewer; prevent self-review; restrict deployment to main; disable administrator bypass where available."
    why_needed: "A workflow reference to an unprotected environment is not an external approval boundary."
    credential_request: "NONE"
    agent_action_without_approval: "FORBIDDEN"
  - approval_id: "E61-APPROVAL-003"
    requested_decision: "Approve a main-branch ruleset/branch-protection policy appropriate for the certification workflow and authority-policy files."
    minimum_configuration: "Require pull-request review and prevent unreviewed direct changes to the protected certification path. Exact repository UI/API changes remain unperformed."
    why_needed: "Current main protection API reports Branch not protected."
    credential_request: "NONE"
    agent_action_without_approval: "FORBIDDEN"
  - approval_id: "E61-APPROVAL-004"
    requested_decision: "Select and approve an independently administered external issuer/verifier service that accepts GitHub OIDC workload identity and maintains revocation/single-use state."
    minimum_configuration: "Trust policy restricts GitHub issuer, immutable repository identity, main ref, named workflow, named environment, audience, and expiry; issuer key material stays outside the repository."
    why_needed: "GitHub artifact provenance alone cannot provide revocation, single-use, or a scoped formal-write capability."
    credential_request: "Do not provide any secret or private key in chat, repository, Issue, or PR. The service owner configures it directly in its own control plane."
    agent_action_without_approval: "FORBIDDEN"
  - approval_id: "E61-APPROVAL-005"
    requested_decision: "Approve OIDC subject customization to immutable repository identity when supported by the selected provider/policy."
    minimum_configuration: "Policy must bind immutable repository identifiers in addition to ref/workflow/environment constraints."
    why_needed: "The observed repository setting has use_immutable_subject false."
    credential_request: "NONE"
    agent_action_without_approval: "FORBIDDEN"
explicit_non_requests:
  - "No GitHub token, cloud credential, private key, App key, account password, cookie, or secret value."
  - "No market, broker, account, order, or trading authorization."
  - "No approval to enable formal PROJECT/GLOBAL knowledge writes yet."
safe_next_action_after_approval: "Publish a narrowly scoped implementation route naming the approved provider, issuer policy, protected environment, verification endpoint/interface, and testable revocation/single-use semantics."
safe_next_action_if_rejected_or_deferred: "Keep formal writes disabled; implement no external configuration; retain candidate packages as CANDIDATE_ONLY."
