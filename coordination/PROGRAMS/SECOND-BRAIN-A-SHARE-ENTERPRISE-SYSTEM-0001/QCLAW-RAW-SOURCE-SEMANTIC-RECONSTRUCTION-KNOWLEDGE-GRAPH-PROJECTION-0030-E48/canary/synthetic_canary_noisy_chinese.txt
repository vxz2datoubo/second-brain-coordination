# synthetic_canary_noisy_chinese.txt — E48 PUBLIC_SAFE canary corpus (R1)
#
# All text here is synthetic / fictional. No real private content.
#
# Required noise categories per E48 plan §5 / Issue #216 GPT R1 review:
#   (a) filler words + repetitions                  -> line 1 ("呃", "那个")
#   (b) missing punctuation                         -> line 1 has no terminal 。 → PUNCTUATION edit
#   (c) one obvious typo                             -> line 2 ("部份" → "部分")
#   (d) one high-confidence ASR homophone correction -> line 2 ("式式" → "试试")
#   (e) one mid-confidence correction with alternatives retained
#                                                    -> line 3 ("他她" pronoun mid-confidence)
#   (f) one UNKNOWN pronoun / homophone we refuse to resolve
#                                                    -> line 3 (recorded as UnknownMarker)
#   (g) one terminology alias                        -> line 3 ("术语别名" → "量子纠缠")
#   (h) one cross-sentence mechanism / conditional relation
#                                                    -> line 1 ("如果…那么…" carries to L3)
#
# Lines are written so that each noise category is non-overlapping in byte
# ranges, and so that the bounded R1 punctuation rule (line-break only)
# fires exactly once per unterminated line.
呃那个今天我们来讨论一下成交量和价格的关系那个如果成交量上升那么价格就倾向于上升但如果成交量下降价格就可能下降这是个常见的机制需要注意
部份人以为式式看价格就能赚钱但实际上成交量才是关键那个其实看成交量比较靠谱如果量比值很高价格就式式会动但也可能不动
他她在会议上说术语别名应该统一叫量子纠缠他的观点是对的但他的证据还不充分