schema: UNKNOWNRegistry/v1
task_id: CODEX-DS10-P0C-PBO-CSCV-R193
unknowns:
  - id: R193-U-001
    statement: "Source Issue #562 comments were not freshly readable through gh because the configured token returned HTTP 401."
    attempts: ["gh auth status", "gh issue view 562 --repo vxz2datoubo/second-brain-coordination --json number,title,body,comments,url"]
    safe_disposition: "Use the canonical checked-in effective spec snapshot only; do not invent comment requirements."
    owner: "GitHub credential maintainer or GPT Architecture Owner"
    closure_condition: "A fresh authenticated Issue body/comment read confirms the frozen snapshot has no later conflicting amendment."
  - id: R193-U-002
    statement: "No real-market complete synchronous performance matrix or cost evidence was admitted or tested."
    safe_disposition: "Synthetic public-safe fixture only; preserve research-only/no-trade boundary."
    owner: "Future governed data-admission and validation routes"
    closure_condition: "A separately authorized point-in-time dataset and immutable evidence chain are available; this route does not close it."
  - id: R193-U-003
    statement: "GitHub exact-head CI was authored but not run because dispatch explicitly forbids pushing in this execution pass."
    safe_disposition: "Local test evidence remains local-only and is not represented as remote CI evidence."
    owner: "External exact-head verifier after an authorized ordinary push"
    closure_condition: "Workflow run records matching checkout SHA on both Python 3.11 and 3.13."
  - id: R193-U-004
    statement: "P0C v1.1.0 intentionally supports only the W4-exposed public synthetic metric identity and ARITHMETIC_MEAN/V1 scorer."
    safe_disposition: "Unknown metric identities or versions ABSTAIN; relabels that disagree with W4 are INPUT_INTEGRITY_BLOCKED."
    owner: "Future separately governed metric-registration route"
    closure_condition: "A new metric implementation is registered with immutable scorer semantics, W4 contract support, a new P0C method version, and independent review."
