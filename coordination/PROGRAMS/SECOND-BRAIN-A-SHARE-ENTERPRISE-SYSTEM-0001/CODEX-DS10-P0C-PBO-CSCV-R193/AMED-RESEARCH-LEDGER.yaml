schema: AMEDResearchLedger/v1
task_id: CODEX-DS10-P0C-PBO-CSCV-R193
boundary: PUBLIC_SAFE / RESEARCH_ONLY / NO_TRADE
entries:
  - claim: "P0C reproduces the frozen classical CSCV mechanics rather than a sampled variant."
    source: "Issue #562 frozen effective specification snapshot; Bailey/Borwein/Lopez de Prado/Zhu contract incorporated there."
    implementation_check: "S=4 enumerates 6 oriented halves; S=16 budget diagnostic reports 12870; budget fail produces no partial splits."
    status: LOCALLY_VALIDATED
  - claim: "P0C consumes W4 and P0A rather than creating a parallel experiment or read-receipt source."
    source: "canonical W4 read_binding.py and DS10 P0A research_integrity.py APIs."
    implementation_check: "Receipt verification, fresh canonical family read, family/matrix identity comparison, P0A canonical audit digest check."
    status: LOCALLY_VALIDATED
  - claim: "PASS_COMPUTED is calculation-only and does not grant downstream validation or trading authority."
    source: "effective spec snapshot and route hard locks."
    implementation_check: "Closed result authority map is all false; schema rejects trade_authority=true; result has no probability field."
    status: LOCALLY_VALIDATED
external_issue_read:
  gh_cli_available: true
  gh_authenticated: false
  observed_failure: "HTTP 401: active GitHub CLI token invalid"
  disposition: "Used canonical effective-spec snapshot and repository evidence; did not infer unavailable Issue comments."
