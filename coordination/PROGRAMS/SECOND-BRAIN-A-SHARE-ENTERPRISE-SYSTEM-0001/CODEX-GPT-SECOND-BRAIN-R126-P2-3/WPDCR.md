# R126 P2.3 WPDCR

agent_id: CODEX

## PRIMARY_WORK_AND_PROCESS_TRACE

The task closes R125-B01 without changing R124/R125 retrieval authority or ordinary budget semantics. The repair moves only omitted-capture verification to a ContextAssembler-internal exact identity admission check and adds a synthetic duplicate-statement adversarial regression.

## DIFFICULTY_AND_COMPLEXITY

Planned difficulty was D2; actual difficulty was D2. The hardest point was separating identity proof from lexical discovery without making direct store presence count as recall. The resulting path reuses `_consider_candidate`, preserving caller observability and admission while intentionally avoiding rank/tie order.

## DISCOVERIES_AND_NEGATIVE_RESULTS

The legacy `budget=1` statement query deterministically selects the older, lexicographically smaller duplicate atom even though the newer atom is admissible. A direct row-existence assertion, increasing the budget, restoring adapter scans, adding scores, or introducing a second resolver were rejected as incompatible with the route.

The first YAML validation command used repository-relative paths from a Phase-3 subdirectory and stopped with `FileNotFoundError` before parsing or mutating an artifact. This local working-directory containment event is recorded in the execution receipt; validation is rerun from the isolated repository root.

The root retry parsed local YAML but Python 3.13 attempted to decode UTF-8 `git show` route output with the Windows GBK default and raised `UnicodeDecodeError`. The canonical route is therefore read with explicit UTF-8 decoding; this is a contained recurrence of the Windows Unicode execution pattern, not a malformed-route claim.

## COORDINATION_AND_BOUNDARIES

CODEX owns this additive synthetic implementation and evidence. GPT owns review and all future schema, performance, private, formal, live, permission and merge decisions. Private source/store, real ingestion/canary, formal PROJECT/GLOBAL, scheduler, E48/live, production MCP/Gateway, permissions and trading remain locked.

## UNKNOWN_AND_NEXT_GATE

R122 and R120 remain deferred. Per-omission proof throughput at unbounded capture scale is not claimed. Run full regression and public-safety checks, push the additive commit to PR #318, await exact-head Python 3.11/3.13 CI, then request GPT review.
