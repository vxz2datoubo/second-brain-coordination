# PROJECT-PLAN.md — E44

**Task:** QCLAW-E43-POST-RECEIPT-SEMANTIC-CAPABILITY-DERIVATION-MASTER-COGNITION-SKILL-CORPUS-EVALUATOR-REAL-MUTATION-AND-PROVIDER-CLOSURE-0025-E44
**Epoch:** 44
**Source:** E43 frozen at `89529da5` (PR #184, Issue #183)
**Disposition:** ALL_REWRITE — E43 has tautological evaluator, caller-controlled authority, no real mutations; E44 starts fresh

## Phase Plan

### Q0 — Fresh route, truthful identity, source selection
- Verify frozen E43, active Codex E56, latest main
- One-file plan commit preserving canonical tree
- E43-SOURCE-SELECTION.yaml before any code copy
- Sole Draft PR + TaskLeaseClaim

### Q1 — VerifiedEvidenceCapability boundary contract
- Narrow immutable consumer contract (no Codex E56 duplication)
- Synthetic fixtures only (test doubles, not canonical authority)
- Capability exposes issuer, source identity, span identity, evidence digest, origin class, verification result
- Foreign/mutable/self-issued/stale/incomplete → fail closed
- BOUNDARY_CONFLICT if E56 cannot satisfy E44

### Q2 — Derived evidence, bundle and atom authority
- Evidence fields are evaluator-derived from verified capability + policy (no caller enums/booleans)
- Registry insertion private and identity-bound; verification recomputes every canonical field
- No caller-accessible signing capability, self-registration, or source digest injection

### Q3 — Stable master, version and contradiction authority
- Semantic identity separate from content (versioned normalization rule)
- Every transition registry-issued, verifying prior identity/hash/content/evidence/reason
- Contradictions default UNRESOLVED; classification requires verified evidence + policy

### Q4 — User cognition and memory write-candidate authority
- KNOWN_AND_STATED requires verified user-origin/message evidence
- State/stability/memory destination recomputed from evidence quality/origin
- GLOBAL requires multiple correlated sources
- Emit memory write candidates only

### Q5 — Evidence-bound skill lifecycle
- Promotion inputs from verifiable evaluator/test receipts (not caller counts)
- Counts/booleans derived from receipts
- Verify issued skill identity and exact legal transition

### Q6 — Real end-to-end semantic corpus evaluator
- Production receives only input + policy (never ExpectedOutcome/ground truth)
- Ground truth held by evaluator comparator outside production
- Corpus identity includes input, ground truth, expected outputs, policy version
- Any failed case → nonzero exit

### Q7 — Genuine copied-production mutations
- Mutate in isolated workspace (never active source tree)
- Verify mutant nonzero → finally restore → verify byte-exact → rerun green
- Cover: self-registration, caller class bypass, generic→user-stated, same-ID skill, ground-truth omission, expected-answer injection

### Q8 — Exact-head semantic Provider
- Workflow: 3.11+3.13 × seeds 0/1/777 = 6 evaluator + 1 compare
- Every job runs tests + mutations + every corpus case; nonzero on failure
- 6 canonical artifacts + 6 environment artifacts + 6 mutation-result payloads
- Byte-exact compare of canonical inner files

### Q9 — Strict receipt and review-grade report
- Verify full ancestry before receipt
- Provider-observed tested-head success required before receipt
- Receipt-head full matrix rerun, publish external anchor
- Complete report with hashes, commands, exits, corpus outcomes, mutations

## Governance
- No merge, main write, rebase, force-push, amend
- Synthetic/public-safe fixtures only
- Codex E56 (Issue #185) — parallel, no editing
- ALL_REWRITE policy for E43 material
