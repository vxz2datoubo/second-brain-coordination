# E25-ARCHIVE-PROVENANCE-MATRIX.yaml
schema_version: "26.0"
epoch: 25
gate: "B R10"

matrix:
  seeds: [0, 42, 1615446236]
  tested_commit: "{tested_commit_sha}"
  format: "git-archive"
  prefix: "second-brain-coordination-archive/"
  method: "git archive --prefix=<PREFIX> --format=zip --output=FILE <SHA>"
  expected: "All 3 seeds produce identical SHA256 and size"

per_seed: []
verification: "to be completed after corrective commit and 3-seed archive generation"
