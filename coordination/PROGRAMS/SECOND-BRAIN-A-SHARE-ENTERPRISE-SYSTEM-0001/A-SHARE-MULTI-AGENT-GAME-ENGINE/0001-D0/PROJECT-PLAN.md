# D0 Project Plan

## Objective

Produce a public-safe, repository-grounded plan for a research-only A-share multi-agent game engine. D0 does not implement the engine; it defines ownership, contracts, uncertainty, integration gates, validation and recovery.

## Inputs and authority

* Authorized base: `742bf80c48087f1062886e2c437f2063a5359e5f`.
* Current route: Issue #23, epoch 2, `CODEX_A_SHARE_MULTI_AGENT_GAME_ENGINE_D0_INTEGRATED_PLAN_READY_FOR_GPT_REVIEW`.
* Canonical public blueprints: multi-agent game engine, family advantage/weakness matrix, project integration index and engineering-learning system.
* PR #93: frozen candidate contract input only; never a factual market authority.

## Execution discipline

All committed files remain under this D0 directory. Temporary validation helpers, generated fixtures and command output remain outside the Git tree. D0 creates no data source, simulator, replay, account, order or execution path. Each checkpoint records verified facts, UNKNOWNs, rejected alternatives and rollback information.

## Completion definition

The package contains every mandatory deliverable, parses strictly, identifies the existing system of record, keeps identities latent, represents A-share feasibility constraints, gates advanced modeling, maps protected-blueprint deltas without copying protected text, and passes file-surface and public-safety validation. Completion requires GPT review; it is not a runtime promotion.
