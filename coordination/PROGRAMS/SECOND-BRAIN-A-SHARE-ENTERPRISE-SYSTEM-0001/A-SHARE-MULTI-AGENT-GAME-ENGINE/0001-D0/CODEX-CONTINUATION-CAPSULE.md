# Codex Continuation Capsule

## Resume rule

Before any follow-up, fetch remote `main`, reread the active Codex route, lease protocol, Issue #23 and the current Draft PR. Do not continue from this capsule if the route epoch, branch, completion signal or allowed-file surface changed.

## Current D0 disposition

The package is `CANDIDATE_ONLY / research_only / NO_TRADE`. It defines architecture and validation gates only. It does not admit data, run replay/backtests, train MARL, assert identity, issue a signal or touch accounts/orders.

## Recommended next route after GPT acceptance

Create a separate, explicitly authorized task for a deterministic synthetic rules MVP. It should implement only state/action feasibility and abstention using fixtures. It must not consume local market data. A further task is needed for approved point-in-time data admission and replay calibration.

## Preservation and rollback

All D0 changes are new files under one directory. Revert the D0 branch range or delete the Draft PR branch after GPT decides not to retain the plan. Do not delete Issue comments, PR evidence or protected-blueprint audit history.
