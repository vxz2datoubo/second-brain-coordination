# Test Run Receipt

Status: `PENDING_FINAL_STATIC_VALIDATION`.

This document is updated only after the complete D0 delivery surface exists. It will record exact commands, exit codes, normalized output hashes, strict duplicate-key coverage, JSON/YAML parsing, required-file checks, allowlist checks, public-safety scan results and preserved UNKNOWNs. No real data, replay, backtest, MARL, account or trade test is permitted.
