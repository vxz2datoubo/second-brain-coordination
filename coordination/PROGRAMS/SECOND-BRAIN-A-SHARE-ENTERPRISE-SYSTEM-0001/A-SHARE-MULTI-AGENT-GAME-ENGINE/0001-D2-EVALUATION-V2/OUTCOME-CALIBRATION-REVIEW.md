# Outcome Calibration Review: E22

| Forecast item | Observed result | Calibration |
| --- | --- | --- |
| Fresh Evaluation V2 can replace frozen catalog-only evaluation ideas with executable evidence. | A fresh branch from `fa68186...` contains an executable registry, independent oracle, metamorphic suite, and catalog reconciliation. | Supported locally. |
| Decorative mutants are a material risk. | The first arrival-order design was behaviorally inert; the registry rejected it until activation changed actual priority. | Risk confirmed and controlled. |
| Independent accounting can detect stored-state trust and accounting defects. | Omitted/inverted external flow, partial peer commit, stored-hash trust, causal cycle, conflict bypass, and late reservation artifacts were killed. | Supported for selected synthetic fault families. |
| Three clean archives can demonstrate determinism. | Seeds `1`, `7`, and `97` produced one identical full-report SHA256. | Supported locally. |
| Python 3.11/3.13 CI will cover Evaluation V2. | Existing matrix does not run E22 and local 3.11 is absent. | Not yet supported; requires GPT route decision. |

## Net Effect

The intended semantic-evaluation benefit is observed within the synthetic
boundary. The CI-route gap is a negative finding, not a reason to promote the
work beyond `SUCCESS_WITH_FINDINGS`. No real-market or trading conclusion is
created.

## E27 Calibration Addendum

| Forecast item | Observed result | Calibration |
| --- | --- | --- |
| Canonical remote identity can be proven without destructive Git work. | Canonical URL, `ls-remote`, GitHub repository identity and branch ref agreed. | Supported. |
| A preserved commit can be pushed unchanged and remotely validated. | `b45ab1...` retained its declared parent/tree and passed exact-branch dual-Python CI. | Supported. |
| A green PR event proves the required commit archive. | It archived merge SHA `c311af...`, not the branch commit. | Refuted; corrected by `workflow_dispatch`. |

No evidence here changes the synthetic-only, candidate-only boundary.
