# E13 Candidate Quarantine Test Receipt

agent_id: CODEX

`python -m unittest tests.test_candidate_quarantine -v` completed with 16
passing tests. Candidate acceptance remains synthetic-only, candidate-only,
and requires immutable ontology translation plus three run attestations.
