components:
  P1_FoundationSharedEnvelope: {action: REUSE, evidence: "P1 schema and ContractRuntime.envelope"}
  P1_TemporalSemantics: {action: ADAPT, evidence: "Bar timestamps and available_at gate"}
  P1_LineageQuality: {action: ADAPT, evidence: "dataset/source/hash manifest"}
  P1_CapabilityEntitlement: {action: ADAPT, evidence: "HISTORICAL_BAR allow-list and rejection"}
  P1_ApprovalRollback: {action: ADAPT, evidence: "NO_TRADE output and branch/PR rollback"}
  local_brain_core_types: {action: UNKNOWN, evidence: "not materialized in public worktree"}
  offline_research_engine: {action: NEW, reason: "public-safe executable reference slice"}
  private_knowledge_gateway: {action: FUTURE, owner: WORKBUDDY_GPT, reason: "private transport is out of scope"}
  QQ_learning_packet_adapter: {action: ADAPT, status: CANDIDATE_ONLY, evidence: "Issue 39 comment observation"}
