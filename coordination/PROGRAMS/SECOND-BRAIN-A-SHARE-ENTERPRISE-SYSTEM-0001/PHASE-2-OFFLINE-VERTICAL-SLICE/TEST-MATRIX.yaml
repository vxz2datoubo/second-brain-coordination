test_suite: "tests/test_offline_research.py"
execution: "python -m unittest discover -s coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/PHASE-2-OFFLINE-VERTICAL-SLICE/tests -p test_*.py -v"
coverage_statement:
  test_methods: 21
  meaningful_assertions_or_raises: 79
  rationale: "Assertions exercise successful paths and independent negative gates; the count is not file-existence checks."
categories:
  contract: [future_available_at, lineage, capability, price_volume, schema_major]
  ingestion: [csv, json, jsonl, manifest, quarantine]
  replay: [stable_sort, duplicate, near_duplicate, checkpoint_resume, checkpoint_mismatch, deterministic_hash]
  research: [candidate_status, availability_delay, feature_lineage]
  ashare_simulation: [T_plus_1, suspension, limit_up, costs, SH_fee, turnover_limit, no_trade]
  validation: [chronological_split, walk_forward, insufficient_abstention, no_random_shuffle]
  knowledge: [candidate_full_access, status_filter, context_budget, hard_secret_denial, visibility]
  evidence: [bundle_hash, manifests, learning_packet_idempotency, authority_write_false]
