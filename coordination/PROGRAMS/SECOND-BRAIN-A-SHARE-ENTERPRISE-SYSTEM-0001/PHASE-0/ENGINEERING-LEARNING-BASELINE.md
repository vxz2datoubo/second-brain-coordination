# Engineering Learning Baseline

## Input forecast versus observed outcome

| Forecast | Observation | Result |
|---|---|---|
| B1: enterprise map | 11 bounded contexts, 9 workstreams and authority map produced. | supported |
| B2: reduce duplicate construction | Reuse/adapter/migrate/unknown decisions recorded for core contracts, legacy endpoints, knowledge dirs and TEIF. | supported |
| B3: executable slices | Three candidate slices and one selected replay slice, plus Codex/WorkBuddy packages, were produced. | supported |
| B4: reusable learning | This package links ELR-0001 and ELR-0002 and defines outcome-capture fields. | partial; adoption untested |
| H1: overplanning | Contained by a 20-artifact cap, WIP=2 and a selected P1 slice. | mitigation applied |
| H2: context exhaustion | Large GitHub responses were truncated during audit; staged retrieval and explicit UNKNOWN entries avoided false certainty. | observed/contained |
| H3: protected conflict | No protected body published; delta package used. | contained |
| H4: governance overhead | Not measured yet; P1 must record planning-to-implementation lead time. | open |

## Baseline measures

- audited remote public inputs: Issue #31, active router, forecast, enterprise charter, program index, integration index, QCLAW and multi-agent inputs, PR #8, engineering registry;
- audited local facts: Git state, worktrees, listeners, directory/code/test symbol surfaces, protected-file metadata;
- local code execution tests claimed: none;
- unsafe actions: none;
- newly observed limitation: public program index lagged active router state; routing must privilege ACTIVE-CODEX-TASK.yaml.

## Learning candidates

1. **ELR candidate:** shared dirty workspaces need branch-isolated planning artifacts; the fact should remain an environment-specific hypothesis until repeated.
2. **ELR candidate:** active-task routing and program dashboards can drift; a task must declare precedence and freshness.
3. **ELR candidate:** wide enterprise audits need bounded evidence ledger and unknown registry to prevent context truncation from turning into invented completeness.

These are observations, not new standards. Issue #30 may later decide whether to promote or automate them.
