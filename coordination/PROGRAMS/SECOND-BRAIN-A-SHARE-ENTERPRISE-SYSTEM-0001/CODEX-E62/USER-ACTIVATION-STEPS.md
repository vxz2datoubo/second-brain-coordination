# User Activation Steps (Not Authorized in E62)

agent_id: `CODEX`

No step below is authorized now. It is the smallest later activation checklist after GPT and a new exact user-approval route.

1. The user creates/selects an AWS account, enables billing, and establishes an MFA-protected human control identity unavailable to agents.
2. The user selects a Region and operational owner, cost budget, log-retention policy, revocation owner, and incident contact.
3. The user creates/reviews the KMS key, DynamoDB ledger, issuer Lambda, human-control policy, and invoker-only GitHub OIDC role from a separately approved activation package.
4. The user reviews GitHub supporting controls: immutable OIDC subject setup where compatible, protected authority workflow path, and optional environment/ref/bypass controls.
5. The user performs a synthetic canary only: one approval, one issue, one verification, one revoke/reject path, and no real formal knowledge write.
6. Only a later route may bind an accepted grant to a named private Git knowledge repository. E62 does not create that repository.

Never paste credentials, tokens, private keys, MFA recovery material, cookies, or account secrets into chat, Git, Issues, PRs, templates, or tests.
