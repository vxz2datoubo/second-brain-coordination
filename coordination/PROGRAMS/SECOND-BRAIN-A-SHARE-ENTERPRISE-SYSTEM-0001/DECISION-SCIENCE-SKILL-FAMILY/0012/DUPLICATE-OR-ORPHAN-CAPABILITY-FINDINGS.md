# Duplicate or Orphan Capability Findings

## Critical

- **PR #65 duplicate runtime risk:** query parsing, retrieval, AnswerEvidenceBundle and memory APIs overlap the real PR #57/#58 Python runtime. Current M0 conclusions are rejected until re-audited against those files.
- **Canonical ambiguity in local legacy code:** `F:/aidanao/brain_core` contains useful contracts and trading logic, but it is not automatically the remote canonical memory runtime. Migration must be explicit and one-way.

## High

- **Ghost skill count:** the legacy catalogue's 78 headings represent 71 unique names and include adapters, stores and orchestrators.
- **Uncalibrated probability fragment:** existing weighted fusion scaffolds are experimental; fixed weights plus a sigmoid do not establish calibrated probability.
- **Stale T+1 wording:** same-day resale restrictions on fresh inventory do not mean each stock or seasoned inventory can trade only once.
- **A-share rule drift:** ST/risk-warning, price limits, sessions, after-hours trading and product exceptions require effective-date rule snapshots.

## Medium

- **Orphan methods:** important decision-science terms were mentioned without skill IDs, owners, contracts and validation gates; W12 registration fixes naming but not implementation.
- **Wyckoff circular confirmation:** Spring/Test/SOS/Effort-Result/LPS fragments share evidence and must become one hypothesis graph rather than independent votes.
- **PR #8 and #34 historical assets:** reusable as migration evidence only; their current-state and canonical claims are stale.

## Required Actions

1. Implement the Gap Compiler before child runtimes.
2. Freeze DS-01, DS-02 and DS-10 contracts as the first three child slices.
3. Require explicit correlation groups, provenance and abstention in DS-02.
4. Require an experiment family ledger and failed-trial retention in DS-10.
5. Keep Wyckoff after the probability and research-truth gates.
