# Implementation Roadmap

The authoritative detailed plan is `D0-D6-PROJECT-PLAN.md` and the machine DAG
is `PRIORITY-DEPENDENCY-DAG.yaml`.

1. **D0 reality freeze:** audit public and accessible local evidence. No runtime.
2. **D1 gap governance:** deterministic Gap Compiler and maturity registry.
3. **D2 DS-01:** versioned DecisionFrame candidate.
4. **D3 DS-02:** calibrated shared `ProbabilityEstimate`; no parallel PEOS or personal-model probability object.
5. **D4 DS-10:** experiment-family ResearchAuditReport candidate.
6. **D5 integration:** reference W10/W11/W7 and candidate LearningPacket into the
   PR #57 memory lineage; PR #58 is used only after its independent gate passes.
7. **D6 adversarial/shadow validation:** cross-period, rule-transition and failure
   tests. Promotion remains research-only.

Only one child task is activated at a time. D1 is the next recommended task after
GPT accepts D0; it does not consume one of the three vertical-skill slots.
