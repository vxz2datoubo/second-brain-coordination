# E53 work-package decision and change report

## WP0
Fresh E53 branch and plan-only first commit were created from `a1f7146...`.
The first attempted long worktree path exceeded Windows path limits; it was
abandoned before work and replaced with the compliant direct child
`F:\aidanao-worktrees\e53`.

## WP1-WP3
Chose controlled factories plus immutable source ownership. The narrower
public-contract boundary is explicit in `UNKNOWN-REGISTRY.yaml`.

## WP4
All E52 runtime candidates were rewritten as new E53 files. `E52-SOURCE-SELECTION.yaml`
keeps exact predecessor blobs and rejects historical generated bytecode.

## WP5 change
The original plan's isolated bad-input probes were strengthened after the
first Provider run. Because amend/rebase/history rewrite are forbidden, the
fix is a separate narrow substantive commit `0e6b921...`, which supersedes
`2ec33f7...` as the tested head.

## WP6
The workflow checks out the exact Draft PR head, runs Python 3.11/3.13 by
three seeds, uploads independent canonical/environment artifacts and refuses
anything other than six extracted byte-identical canonical files.

## WP7-WP8
This evidence-only commit is the final repository write. Its self commit and
subsequent receipt-head Provider identifiers are structurally external facts
and will be anchored in PR and Issue comments without a follow-up commit.
