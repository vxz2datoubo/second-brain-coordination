schema: "WorkBuddyTaskBrief/v1"
project: "CREATIVE-INTERACTIVE-FILM-SECOND-BRAIN-0001"
repository: "vxz2datoubo/second-brain-coordination"
task_id: "WORKBUDDY-R586-PR578-REMEDIATION"
route_epoch: 586
active_issue: 586
source_issue: 577
source_pull_request: 578
frozen_source_head: "9d536357755a44ac50ea4352ac7c319d97101779"
independent_review: "pullrequestreview-5108122069"
review_result: "CHANGES_REQUIRED"
canonical_base_at_release_snapshot: "04124e233dc813cca4054851ef6a470b342d82fe"
implementation_branch: "workbuddy/r586-pr578-review-bounded-remediation"

mission: >-
  Build a clean successor from the governed current-main base by exact-path porting the
  E577-authorized PR #578 payload and closing only the two P1 findings in the independent
  review: stale exact-head CI baseline binding and caller-mintable scope baseline identity.

mandatory_findings:
  - id: "P1-01"
    name: "EXACT_HEAD_CI_SCOPE_GATE_USES_STALE_BASELINE"
    required_outcome: >-
      The authoritative pull-request workflow must derive one trusted base SHA from the PR
      event and use that same identity for diff checking and R175 scope verification.
  - id: "P1-02"
    name: "CALLER_SELECTED_BASELINE_CAN_REDEFINE_SCOPE_UNIVERSE"
    required_outcome: >-
      The authoritative scope verifier must fail closed when a caller-supplied baseline does
      not equal the trusted governed base identity; HEAD-as-baseline, unrelated ancestors and
      substituted baselines must not shrink the scope universe.

preserve:
  - "E577 explicit allowlist exactly; no widening"
  - "canonical public-safe policy floor byte-unchanged/read-only"
  - "WorkBuddy R175 benchmark/director/operator content except exact source port"
  - "offline synthetic-only boundary"
  - "no second runtime, ledger, authority or acceptance oracle"

forbid:
  - "modify frozen PR #578 or codex/r175-workbuddy-dependency-closed-integration"
  - "reuse Codex R577 execution authority"
  - "semantic changes outside .github/workflows/creative-runtime-offline.yml, tools/verify_r175_scope.py, tests/test_creative_public_safe_boundary.py"
  - "network/provider/credential/real-user/paid-generation/deployment/trade expansion"
  - "Ready, merge, self-review, self-acceptance, history rewrite"

required_adversarial_regressions:
  - "wrong baseline fails closed"
  - "HEAD-as-baseline fails closed"
  - "unrelated ancestor fails closed"
  - "caller-substituted baseline fails closed"
  - "exact trusted PR base succeeds for an otherwise valid candidate"

completion_signal: "WORKBUDDY_R586_PR578_REMEDIATION_READY_FOR_GPT_REVIEW"
