schema: PDER/v1
task_id: CODEX-A2-1-DIRECTOR-BEAT-PLAN-R180
pre_execution_requirements:
  - fresh main reconcile
  - matching issue route brief claim lease reservation allowlist
  - effective spec snapshot written before branch creation
  - target branch absent at snapshot and created from exact implementation baseline after snapshot
  - fresh branch readback exact head equals baseline
  - R179 writer authority released before R180 activation
  - fresh task-owned standalone clone before substantive code write
execution_requirements:
  - remain within additive fail-closed allowlist
  - no player/session/NarrativeState/job/media/provider/WorkBuddy writes
completion_requirements:
  - full regression and negative tests
  - exact scope verifier
  - git diff --check
  - fresh standalone clone reproduction
  - exact-head GitHub CI
  - Draft executor-evidence-only handoff
  - no independent review request Ready or merge
