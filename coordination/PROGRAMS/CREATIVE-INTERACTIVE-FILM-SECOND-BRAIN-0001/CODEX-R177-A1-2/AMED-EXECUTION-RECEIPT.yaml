schema: AMEDAgentExecutionReceipt/v1
agent_id: CODEX
task_id: CODEX-A1-2-PERSISTENT-SCRIPT-CATALOG-DIRECTORBRIEF-R177
main_delivery: COMPLETE_AT_IMPLEMENTATION_CHECKPOINT
autonomy_applied:
  A:
    - selected canonical JSON plus SHA-256 already established by R176
    - separated one-time atomic materialization from read-only loaded APIs
  B:
    - added exclusive materialization lock to prevent concurrent immutable replacement
    - rejected exact duplicate packages and noncanonical ordering
    - rejected untyped director selection and binding inputs
  C_proposals:
    - future operator command for evidence-based stale-lock recovery
  D_escalations: []
system_feedback: Preserve catalog identity and director binding as reusable downstream contracts; never make catalog persistence a session authority.
