schema: AMEDResearchLedger/v1
task_id: CODEX-A1-2-PERSISTENT-SCRIPT-CATALOG-DIRECTORBRIEF-R177
research_trigger: EXISTING_CANONICAL_IDENTITY_AND_ATOMIC_PERSISTENCE_INVENTORY
sources_used:
  - source: R176 canonical_json and ScriptPackage hash primitives at the technical dependency head
    use: deterministic catalog identity without a competing hash system
  - source: Python standard-library JSON, pathlib, exclusive create, fsync and replace semantics
    use: offline zero-dependency persistence implementation
external_research: NONE_REQUIRED_FOR_BOUNDED_IMPLEMENTATION
negative_results:
  - Filesystem read-only attributes were not used because they are platform-specific and do not replace contract validation.
  - No database was introduced because it would add authority and migration scope without value for two approved fixtures.
