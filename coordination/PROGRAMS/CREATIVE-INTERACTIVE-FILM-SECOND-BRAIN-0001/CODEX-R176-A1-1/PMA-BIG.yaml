schema: PMA_BIG/v1
project: CREATIVE-INTERACTIVE-FILM-SECOND-BRAIN-0001
task_id: CODEX-A1-1-SCRIPTPACKAGE-REGISTRY-R176
route_epoch: 176
fundamental_goal: deterministic immutable multi-script content registry without creating a second story/session authority
mechanism:
  - validate exact script_id + revision + package_hash identity
  - require approved status and known style profile
  - reject tamper and unknown identity fail closed
  - expose read-only validated selection projection for future DirectorBrief/v2
minimum_delivery_first: true
no_scope_drift: true
