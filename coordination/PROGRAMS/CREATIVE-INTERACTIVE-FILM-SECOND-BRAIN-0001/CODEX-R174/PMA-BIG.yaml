schema: PMA_BIG/v1
task_id: CODEX-R173-PUBLIC-SAFE-MIGRATION-REMEDIATION-R174
route_epoch: 174
root_goal: >-
  Restore a mechanically trustworthy public-safe admission boundary and no-shadow migration publication guarantee without broadening runtime authority.
mechanisms:
  capability_gate: AST/import-aware Python + deterministic browser remote-load validation
  surface_gate: config roots mechanically congruent with pull_request triggers
  filesystem_gate: lstat-first no-symlink descendant traversal
  migration_gate: verify immutable legacy source after staging and before final publish
adjacent_lifecycle:
  - verifier_config_parse
  - filesystem_discovery
  - capability_detection
  - CI_trigger_admission
  - receipt_generation
  - migration_stage
  - final_source_revalidation
  - atomic_publish_or_no_target
success_definition:
  - all four independent P1 blockers closed by adversarial tests
  - retained lifecycle/package/replay/tamper suite passes
  - no second authority and no external capability added
