Epoch 101 exists only because epoch-100 implementation/tests completed locally but could not be staged due linked-worktree index permission. It does not accept the implementation yet. Recovery must use a disposable normal clone of exact current main, copy only the four authorized working files byte-for-byte after stale-base target-path safety checks, re-run synthetic/adversarial tests, push the canonical Codex branch, and stop for GPT review. Real private execution, scheduler activation and formal PROJECT/GLOBAL promotion remain locked.
