schema_version: "1.0"
task_id: "CODEX-BRAINOPS-LIVE-ROUTE-ACTOR-POLICY-AND-PRE-CANARY-READINESS-PROOF-0034-E39"
route_epoch: 40
hypotheses:
  - id: H1
    statement: "Adding the exact repository-owner actor policy to both canonical route views will allow E38 route-authority parsing to verify the live main snapshot."
  - id: H2
    statement: "A live unauthenticated public GitHub observation can prove ref/commit/tree/path/blob/content membership without credentials."
  - id: H3
    statement: "The system can report pre-canary route readiness while still blocking because no canonical approval comment was supplied."
  - id: H4
    statement: "Explicitly defining the Python process as trusted removes the false claim of cryptographic in-process unforgeability without weakening the external evidence chain."
failure_modes:
  - "Main ref changes during observation and drift is not rejected."
  - "Route actor policy differs between ACTIVE-CODEX-TASK and central coordination."
  - "A synthetic fixture is mislabeled as live remote evidence."
  - "An approval comment is silently invented or a normal comment is treated as approval."
  - "PR merge ref is tested instead of the exact branch head."
  - "Canary eligibility is confused with canary execution authorization."
required_observations:
  - "Exact observed main commit, tree SHA, route blob SHAs and content SHA256 values."
  - "Public transport outcome and ref-drift result."
  - "Route authority parse result including authorized actor policy."
  - "Explicit APPROVAL_NOT_SUPPLIED_CANARY_BLOCKED outcome."
  - "Exact tested/receipt head CI identities and receipt topology."
