schema_version: "1.0"
forecast_type: "TASK_IMPACT_FORECAST"
created_at: "2026-08-05T08:18:00+08:00"
created_by: "GPT"
task_id: "CODEX-QCLAW-E40-TAKEOVER-STRICT-BYTE-TRUTH-EXACT-OWNERSHIP-SEMANTIC-EVIDENCE-AND-PROVIDER-CLOSURE-0048-E52"
route_epoch: 54
agent: "CODEX"
issue: 166
mode: "project_plan"

expected_value:
  primary: >-
    Convert QQ's partial E40 candidate into a provider-observable, byte-exact and semantically complete knowledge-atomization foundation without trusting local success reports.
  secondary:
    - "Create a reusable exact-byte ownership gate across TXT, Markdown, JSON, JSONL and conversation sources."
    - "Create a real mutation and provider-artifact pattern reusable by later knowledge-digestion modules."
    - "Preserve QQ's useful work through exact provenance while preventing whole-branch contamination."

highest_risks:
  - risk: "CO copies the QCLAW branch wholesale and inherits hidden failures."
    mitigation: "Exact path/blob selection ledger; no merge/cherry-pick; every import remains untrusted until E52 tests and mutations pass."
  - risk: "Local tests repeat E40's shallow assertions and manufacture green results."
    mitigation: "Failure reproduction first, production-source mutation harness and provider-observable exact-head CI."
  - risk: "Canonical artifacts differ legitimately because environment metadata is mixed into the compared payload."
    mitigation: "Separate byte-identical canonical evidence from job-specific environment evidence."
  - risk: "Receipt is created before tested-head provider proof or contains future facts."
    mitigation: "Provider gate before receipt, external post-commit identity and receipt-head rerun."
  - risk: "Secret fixtures leak through candidates, logs or artifacts."
    mitigation: "Synthetic fixtures only, no matched secret return/persistence and artifact secret scan."

expected_failure_modes:
  - "ByteTruthIndex private assignments still writable despite property-level tests."
  - "Parser coverage counts bytes but permits overlaps or wrong owner classes."
  - "JSON and JSONL malformed cases silently fall through."
  - "Relation evidence is a label rather than executable provenance."
  - "Packet remains a digest inventory instead of full content."
  - "Workflow executes only after Draft conversion or artifact generation references unavailable expression/runtime variables."
  - "Cross-seed compare is print-only or compares the wrong files."

acceptance_observables:
  - "Exact plan, tested and receipt heads with direct topology."
  - "Named provider run, six matrix jobs, compare job and seven or more readable artifacts at tested and receipt heads."
  - "Downloaded canonical evidence is byte-identical across all six jobs."
  - "Mutation manifest lists source mutation, expected product gate, observed nonzero exit and restored green result."
  - "Full packet fixture can be independently decoded and recomputed from source bytes."

confidence: "HIGH"
rationale: >-
  The source blockers are directly observable in committed code and workflow. The corrective route is well specified, but successful closure still depends on disciplined staged implementation and independently visible provider evidence.

boundary: "FORECAST_ONLY / NO_MERGE / NO_PRIVATE_CONFIG / NO_TRADE"
