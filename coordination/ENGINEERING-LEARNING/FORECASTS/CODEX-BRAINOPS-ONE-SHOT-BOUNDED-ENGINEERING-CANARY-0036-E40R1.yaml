schema_version: "1.0"
task_id: "CODEX-BRAINOPS-ONE-SHOT-BOUNDED-ENGINEERING-CANARY-0036-E40R1"
route_epoch: 42
forecast_class: "ONE_SHOT_AUTOMATION_AND_EXECUTION_CANARY"
planned_difficulty: "D3"
operator_model_advisory: "User manually selects highest suitable model with high/highest reasoning; agents may not change settings."

primary_risks:
  - id: "R1_DOUBLE_EXECUTION"
    probability: "medium"
    impact: "critical"
    mitigation: "Atomic nonce reservation, one execution count, mutually exclusive App/CLI owner and failure-consumes-count semantics."
  - id: "R2_ROUTE_OR_APPROVAL_DRIFT"
    probability: "medium"
    impact: "high"
    mitigation: "Reprove main ref/commit/tree/path/blob/content and exact public approval comment immediately before reservation."
  - id: "R3_PARTIAL_FAILURE_AFTER_CONSUMPTION"
    probability: "medium"
    impact: "high"
    mitigation: "No retry; terminal failure record, deterministic task-only cleanup and branch rollback."
  - id: "R4_PERMISSION_CREEP"
    probability: "medium"
    impact: "critical"
    mitigation: "A/B ledger, exact allowed paths/actions and hard denial of secrets, model settings, services, accounts, orders and trades."
  - id: "R5_EVIDENCE_FALSE_GREEN"
    probability: "medium"
    impact: "high"
    mitigation: "Exact PR-head checkout/assertion for tested and receipt heads on Python 3.11/3.13 plus public run/job IDs."
  - id: "R6_APP_CLI_FALLBACK_AMBIGUITY"
    probability: "medium"
    impact: "high"
    mitigation: "CLI fallback only before any App attempt; selected owner and non-attempted owner must be logged."
  - id: "R7_EXPIRY_DURING_EXECUTION"
    probability: "low"
    impact: "medium"
    mitigation: "Expiry must be valid at atomic reservation. A started one-shot may finish, but no new attempt or retry after expiry."

success_criteria:
  - "Exact READY route and approval are independently verified."
  - "At most one route-claim execution is attempted and the nonce cannot replay."
  - "All work remains in Draft PR #127 and task-owned paths."
  - "A/B improvements are fully ledgered and reversible."
  - "No secrets, model setting, service, account, order or trade surface is touched."
  - "Tested and receipt heads each pass exact Python 3.11/3.13 CI."
  - "One non-empty evidence-only receipt follows the final tested head."

negative_tests:
  - "App attempt followed by CLI attempt -> reject."
  - "Invocation failure followed by retry -> reject."
  - "Expired or reused nonce -> reject before external effect."
  - "Wrong issue/comment/actor/body binding -> reject."
  - "Route main ref changes between proof and reservation -> reject."
  - "Command targets model config, secrets, public service, account or trading paths -> reject."
  - "Receipt commit contains executable changes -> reject topology."

rollback:
  branch_checkpoint: "eeeb8087c603200595349f0e64cbf813d47747bd"
  rule: "Revert only E40R1 branch commits and clean only task-created resources; never force-push or mutate main."
  one_shot_caveat: "An attempted invocation remains consumed even after code rollback; rollback cannot grant a second attempt."

updated_by: "GPT"
updated_at: "2026-08-02T14:25:00+08:00"
