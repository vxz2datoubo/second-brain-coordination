schema_version: "1.0"
kind: "ENGINEERING_FORECAST"
task_id: "QCLAW-KNOWLEDGE-ATOMIZATION-CLEAN-UTF8-PUBLICATION-EXACT-CI-AND-RECEIPT-RECONSTRUCTION-0011-E30"
route_epoch: 30
agent: "QCLAW"
mode: "project_plan"
forecast_status: "ACTIVE"

estimated_difficulty: "D2"
confidence: 0.78
primary_risks:
  - id: "R1"
    risk: "Decoded E29 payloads may contain syntax or semantic defects hidden by the Base64 wrapper."
    likelihood: "MEDIUM"
    impact: "HIGH"
    mitigation: "Treat decoded payloads as untrusted reconstruction input and require syntax, compile, schema and behavioral tests before reuse."
  - id: "R2"
    risk: "A naive Base64 detector may reject legitimate encoded data fields or miss wrapped files with whitespace."
    likelihood: "MEDIUM"
    impact: "MEDIUM"
    mitigation: "Scope detection by path/file role, decode plausibility, printable ratio, magic prefixes and expected syntax; include positive and negative fixtures."
  - id: "R3"
    risk: "Exact byte spans drift under CRLF/LF or Unicode normalization."
    likelihood: "HIGH"
    impact: "HIGH"
    mitigation: "Operate on original bytes, define newline and Unicode normalization policy explicitly, and test Windows/Linux archive roots."
  - id: "R4"
    risk: "Structure-aware splitting can over-fragment shared context or under-split sibling rules."
    likelihood: "MEDIUM"
    impact: "HIGH"
    mitigation: "Represent shared parent/context relations separately and manually inspect a real governance digest."
  - id: "R5"
    risk: "Receipt changes accidentally include executable or semantic files."
    likelihood: "LOW"
    impact: "HIGH"
    mitigation: "Enforce a one-file receipt allowlist and compare tested head to receipt head before final CI."

expected_discoveries:
  - "Whether E29 decoded code is semantically reusable after clean publication or requires partial rewrite."
  - "Which file-role heuristics best distinguish malformed whole-file wrappers from legitimate encoded payload fields."
  - "Whether canonical identity remains stable across CRLF/LF, Unicode and Python versions."
  - "Whether the real governance digest reaches independently judgeable atoms without context loss."

predicted_test_surface:
  unit_tests: "40-80"
  adapter_families: ["Markdown", "TXT", "JSON", "JSONL", "conversation"]
  runtimes: ["Python 3.11", "Python 3.13"]
  isolated_roots_minimum: 3
  exact_head_ci_required: true
  receipt_head_ci_required: true

success_prediction:
  clean_publication: 0.90
  exact_ci: 0.82
  semantic_atomicity: 0.72
  full_completion_in_one_route: 0.68

failure_signals:
  - "Any workflow or source file is a high-entropy Base64-like single line instead of expected syntax."
  - "GitHub Actions does not list the E30 workflow for the exact candidate head."
  - "A sibling UNKNOWN or negation changes unrelated siblings' types."
  - "Canonical bytes differ across roots, hash seeds or Python versions without an explicit normalization reason."
  - "Receipt commit is empty, changes more than the evidence allowlist, or is not the direct child of tested head."

learning_capture:
  wpdcr_required: true
  unplanned_improvement_ledger_required: true
  unknown_registry_required: true
  negative_evidence_retention_required: true

updated_by: "GPT"
updated_at: "2026-08-02T11:33:00+08:00"
