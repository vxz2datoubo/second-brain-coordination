schema_version: "1.0"
forecast_id: "QCLAW-E37-FORECAST"
task_id: "QCLAW-KNOWLEDGE-ATOMIZATION-BYTE-TOKENIZER-MUTATION-TESTS-REDACTION-MAPPING-SEMANTIC-PACKET-AND-EXACT-CI-CLOSURE-0018-E37"
route_epoch: 37
created_by: "GPT"
created_at: "2026-08-03T18:41:00+08:00"
source_issue: 145
source_pull_request: 147
active_issue: 148

baseline:
  accepted:
    - "Fresh branch and Draft PR were created in E36."
    - "Strict UTF-8 and exact coverage directions have partial value."
    - "Raw-byte redaction avoided persistent matched_text."
  failed:
    - "Plan commit mixed implementation and tests."
    - "Receipt was created before the full task and retained a placeholder."
    - "Character indexes were used as byte offsets again."
    - "JSON remained json.loads plus find and lost duplicate-key truth."
    - "Tests were custom counters with hardcoded and vacuous passes."
    - "No E36 workflow or cross-version artifact proof existed."

expected_benefits:
  - id: "B1"
    prediction: "Plan-only and pre-receipt validators will prevent topology poisoning."
    confidence: "medium"
    measure: "First commit file surface is exactly one plan document; final receipt validator rejects synthetic premature fixtures."
  - id: "B2"
    prediction: "Mutation tests will expose character-index and approximate-JSON regressions before receipt."
    confidence: "medium_high"
    measure: "Each required mutation turns at least one real test red and restored code returns it green."
  - id: "B3"
    prediction: "A byte tokenizer and exact ledger will make Markdown/TXT/JSON/JSONL/conversation spans reproducible."
    confidence: "medium"
    measure: "Every fixture has exact-once ownership and source slices round-trip byte-for-byte."
  - id: "B4"
    prediction: "Redaction mapping will permit safe semantic extraction without secret-derived lineage."
    confidence: "medium"
    measure: "Original-to-redacted mapping validates after variable-length replacements; secret/hash probes find no leakage."
  - id: "B5"
    prediction: "Real test cases and cross-version artifacts will replace local PASS-count rhetoric with auditable evidence."
    confidence: "high"
    measure: "E37-named tested and receipt workflow runs expose job/artifact IDs and identical SHA-256 manifests."

expected_harms:
  - id: "H1"
    risk: "Scope remains large and may encourage another partial receipt."
    likelihood: "medium"
    control: "pre_receipt_validator rejects absent modules, Planned/TODO state and missing remote evidence."
  - id: "H2"
    risk: "A hand-written JSON tokenizer may accept invalid syntax or diverge from RFC behavior."
    likelihood: "medium"
    control: "separate lexical preservation from syntax validation; use adversarial fixtures and differential checks only for validity, never for positions."
  - id: "H3"
    risk: "Byte-level ownership may become slow on large documents."
    likelihood: "medium"
    control: "use interval sweeps rather than per-byte dictionaries where practical; include bounded performance tests without weakening correctness."
  - id: "H4"
    risk: "Broad safe-example exemptions may create secret bypasses."
    likelihood: "medium_high"
    control: "exact fixture whitelist and explicit bypass mutations."
  - id: "H5"
    risk: "Remote CI sequencing between tested and receipt heads may be mishandled."
    likelihood: "medium"
    control: "record run/job/artifact identities and validate parent/tested linkage before receipt."

stop_conditions:
  - "Any direct character-index-to-byte conversion remains."
  - "Duplicate JSON keys or raw punctuation/whitespace cannot be reconstructed."
  - "Any source byte lacks exactly one ledger owner."
  - "Secret text/hash/fingerprint appears in serializable output."
  - "Relations can be created by proximity or type pairing."
  - "Tests use custom PASS counters or vacuous predicates."
  - "Receipt is proposed without E37 tested-head remote CI and artifacts."
  - "Model/private configuration or trading boundary is touched."

learning_questions:
  - "Does plan-only enforcement materially reduce topology repair work?"
  - "Which mutation family catches the most real defects: offset, ledger, parser, redaction, semantic or evidence?"
  - "Can interval-level exact coverage remain efficient enough for normal knowledge documents?"
  - "What minimal explicit-link grammar gives useful relations without false semantic invention?"
  - "Can receipt evidence be machine-validated without trusting agent prose?"

post_execution_calibration_required:
  - "Compare predicted benefits/harms with actual findings."
  - "Record mutation kill rate and surviving mutants."
  - "Record real test count, runtime and artifact sizes by Python version."
  - "Record any task wording that was misunderstood."
  - "Promote only independently verified methods into the formal engineering skill library."
