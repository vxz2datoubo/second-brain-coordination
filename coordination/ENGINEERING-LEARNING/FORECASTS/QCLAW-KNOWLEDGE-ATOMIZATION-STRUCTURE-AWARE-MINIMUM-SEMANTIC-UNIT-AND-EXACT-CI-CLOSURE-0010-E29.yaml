schema_version: "1.0"
task_id: "QCLAW-KNOWLEDGE-ATOMIZATION-STRUCTURE-AWARE-MINIMUM-SEMANTIC-UNIT-AND-EXACT-CI-CLOSURE-0010-E29"
route_epoch: 29
hypotheses:
  - id: H1
    statement: "Structure-specific adapters will prevent a numbered governance list from collapsing into one compound atom."
  - id: H2
    statement: "Conservative evidence metadata can prevent lexical words from promoting claims to facts or causality."
  - id: H3
    statement: "Including exact source spans in packet canonicalization will make lineage mutations detectable."
  - id: H4
    statement: "A pull_request workflow with explicit head checkout and identity assertion will close the remote evidence gap."
failure_modes:
  - "Byte spans are counted but canonical content silently strips meaningful whitespace."
  - "A marker in one list item reclassifies the entire list."
  - "JSON/JSONL/conversation support is only file-extension acceptance rather than structural parsing."
  - "Semantic fields are clipped to fixed character limits."
  - "Source spans are excluded from packet hash."
  - "Redaction misses cookies, sessions, refresh tokens, 2FA recovery material or partial-overlap cases."
  - "The workflow contains a deliberate failure placeholder or tests only a merge ref."
  - "The final receipt commit is empty."
required_observations:
  - "Adversarial atom output for mixed numbered rules, UNKNOWNs, exceptions and evidence statements."
  - "Exact reconstruction/normalization report with bytes, gaps and canonical text distinctions."
  - "Cross-root, cross-hash-seed and Python 3.11/3.13 byte-identical packets."
  - "Exact remote source commit/tree/path/blob/content proof for the real digest."
  - "Exact tested/receipt head CI logs and non-empty one-file receipt topology."
