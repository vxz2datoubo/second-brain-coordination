schema_version: "1.0"
forecast_id: "P4-GPT-FULL-KNOWLEDGE-GATEWAY-ADVERSARIAL-EVALUATION-0007"
issue: 38
parent_program: 31
predecessor_merge: "473d0ec15b28ac5e1b70db0b8a6a9ab17738161b"
status: ACTIVE_FORECAST
modes:
  codex: "项目计划模式＋主集成执行"
  qclaw: "目标模式＋独立评测供给线"
expected_positive_effects:
  - "Turn the Phase 3 candidate memory runtime into a GPT-callable local-first knowledge gateway."
  - "Preserve full semantic access to authorized candidate knowledge while keeping authority and transport boundaries separate."
  - "Expose conflicts, UNKNOWN, superseded history, source lineage and context-budget omissions instead of hiding them."
  - "Replace easy language-variant retrieval checks with an independent adversarial benchmark."
  - "Create a deterministic feedback-to-LearningPacket loop with snapshot, rollback and rebuild evidence."
expected_costs:
  - "Large cross-cutting implementation and test surface."
  - "At least two independent Draft PRs and repeated cross-contract validation."
  - "Potential local evidence gate if Codex cannot access an authorized local knowledge source."
key_risks:
  - risk: "A second memory or query runtime is created by QCLAW."
    control: "QCLAW may only deliver fixtures, candidate packets, evaluator, reports and handoff artifacts."
  - risk: "Synthetic evaluation is optimized by rewriting easy variants or lowering thresholds."
    control: "100+ independently labeled adversarial cases with expected and forbidden atoms; failures may not be deleted or relabeled after execution without audit."
  - risk: "Private bodies or credential values enter public GitHub, logs or ContextBundle."
    control: "Local references and hashes only; hard-secret negative tests across success, error, audit and rollback paths."
  - risk: "Candidate knowledge is silently promoted to authority."
    control: "Four-axis access model remains machine-enforced; authority writes are denied."
  - risk: "The task claims a real local gateway without real local evidence."
    control: "Final status must be WAITING_LOCAL_EVIDENCE or PARTIAL when no authorized local source is actually validated."
  - risk: "Retrieval scores look strong while conflicts, UNKNOWN or historical versions leak incorrectly."
    control: "Mandatory 100% gates for conflict recall, UNKNOWN recall, source coverage, forbidden-atom leakage and credential leakage."
acceptance_signals:
  - "One canonical Phase 3-derived memory and query runtime."
  - "100+ adversarial queries executed; at least 95 pass and remaining cases are predefined valid abstentions."
  - "2,000 synthetic atoms, deterministic rebuild, snapshot and rollback pass."
  - "Credential-value leakage is zero."
  - "Authorized candidate semantics, sources, conflicts, UNKNOWN and history are queryable."
  - "No realtime, broker, account, order or profitability claim."
stop_conditions:
  - "Any actual credential value is read, logged, packaged or returned."
  - "Private knowledge body would need to be committed to the public repository."
  - "A second store, fusion engine, QueryPlan runtime or ContextBundle runtime is introduced."
  - "Cloud, paid infrastructure, local service modification or authority promotion is required without user approval."
calibration_required: true
reviewer: GPT
updated_at: "2026-07-22"
