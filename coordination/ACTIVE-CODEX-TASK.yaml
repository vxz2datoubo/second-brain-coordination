schema_version: "103.2"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "CODEX"
active_issue: 231
active_pull_request: null
task_id: "CODEX-CLTM-0021-PRIVATE-STORE-WRITABILITY-DIAGNOSTIC"
module_id: "CONVERSATIONAL-LONG-TERM-MEMORY-0021"
route_epoch: 91
mode: "bounded_private_store_writeability_diagnostic"
mode_label: "【Codex有界诊断】CLTM-0021 private root / SQLite 写能力诊断"
status: "READY"
execution_allowed: true
automatic_resume_within_task: false
second_user_start_command_required: true
intermediate_gpt_review_required: true
merge_authorized: false
formal_knowledge_persistence_unlock_allowed: false
real_private_candidate_ingestion_allowed: false
real_private_raw_body_publication_allowed: false
private_source_discovery_allowed: false
private_candidate_source_read_allowed: false
repository_visibility_change_authorized: false
production_mcp_gateway_deployment_authorized: false
private_repository_creation_authorized: false
permission_or_acl_change_authorized: false

canonical_main_at_route_publish: "f87de0645b3d76b274e78e8abb42437e412df775"

predecessor:
  task_id: "CODEX-CLTM-0021-SCRIPT-RUNNER-PRIVATE-CANDIDATE-CANARY"
  route_epoch: 90
  disposition: "PRIVATE_CANARY_STORE_OPEN_FAILURE_NO_RETRY"
  gpt_execution_audit_issue_comment: 5275194964
  binding_status_observed: "PRIVATE_SOURCE_BINDING_CONFIGURED"
  runner_compile_gate: "PASS"
  candidate_gate_path: "EXACTLY_ONE_BRANCH_REACHED_STORE_OPEN"
  private_candidate_ingested: false
  formal_project_global_write: "LOCKED"
  epoch90_retry_allowed: false

epoch91_preexecution_refresh_observation:
  gpt_audit_issue_comment: 5275420683
  diagnostic_attempt_consumed: false
  fetch_head_write_denied_observed: true
  git_object_write_denied_observed: true
  local_origin_main_stale_after_failed_fetch: true
  local_git_repair_authorized: false

epoch91_exact_sha_api_observation:
  gpt_audit_issue_comment: 5275656885
  diagnostic_attempt_consumed: false
  git_ls_remote: "PASS"
  remote_main_sha_observed: "f87de0645b3d76b274e78e8abb42437e412df775"
  powershell_raw_https_read: "FAILED"
  curl_schannel_read: "FAILED"
  gh_auth_status: "INVALID"
  gh_reauthentication_required_for_this_task: false
  credentials_or_token_request_allowed: false
  gpt_remote_runtime_delta_audit:
    base: "c625eaf247298051e6e23d530f2203dc182e7177"
    head: "f87de0645b3d76b274e78e8abb42437e412df775"
    changed_paths:
      - "coordination/ACTIVE-CODEX-TASK.yaml"
    relevant_cltm_runtime_implementation_changed: false

fundamental_goal: >-
  Determine whether the explicit CLTM private data-root is writable by the current Codex/Python process and whether the existing
  MemoryStore SQLite/WAL initialization can create a fresh database there, without reading any private candidate source and without
  performing ingestion or canary execution.

canonical_remote_read_policy:
  rationale: >-
    The current linked worktree cannot reliably write FETCH_HEAD or fetched Git objects, while git ls-remote succeeds. Canonical task
    acquisition must therefore be independent of the broken linked-worktree Git database and of PowerShell/curl/gh authentication.
  remote_head_gate: >-
    Run `git ls-remote https://github.com/vxz2datoubo/second-brain-coordination.git refs/heads/main` and parse exactly one 40-hex SHA
    for refs/heads/main. Do not update local refs or write objects in the linked worktree.
  exact_sha_http_or_gh_optional:
    rule: >-
      Exact-SHA raw HTTPS or authenticated gh/API may be used only if already working. Do not re-authenticate gh and never request,
      print or paste a token/credential for this task. If these methods fail, continue only to the disposable-temp-Git fallback below.
  disposable_temp_git_fallback:
    purpose: >-
      Use Git's working remote transport while isolating all fetched public objects from the broken linked worktree.
    location: "Unique task-owned directory under the OS temporary directory only; never repository/worktree and never CLTM private root."
    repository_shape: "Initialize a bare disposable Git repository; no checkout or working tree."
    allowed_sequence:
      - "Create one unique empty OS-temp directory owned by this task."
      - "Initialize it as a bare Git repository."
      - >-
        Fetch `refs/heads/main` from `https://github.com/vxz2datoubo/second-brain-coordination.git` into that disposable bare repository
        with no tags and shallow history. This fetch may write only inside the disposable temp repository.
      - "Resolve the fetched commit and require it to equal the SHA returned by the immediately preceding ls-remote gate."
      - >-
        Read only `coordination/ACTIVE-CODEX-TASK.yaml` from that fetched exact commit using Git object inspection; do not checkout files.
      - "Validate the exact control contract, then delete only the task-owned disposable temp repository before any private-root access."
    remote_drift_rule: >-
      If the temp fetch resolves a different main SHA than the preceding ls-remote result, treat this as concurrent remote drift, clean
      only the task-owned temp repository, and stop before private-root access. Do not loop or retry automatically.
    auth_rule: >-
      Do not invoke `gh auth login`, credential-manager mutation, token prompts or credential persistence. If Git fetch unexpectedly
      requires interactive credentials, clean the task-owned temp repository and stop.
    canonical_read_mode_public_value: "TEMP_BARE_GIT_EXACT_HEAD"
  control_validation: >-
    Require repository vxz2datoubo/second-brain-coordination, task_id CODEX-CLTM-0021-PRIVATE-STORE-WRITABILITY-DIAGNOSTIC,
    route_epoch 91, status READY, execution_allowed true, and all epoch-91 private/formal/live/production/trading locks intact.
  fail_closed: >-
    If ls-remote, disposable temp Git fetch, exact-head equality, control object read, cleanup or validation fails, stop before reading
    CLTM_PRIVATE_DATA_ROOT or performing any diagnostic write.
  broken_linked_worktree_mutation_forbidden:
    - "Do not git fetch in the linked worktree after its object-write failure."
    - "Do not update origin/main or any local ref in the linked worktree."
    - "Do not repair, chown, chmod, icacls, reset, prune, gc or otherwise modify its .git/worktrees or object storage."
    - "Do not merge, checkout, reset or modify the linked working tree."
  canonical_authority: "Exact remote main SHA plus ACTIVE-CODEX content read from the exact fetched commit in isolated temp Git storage."

main_drift_policy:
  rule: >-
    Do not require remote main to equal a historical pinned SHA. The current exact remote main SHA may advance because multiple agent
    routes share the repository. Proceed only when the exact-head ACTIVE-CODEX remains this route_epoch 91 task with all locks intact.
    If exact remote control or relevant CLTM runtime state is ambiguous, stop before local diagnostic writes.

pre_task_context_policy:
  no_agent_memory_bootstrap: true
  forbidden_local_context_reads:
    - "Codex memory files or directories"
    - "user home/profile history or notes"
    - "browser/profile/history/session data"
    - "other private local context not explicitly authorized by this route"
  allowed_context:
    - "exact remote canonical control plane acquired under canonical_remote_read_policy"
    - "public repository code required for MemoryStore import"
    - "task-owned OS-temp bare Git repository used only for canonical control acquisition"
    - "CLTM_PRIVATE_DATA_ROOT environment variable value, process-local use only, never print it"
    - "OS temporary runner created by this task"
  forbidden_private_source_access: >-
    Do not read, open, stat for contents, parse, validate or otherwise access the bound DailyMemoryCandidate-v2 source in epoch 91.
    Do not call private_source_binding_status or load_daily_memory_candidate_v2.

public_runtime_bootstrap:
  source_roots_relative_to_repo:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/PHASE-3-INTEGRATED-OFFLINE-MEMORY/src"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/PHASE-3-LOCAL-ADAPTER-IMPLEMENTATION/src"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/PHASE-2-OFFLINE-VERTICAL-SLICE/src"
  environment_rule: >-
    Prepend only these exact public repository-local source roots to PYTHONPATH for the diagnostic child process.
    Do not persist PYTHONPATH to User, Machine, registry, profile or shell startup files.
  current_local_runtime_basis: >-
    GPT independently audited `c625eaf247298051e6e23d530f2203dc182e7177..f87de0645b3d76b274e78e8abb42437e412df775`
    and found only `coordination/ACTIVE-CODEX-TASK.yaml` changed; no CLTM runtime implementation file changed. If remote main advances after
    this route publication and relevant CLTM implementation freshness cannot be established without mutating the linked worktree, stop
    for GPT review rather than running stale local runtime code.

runner_boundary:
  python_c: "FORBIDDEN"
  shell_inline_python_source: "FORBIDDEN"
  runner_location: "OS temporary directory only; never public repository/worktree and never CLTM private root"
  runner_leaf_name: "cltm-private-store-diagnostic-r91.py"
  runner_encoding: "UTF-8"
  compile_gate: >-
    Compile the temporary runner before reading CLTM_PRIVATE_DATA_ROOT or performing any local diagnostic write. On compile failure,
    stop with no private-root access and no retry.
  runner_content_rule: >-
    The runner may contain only fixed orchestration, environment variable names, fixed diagnostic leaf names, MemoryStore invocation,
    fixed status strings and cleanup logic. It must not embed private paths, candidate semantics, user/project scope literals or secrets.

private_root_boundary:
  required_env_name: "CLTM_PRIVATE_DATA_ROOT"
  path_output: "FORBIDDEN"
  discovery: "FORBIDDEN"
  allowed_root_checks:
    - "bound value present"
    - "root exists"
    - "root is directory"
    - "root resolves outside current public repository/worktree"
  if_invalid: "Stop with fixed redacted status; do not create any probe."

r90_artifact_observation:
  fixed_leaf_name: "cltm-private-canary-r90.sqlite3"
  allowed_check: "Existence/type only for this exact leaf; never open, delete, overwrite, reuse or inspect contents."
  allowed_public_values: ["NONE", "FILE", "DIRECTORY", "OTHER"]

plain_write_probe:
  fixed_leaf_name: ".cltm-private-write-probe-r91.tmp"
  freshness_required: true
  if_preexisting: "Stop as DIAGNOSTIC_PROBE_NOT_FRESH; do not delete or overwrite unknown pre-existing content."
  action: >-
    Create this exact leaf with exclusive-create semantics, write only the fixed ASCII marker CLTM_R91_WRITE_PROBE, flush/close,
    then delete only the file created by this task.
  on_failure: "Stop; do not run SQLite probe. Report only fixed redacted failure class."

sqlite_memorystore_probe:
  run_only_after_plain_probe_pass: true
  fixed_leaf_name: "cltm-private-memorystore-probe-r91.sqlite3"
  freshness_required: true
  if_preexisting: "Stop as DIAGNOSTIC_PROBE_NOT_FRESH; do not delete or overwrite unknown pre-existing content."
  action: >-
    Create/connect one fresh MemoryStore at this exact leaf using existing MemoryStore, verify connection/schema initialization succeeds,
    close it, then delete only this task-created probe database and any same-leaf -wal/-shm sidecars if they exist.
  contains_private_candidate: false
  ingestion: "FORBIDDEN"
  query_or_recall: "FORBIDDEN"
  cleanup_failure: "Report fixed cleanup failure and stop; do not broaden deletion scope."

public_receipt_allow_only:
  - "schema_version"
  - "status"
  - "remote_main_sha_observed"
  - "canonical_read_mode"
  - "root_binding_present"
  - "root_shape_valid"
  - "r90_store_leaf_state"
  - "plain_write_probe"
  - "sqlite_memorystore_probe"
  - "cleanup_status"
  - "LOCAL_EXECUTION_ISSUES"
  - "lock_statement"

status_contract:
  success: "PRIVATE_STORE_WRITEABILITY_DIAGNOSTIC_PASS"
  canonical_refresh_blocked: "CANONICAL_CONTROL_READ_ONLY_REFRESH_BLOCKED"
  root_missing_or_invalid: "PRIVATE_ROOT_BINDING_OR_SHAPE_INVALID"
  preexisting_probe: "PRIVATE_STORE_DIAGNOSTIC_PROBE_NOT_FRESH"
  plain_write_failed: "PRIVATE_ROOT_PLAIN_WRITE_PROBE_FAILED"
  sqlite_probe_failed: "PRIVATE_ROOT_MEMORYSTORE_SQLITE_PROBE_FAILED"
  cleanup_failed: "PRIVATE_STORE_DIAGNOSTIC_CLEANUP_FAILED"

forbidden_actions:
  - "Do not call private_source_binding_status in epoch 91."
  - "Do not read or validate the DailyMemoryCandidate-v2 file."
  - "Do not run candidate counting, ingestion, recall or canary execution."
  - "Do not create or reuse cltm-private-canary-r90.sqlite3 or any new canary store."
  - "Do not read Codex memory, home/profile, browser/history or other unrelated local context."
  - "Do not use python -c or shell-inline multiline Python."
  - "Do not scan, enumerate or guess private directories/files."
  - "Do not print private root/path values."
  - "Do not modify ACLs, ownership, Windows Defender/Controlled Folder Access, registry, filesystem policy or permissions."
  - "Do not repair or mutate the broken linked-worktree Git metadata/object store in epoch 91."
  - "Do not re-authenticate gh, credential manager or Git for this diagnostic."
  - "Do not persist PYTHONPATH."
  - "Do not perform a second diagnostic attempt in epoch 91 after any local private-root probe has actually begun."

hard_boundaries:
  - "No formal PROJECT/GLOBAL knowledge write or authority promotion."
  - "No E48/live integration."
  - "No production MCP/Memory App/Gateway deployment."
  - "No repository visibility or permission change."
  - "No private repository creation."
  - "No trading/accounts/funds actions."
  - "No Codex merge or public implementation change is authorized by this diagnostic."

completion_signal: "CODEX_CLTM_0021_PRIVATE_STORE_DIAGNOSTIC_R91_READY_FOR_GPT_REVIEW"
next_command: "OWNER_START_REQUIRED_THEN_GPT_REVIEW_EPOCH_91_PRIVATE_STORE_DIAGNOSTIC"
updated_by: "GPT"
updated_at: "2026-08-13T11:36:00+08:00"
