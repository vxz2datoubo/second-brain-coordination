schema_version: "1.0"
task_id: "CODEX-BRAINOPS-CANARY-NONCE-AUTHORITY-AND-ROUTE-PROOF-CLOSURE-0032-E37"
route_epoch: 38
mode: "target"
mode_label: "【Codex模式：目标模式】BrainOps Canary Nonce、Approval Authority与Route Proof闭环 E37"
owner_agent: "CODEX"
repository: "vxz2datoubo/second-brain-coordination"
issue: 114
source_pull_request: 113
source_tested_head: "7012ed7681d16d936d3ea5ce1311c13b5be46337"
source_receipt_head: "d22aeffc30e5848f2e0cbdb22742ad129f386275"
planned_branch: "codex/brainops-canary-authority-proof-0032-e37"
planned_draft_pr: true
reviewed_base: "dd8aab05109d4b0adec901ace277633546563354"
status: "READY"
execution_allowed: true
automatic_dispatch_allowed: false
canary_execution_allowed: false
completion_signal: "CODEX_BRAINOPS_E37_CANARY_NONCE_AUTHORITY_ROUTE_PROOF_READY_FOR_GPT_REVIEW"

objective: >-
  Preserve E36's useful read-only hardening while repairing the three remaining trust gaps before any real canary can be authorized:
  approval nonce reuse, unverified approval provenance, and caller-supplied route-state hashes. Deliver a fresh tested head and one
  evidence-only receipt on a new branch/PR. Do not execute the canary, App Automation, Codex CLI or automatic GPT review.

verified_input_evidence:
  source_pr: 113
  source_tested_head: "7012ed7681d16d936d3ea5ce1311c13b5be46337"
  source_receipt_head: "d22aeffc30e5848f2e0cbdb22742ad129f386275"
  substantive_ci_run: 30717574429
  receipt_ci_run: 30718668128
  python_311_jobs: [91415730353, 91418597512]
  python_313_jobs: [91415730388, 91418597471]
  tests_each_runtime: "81 PASS"
  accepted_reusable:
    - "Exact lowercase SHA256 event contract."
    - "Bound approval fields for canary/task/epoch/scope/expiry/nonce."
    - "MANUAL_APP excluded from automatic ownership."
    - "Event/idempotency persistence, lease expiry/fencing and value-aware redaction."
    - "Clean tested-head plus one receipt topology and green dual-Python CI."
  blocking_findings:
    - "approval nonce is not persisted or consumed; the same approval can be reused with a new event_id and idempotency_key."
    - "approval_ref is only a syntactically valid identifier and is not bound to an immutable GitHub comment, actor or content hash."
    - "RouteStateEvidence accepts caller-supplied hashes without recomputing them from exact remote main files/blob identities."
    - "route-state observed_at freshness relative to checked_at is not enforced."
    - "No automatic canary or automatic GPT review wake-up occurred."

required_implementation:
  - "Add an approval-consumption ledger with a UNIQUE key over task_id, route_epoch, canary_id and nonce."
  - "Consume the approval nonce atomically in the same transaction as first canary reservation and route-state evidence."
  - "A reused nonce with any different event ID, idempotency key or payload must fail closed without a second state record or external effect."
  - "Model immutable ApprovalEvidence with repository, issue/comment ID, actor, issued_at, body_sha256, approval_ref and exact binding payload."
  - "Require a read-only approval verifier result; arbitrary approval_ref strings are insufficient."
  - "Do not claim webhook-signature verification unless a real signed webhook path exists. Use READ_ONLY_FETCH_VERIFIED or UNKNOWN truthfully."
  - "Bind RouteStateEvidence to exact main commit, both route-file paths, Git blob SHAs and recomputed content SHA256 values."
  - "Reject caller-only hashes, wrong repository/ref/path/blob, mismatched content, future observation times and stale route observations."
  - "Require observed_at <= checked_at and a documented maximum freshness window."
  - "Keep ordinary automatic dispatch, App/CLI invocation and canary execution disabled."
  - "Add adversarial tests for nonce reuse with new IDs, forged approval references, actor/body-hash mismatch, stale/future route proof, blob/content mismatch and transaction rollback."
  - "Retain value-only secret redaction and ensure approval/event bodies are never persisted raw."

commit_and_evidence_shape:
  source_branch_immutable: true
  new_branch_required: true
  new_draft_pr_required: true
  corrective_executable_commits_max: 2
  final_tested_head: "THE_E37_NONCE_AUTHORITY_ROUTE_PROOF_HEAD"
  exact_head_ci_required: ["Python 3.11", "Python 3.13"]
  receipt_commits_allowed: 1
  receipt_commit_class: "NON_EMPTY_EVIDENCE_ONLY"
  receipt_parent_must_equal_tested_head: true
  receipt_head_validation_required: true
  post_receipt_commit_forbidden: true
  prohibited: ["amend", "rebase", "reset", "force_push", "merge", "direct_main_write", "receipt_correction_chain"]

model_reasoning_execution_profile:
  requested_model: "gpt-5.6-sol"
  reasoning_effort: "max"
  execution_intensity: "X4_MAXIMUM"
  permission_profile: "P1_ENHANCED_BOUNDED"
  silent_downgrade_forbidden: true
  actual_profile_reporting_required: true
  inaccessible_actual_profile_value: "ACCESS_NOT_EXPOSED"

hard_boundaries:
  - "LOCAL_FIRST / APP_FIRST / PRE_CANARY_SECURITY_CLOSURE_ONLY / PUBLIC_SAFE / NO_TRADE"
  - "No actual canary, App Automation call, Codex CLI invocation, automatic GPT review, UI automation or private IPC."
  - "No PR #113/#110/#107/#100 or QQ branch edits; use a new branch and Draft PR."
  - "No main write by agent, merge, service installation, firewall change, non-loopback bind, credentials, accounts, orders or trades."
  - "QQ and WorkBuddy remain paused; Codex QQ takeover E27 remains queued."

after_completion: "STOP_AND_REQUEST_GPT_SECOND_PASS"
boundary: "LOCAL_FIRST / APP_FIRST / PRE_CANARY_SECURITY_CLOSURE_ONLY / PUBLIC_SAFE / NO_TRADE"
