schema_version: "1.1"
record_type: "AMEDTaskBrief"
task_id: "QCLAW-PR100-E21-EVIDENCE-PROVENANCE-SELF-AUDIT-EXACT-COMMIT-CI-AND-CUMULATIVE-TRUTH-CLOSURE-0024-E22"
issue_or_pr: "Issue #59 / PR #100"
assigned_agent: "QCLAW"
mode: "target"
mode_label: "【QCLAW模式：目标模式】E21证据来源自审、精确tested commit、相关CI与累计真值闭环E22"
mode_reason: >-
  E21 added useful portable path resolution and self-contained Q0 fixtures, but its machine results and receipt were still generated
  from the old e17_gate_b_r2 workspace with a fixed QClaw interpreter; D05 archived commit 30b991... instead of an E21 descendant;
  no relevant CI exists; the validator does not enforce exact case or archive truth; and cumulative handoff remains stale. Repair is
  bounded to the same candidate PR and evidence surface.
task_weight: "STANDARD"
proactive_mission_authoring_protocol: "coordination/GOVERNANCE/PROACTIVE-MISSION-AUTHORING-AND-BOUNDED-INITIATIVE-GRANT-PROTOCOL-v1.0.yaml"
read_claim_execute_protocol: "coordination/GOVERNANCE/AGENT-READ-TASK-CLAIM-AND-EXECUTE-COMMAND-SEMANTICS-v1.0.yaml"
work_process_reporting_protocol: "coordination/GOVERNANCE/AGENT-WORK-PROCESS-DIFFICULTY-DISCOVERY-AND-COORDINATION-REPORTING-PROTOCOL-v1.0.yaml"

mission_intent:
  fundamental_goal: >-
    Convert E21's portable code candidate into independently replayable evidence whose commands, roots, commit, interpreter, case set,
    CI and cumulative handoff all agree with the exact tested tree.
  user_capability_outcome: "QQ evidence that cannot claim portability while secretly executing an older workspace or ancestor commit."
  current_state_to_change: >-
    PR #100 head 66972df... contains two E21 commits. Source code uses Path(__file__) and sys.executable, but the committed E21 receipt and
    JSON name the old e17_gate_b_r2 path and fixed QClaw Python; D05 names 30b991...; no workflow run exists; AI_HANDOFF remains E18.
  causal_mechanism:
    - "Make the runner and validators bind every command and result to the exact current/archive root and tested SHA."
    - "Reject stale absolute paths, wrong commits, repeated roots and receipt/result drift."
    - "Run the whole evidence chain in Python 3.11/3.13 CI from exact clean archives."
    - "Update cumulative process, difficulty, discovery, UNKNOWN and handoff truth."
  observable_success:
    - "No E22 command/result/receipt contains e17_gate_b_r2, a fixed QClaw Python path or an external workspace."
    - "Three distinct archive roots bind the exact E22 tested commit, not an ancestor or route base."
    - "Expected case catalog, machine JSON and receipt have exact 42-ID equality with no duplicates or drift."
    - "Python 3.11/3.13 CI executes generator, validator, 42-case runner and evidence validators."
    - "AI_HANDOFF, QUALITY-GATE, SUPPLY-CHAIN, AMED, UNKNOWN and WPDCR are cumulative through E22."
  failure_conditions:
    - "Any completion artifact is generated by an old workspace or fixed interpreter."
    - "D05 commit is not the exact E22 tested commit."
    - "Commands are truncated, roots repeat, hashes are shortened or case sets drift."
    - "CI is absent or does not execute PR #100 evidence."
    - "E21 invalid evidence is silently overwritten or reclassified as accepted."
  stop_conditions:
    - "Exact tested commit cannot be archived or run on both Python versions."
    - "Required Q0 fixtures cannot be proven identical to locked sources."
    - "A new canonical runtime, private source, permission or user-gated action would be required."

primary_delivery_and_non_negotiables:
  minimum_required_deliverables:
    - "One E22 corrective tested commit from 66972dfc790b2a8d1ce0a5c6aae692f021736825."
    - "One E22 receipt-only commit after relevant dual-Python CI passes."
    - "E22 machine result JSON, exact-set receipt, archive matrix and cumulative handoff package."
    - "E21 invalid-evidence disposition preserved explicitly."
  frozen_constraints:
    - "PR #100 / branch qclaw/q0-d2-candidate-adapter-0011-e8 / Gate B only."
    - "CANDIDATE_ONLY, research_only, NO_TRADE."
    - "No Gate C/D/E, Issue #92, canonical promotion, private knowledge, secrets, real data, accounts, orders or trading."
    - "No amend, rebase, reset, force push, merge or direct main write."
  required_commit_or_artifact_shape:
    - "Exactly one E22 corrective tested commit."
    - "Exactly one E22 receipt-only commit with no executable changes."
  evidence_gates:
    - "Static and runtime stale-path rejection."
    - "Exact tested SHA and three distinct archive roots."
    - "Full commands and full SHA-256 values."
    - "Exact case-set equality and correct observed outcome semantics."
    - "Relevant Python 3.11/3.13 CI."
    - "Complete cumulative WPDCR and handoff."
  excluded_work:
    - "Reclassifying E21 evidence as valid without rerun."
    - "Changing D2 canonical interfaces or other Agent paths."

system_position:
  workstream: "QCLAW D2 candidate adapter evidence"
  bounded_context: "Gate B machine evidence provenance and cumulative handoff"
  systems_of_record:
    - "PR #100 exact tested and receipt commits"
    - "E22 machine JSON and generated receipt"
    - "GitHub Actions Python 3.11/3.13 jobs"
  upstream_dependencies:
    - "E20 42-case catalog and source locks"
    - "E21 portable runner code candidate"
  downstream_consumers:
    - "GPT independent QCLAW Gate B review"
  canonical_components_not_to_duplicate:
    - "Existing generator, validator, source lock and candidate adapter package"

proactive_mission_assignment:
  initiative_required: true
  optimize_for_fundamental_goal_not_literal_steps: true
  better_than_minimum_is_expected_when_evidence_supports_it: true
  executor_may_choose:
    - "Portable archive harness design and exact-set receipt schema."
    - "Minimal workflow steps and evidence validator layout."
    - "AMED A/B fixes inside the authorized candidate path."
  executor_must_not_wait_for_extra_instruction_to:
    - "Inspect all generated artifacts for stale paths, stale epoch fields and wrong commit anchors."
    - "Fix command truncation and incorrect actual_result semantics."
    - "Add negative tests for old path leakage and ancestor-commit archives."
    - "Report contradictions, failures, unresolved problems and system feedback."
  proposal_or_stop_when:
    - "A canonical runtime or cross-Agent service would be required."
    - "The source fixtures cannot be lawfully or exactly embedded."
    - "The exploration budget would be exceeded."

mandatory_discovery_questions:
  - "Why did the modified portable runner generate a committed receipt from e17_gate_b_r2?"
  - "Why was 30b991 archived, and what exact relationship does it have to the E21 base and head?"
  - "Which validators trusted labels or field presence instead of proving case, root and commit truth?"
  - "Can one reusable evidence schema eliminate repeated receipt/archive mistakes without creating a second canonical runtime?"
  - "What remains unresolved after a clean dual-Python replay?"
  - "Who must coordinate exactly what review, workflow permission or source identity check?"
  - "Which protocol, template, validator, engineering lesson or future task should change from E21?"

anticipated_difficulty_and_unknowns:
  planned_difficulty: "D2_HARD"
  expected_hard_parts:
    - "Separating reusable code from invalid generated evidence."
    - "Binding git archive provenance without a .git directory inside extraction roots."
    - "Ensuring exact command portability on Windows and Linux CI."
  expected_unknowns:
    - "The exact E21 substantive parent SHA until branch topology is machine-recorded."
    - "Whether embedded Q0 fixtures exactly match all locked source bytes."
  evidence_required_before_declaring_blocked:
    - "Commands, exits, full hashes, source fixture comparisons, CI logs and exact failed field."

coordination_and_handoff_design:
  likely_upstream_owners: [GPT, GITHUB_ACTIONS]
  likely_downstream_owners: [GPT_REVIEWER]
  exact_request_shape: "Name workflow/access/source decision, command attempted, result hashes, affected scope and resume condition."
  unaffected_work: "E20 42-case catalog and E21 portable source patterns may be reused; E21 generated evidence receives no credit."
  acceptance_gate: "Exact E22 tested commit, relevant dual-Python CI and cumulative evidence truth."

system_feedback_and_double_loop_targets:
  inspect_and_update_if_supported:
    - "Machine result/receipt exact-set validator"
    - "Archive provenance schema"
    - "Command full-path and truncation rules"
    - "Outcome semantics and stale-path negative tests"
    - "QCLAW task/report templates"
    - "Engineering learning registry"
  final_acceptance_by: "GPT"

research_trigger:
  level: "L1_REUSE_AND_QUICK_CHECK"
  research_ledger_required: true

exploration_budget:
  primary_delivery_share: "80%"
  active_discovery_share: "15%"
  system_opportunity_share: "5%"
  max_new_architecture_proposals: 1
  max_new_skill_candidates: 1
  max_unplanned_files: 3
  scope_expansion_without_gate: false

improvement_authority:
  A_SAFE_LOCAL_AUTONOMOUS:
    allowed_examples: ["exact-set validator", "stale-path scanner", "correct outcome fields", "full command capture"]
  B_BOUNDED_IMPLEMENT_AND_REPORT:
    allowed_examples: ["portable archive evidence carrier", "minimal relevant CI steps"]
  C_PROPOSAL_ONLY:
    examples: ["cross-Agent canonical evidence service", "new Skill"]
  D_PROHIBITED_OR_USER_GATE:
    examples: ["private knowledge", "credentials", "real data", "production or trading access"]

required_evidence:
  commands_and_exit_codes: true
  tests_pass_fail_skip: true
  negative_results: true
  alternatives_and_tradeoffs: true
  rollback: true

mandatory_delivery_artifacts:
  - "E21-INVALID-EVIDENCE-DISPOSITION.yaml"
  - "E22-WORK-PROCESS-AND-COORDINATION-REPORT.yaml"
  - "E22-PORTABLE-COMMAND-LEDGER.json"
  - "E22-ARCHIVE-PROVENANCE-MATRIX.yaml"
  - "E22-TEST-RUN-RECEIPT.md"
  - "E22-OUTCOME-CALIBRATION.yaml"
  - "AI_HANDOFF.yaml"
  - "QUALITY-GATE-REPORT.md"
  - "SUPPLY-CHAIN-HASH-VERIFICATION.yaml"
  - "D05-COMMAND-EVIDENCE.yaml"
  - "AMED-AGENT-EXECUTION-RECEIPT.yaml"
  - "AMED-RESEARCH-LEDGER.yaml"
  - "UNPLANNED-IMPROVEMENT-LEDGER.yaml"
  - "SYSTEM-DISCOVERY-AND-OPPORTUNITY-REPORT.md"
  - "UNKNOWN-REGISTRY.yaml"

executor_lease_acknowledgement:
  must_confirm:
    - "读取任务 means claim and execute E22 immediately."
    - "E21 generated evidence is superseded negative history, not accepted proof."
    - "Only exact E22 tested commit and relevant CI can receive completion credit."

second_pass_audit:
  required: true
  reviewer: "GPT"
  gates:
    - TASK_ASSIGNMENT_INITIATIVE_QUALITY_GATE
    - TASK_COMPLETENESS_GATE
    - FACT_EVIDENCE_GATE
    - ENGINEERING_CORRECTNESS_GATE
    - PROCESS_DIFFICULTY_COORDINATION_GATE
    - SYSTEM_EVOLUTION_GATE
    - NEXT_ACTION_GATE

boundary: "PUBLIC_SAFE / CANDIDATE_ONLY / research_only / NO_TRADE"
