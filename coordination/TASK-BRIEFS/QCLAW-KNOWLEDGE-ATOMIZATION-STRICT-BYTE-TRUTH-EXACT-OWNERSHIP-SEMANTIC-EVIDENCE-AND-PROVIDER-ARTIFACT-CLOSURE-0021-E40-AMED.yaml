schema_version: "1.0"
task_type: "QCLAW_PROJECT_PLAN_EXECUTION"
mode: "project_plan"
mode_label: "【QCLAW模式：项目计划模式】严格字节真值、完整所有权与可观察证据闭环 E40"
repository: "vxz2datoubo/second-brain-coordination"
active_issue: 164
foundation_issue: 20
parent_program: 31
task_id: "QCLAW-KNOWLEDGE-ATOMIZATION-STRICT-BYTE-TRUTH-EXACT-OWNERSHIP-SEMANTIC-EVIDENCE-AND-PROVIDER-ARTIFACT-CLOSURE-0021-E40"
route_epoch: 40
status: "READY"
execution_allowed: true
planned_branch: "qclaw/knowledge-atomization-strict-byte-evidence-closure-0021-e40"
completion_signal: "QCLAW_E40_STRICT_BYTE_TRUTH_EXACT_OWNERSHIP_PROVIDER_ARTIFACT_READY_FOR_GPT_REVIEW"

source:
  issue: 156
  pull_request: 157
  plan_head: "943c23ba61970a4a9bc3decf4811bb40c9b664a8"
  tested_claim: "71dc7588dc1e8439803472b7350f5027ffe7f028"
  frozen_head: "ed5f307fa7fbfd015b21c89b54917faa3fe4cbd4"
  review_record: "coordination/ENGINEERING-LEARNING/REVIEWS/QCLAW-E39-COMPLETE-UPLOAD-FINAL-REVIEW.yaml"
  review_commit: "1677cab9f82299372b21792ea86aa52b4df07f84"
  classification: "E39_TREE_SCOPE_SUBPROCESS_UTF8_DIRECTION_LEDGER_ADAPTER_REDACTION_ATOM_RELATION_PACKET_CANDIDATES_AND_LOCAL_207_TESTS_PRESENT_BUT_S0_REAL_MUTATION_IMMUTABILITY_EOF_LINE_MODEL_EXACT_BYTE_OWNERSHIP_PRODUCT_VALIDATORS_REMOTE_ARTIFACT_CI_AND_PLACEHOLDER_FREE_RECEIPT_FAILED"
  status: "REVIEWED_AND_FROZEN"

objective: >-
  Build the single successor implementation that preserves the valid E39 directions while closing strict UTF-8 progress, immutable indexing, EOF-exclusive mapping, canonical line semantics, exact byte ownership, verified semantic evidence, complete packet serialization, executable validators, active mutations, provider-observable deterministic artifacts and placeholder-free receipt authority.

first_actions:
  - "Read Issue #164, the complete E39 review, frozen Issue #156/PR #157 and current canonical main."
  - "Create a fresh normal descendant branch from the exact canonical main observed at claim."
  - "Commit exactly one E40 PROJECT-PLAN.md as the first commit and prove the canonical base tree is preserved."
  - "Open the sole Draft PR and publish a TaskLeaseClaim/WPDCR packet."
  - "Create E39-SOURCE-SELECTION.yaml before importing candidate code; import selected paths/blobs only, never merge or cherry-pick the frozen branch."

phase_order:
  - "S0 strict UTF-8 progress, immutable indexes, legal EOF-exclusive mapping, process cleanup and canonical line model."
  - "S1 exact owner ledger and complete format adapters with no gaps or overlaps."
  - "S2 redaction lineage and secret safety."
  - "S3 atom semantics, relation evidence and complete canonical packet."
  - "S4 product validators and isolated mutation harness."
  - "S5 exact-head provider artifact CI."
  - "S6 receipt only after independently observable tested-head CI succeeds."

mandatory_controls:
  s0:
    - "Finalized collections are tuples/frozensets or equivalent and resist all external mutation paths."
    - "legal_boundary_to_codepoint_index maps EOF total_bytes to codepoint_count and rejects continuation offsets."
    - "containing-codepoint lookup, if retained, is a separate API."
    - "A copied production source is genuinely mutated at the 0xED progress branch and the isolated worker is forcibly terminated/reaped."
    - "Every TimeoutExpired path kills and joins/communicates the child in finally."
    - "One explicit line model covers empty, no terminator, LF, CRLF, repeated terminators and trailing empty lines."
  ownership:
    - "Every original byte has exactly one ATOM_CANDIDATE, STRUCTURE or UNKNOWN_ERROR owner."
    - "JSON owns whitespace, punctuation, escapes, duplicate keys, grammar errors and trailing bytes."
    - "JSONL parses bounded line slices and offsets spans without cross-line consumption."
    - "Conversation owns role/time/metadata/boundaries/colon/spaces/body/terminators."
    - "Markdown and TXT follow the canonical line model."
  semantics:
    - "Production redaction has no implicit safe-example bypass; fixture bypass is explicit test configuration only."
    - "No secret text/hash/fingerprint/reversible material persists."
    - "Seven atom semantic fields have source rules, UNKNOWN/default handling, validators and mutations."
    - "Relations require executable evidence and valid source/target atoms; adjacency and caller labels do not grant a relation."
    - "Canonical packet serializes full content, not counts-only, and its ID binds the complete non-self-referential payload."
  validators:
    - "No assertTrue(True), import-only, existence-only, documentation-only or print-only gate receives credit."
    - "Critical controls have named isolated mutations and observed nonzero failures."
    - "Placeholder scan covers source, workflow, manifests and every receipt family."
  ci:
    - "Workflow path: .github/workflows/qclaw-e40-strict-byte-evidence.yml."
    - "pull_request base branch is main; exact event head and expected task branch are asserted."
    - "Python 3.11/3.13 and seeds 0/1/777 produce six real canonical artifacts."
    - "Upload uses if-no-files-found: error."
    - "Independent compare fails if any artifact is missing or byte-different; printing is not comparison."
    - "GPT must be able to read exact run, jobs, artifacts, digests, head SHA and conclusions."
  receipt:
    - "Receipt is forbidden before exact tested-head provider CI succeeds."
    - "Receipt is the unique nonempty evidence-only final commit."
    - "No THIS_COMMIT, E40_TESTED_HEAD, TODO, placeholder SHA or future fact."
    - "Receipt identity uses EXTERNAL_POST_COMMIT_PROVIDER_FACT and strict cross-document schemas."
    - "Receipt-head matrix and artifact comparison must also pass before review."

repository_scope:
  allowed_paths:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/QCLAW-KNOWLEDGE-ATOMIZATION-STRICT-BYTE-EVIDENCE-CLOSURE-0021-E40/**"
    - ".github/workflows/qclaw-e40-strict-byte-evidence.yml"
  task_branch_only: "qclaw/knowledge-atomization-strict-byte-evidence-closure-0021-e40"
  source_branch_write_forbidden: true
  source_branch_merge_forbidden: true
  sole_draft_pr_required: true

hard_boundaries:
  - "ENGINEERING_AND_SYNTHETIC_PUBLIC_SAFE_FIXTURES_ONLY."
  - "No model/provider/reasoning/default/thread/subagent/workspace/session/private-config access or mutation."
  - "No real credentials, secrets, accounts, orders, positions, funds, market data or trading."
  - "No Codex or WorkBuddy route mutation."
  - "No direct main write, merge, rebase, force-push or repository-settings mutation."

command_execution_contract:
  trigger: "读取任务"
  second_user_start_command_required: false
  read_only_acknowledgement_forbidden: true
  minimum_progress_before_return: >-
    Verify Issue #164, frozen E39 and current main; create the fresh E40 branch and strict one-file plan commit; open the sole Draft PR; publish the claim and source-selection ledger; begin S0 with failing immutability, EOF-exclusive and real 0xED mutation tests before implementation.
  after_completion: "STOP_AND_REQUEST_GPT_INDEPENDENT_REVIEW"

boundary: "E40_READY / E39_FROZEN / SELECTED_IMPORT_ONLY / S0_FIRST / EXACT_OWNERSHIP / PROVIDER_ARTIFACTS / PLACEHOLDER_FREE_RECEIPT / NO_TRADE"
