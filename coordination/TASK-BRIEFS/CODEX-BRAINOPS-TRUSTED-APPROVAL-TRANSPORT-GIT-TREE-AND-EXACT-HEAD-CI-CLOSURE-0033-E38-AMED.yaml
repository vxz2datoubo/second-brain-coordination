schema_version: "1.0"
task_id: "CODEX-BRAINOPS-TRUSTED-APPROVAL-TRANSPORT-GIT-TREE-AND-EXACT-HEAD-CI-CLOSURE-0033-E38"
route_epoch: 39
agent: CODEX
mode: target
mode_label: "【Codex模式：目标模式】BrainOps可信批准、Git Tree与精确Head CI闭环 E38"
status: READY
source_pr: 115
source_tested_head: "0221d629a80afe1232ebe4b1e05a77af64940851"
source_receipt_head: "665bed5411248f0b9926c4beac4529694387ff70"
planned_branch: "codex/brainops-trusted-authority-exact-ci-0033-e38"
planned_draft_pr: true
completion_signal: "CODEX_BRAINOPS_E38_TRUSTED_APPROVAL_GIT_TREE_EXACT_HEAD_CI_READY_FOR_GPT_REVIEW"

objective: >-
  Preserve E37 atomic nonce consumption while closing three remaining pre-canary trust gaps:
  public forgeable verification constructors, approval comments whose bodies are not parsed into the exact binding,
  and route/CI evidence that does not cryptographically or operationally bind the claimed exact Git identities.

review_disposition:
  classification: "E37_NONCE_TRANSACTION_REUSABLE_BUT_APPROVAL_ROUTE_AUTHORITY_AND_EXACT_HEAD_CI_NOT_CLOSED"
  completion_credit: false
  substantive_hardening_credit: true
  reusable:
    - "Atomic approval nonce consumption, event reservation and route-proof persistence in one SQLite transaction."
    - "Nonce replay suppression even when event ID, idempotency key and payload change."
    - "Rollback on downstream insert failure and raw body non-persistence."
    - "One non-empty evidence-only receipt directly after the declared tested head."
    - "Python 3.11/3.13 each ran 103 tests successfully on PR merge refs."
  blockers:
    - "ApprovalVerificationResult.verified and RouteProofVerification.verified are public forge paths callable without the verifier."
    - "Approval binding payload is copied from the caller approval object, not parsed and verified from the fetched comment body."
    - "No authorized-actor policy proves that the comment author has approval authority."
    - "Route verifier checks caller-provided bytes and hashes but does not prove path-to-blob membership in the declared main commit tree."
    - "The external read-only fetcher is absent, so caller-supplied repository/comment/commit documents remain the trust root."
    - "GitHub Actions checked out pull-request merge commits c6f3099... and 7e22dcf..., not declared heads 0221d629... and 665bed5...."

required_results:
  - "Remove or make inaccessible every public constructor/classmethod that can mint a VERIFIED approval or route result outside the trusted verifier path."
  - "Parse one strict canonical approval block from fetched GitHub comment text with duplicate-key rejection and exact task/epoch/canary/scope/expiry/nonce matching."
  - "Require an explicit authorized approval actor policy bound by the active route; reject arbitrary syntactically valid actors."
  - "Implement a bounded read-only public GitHub transport for this public repository, or return BLOCKED_TRUSTED_TRANSPORT_UNAVAILABLE. No credential or session inspection."
  - "For route proof, independently fetch refs/heads/main and exact file objects; prove commit/ref/path/blob/content linkage, not merely self-consistent caller bytes."
  - "Parse both route YAML documents and require matching task ID, route epoch, READY status, execution_allowed, automatic_dispatch_allowed=false and canary_execution_allowed=false."
  - "Reject redirects to untrusted hosts, oversized responses, unexpected media types, mutable ref drift, path substitution and tree/blob mismatch."
  - "Change E38 workflow to checkout github.event.pull_request.head.sha explicitly and assert git rev-parse HEAD equals that SHA."
  - "Run Python 3.11 and 3.13 on the exact substantive head and exact receipt head; logs must print and verify both identities."
  - "Create exactly one final non-empty evidence-only receipt directly after the final tested head; no later commit."

required_tests:
  - "Public verified() constructor bypass is impossible."
  - "A normal unrelated comment with a matching body hash cannot authorize."
  - "Wrong actor, duplicated approval keys, extra binding fields, stale/expired approval and body mutation fail closed."
  - "Arbitrary bytes with self-consistent blob hashes but absent from the declared commit tree fail closed."
  - "Main ref moving between fetches fails closed."
  - "Wrong route YAML task/epoch/status/flags fail closed."
  - "CI checkout of merge SHA fails the exact-head assertion fixture; explicit head checkout passes."

hard_boundaries:
  - "PRE_CANARY_TRUST_CLOSURE_ONLY / PUBLIC_READ_ONLY / NO_TRADE"
  - "No canary execution, automatic dispatch, App Automation, Codex CLI execution or GPT auto-review."
  - "No credentials, tokens, cookies, sessions, private IPC, UI automation or arbitrary URLs."
  - "No edits to PR #115/#113/#111/#110/#107/#100 or QQ branches; use a new branch and Draft PR."
  - "No main write by agents, merge, service install, account, order or trade."

model_profile:
  requested_model: "gpt-5.6-sol"
  reasoning_effort: "max"
  execution_intensity: "X4_MAXIMUM"
  permission_profile: "P1_ENHANCED_BOUNDED"
  silent_downgrade_forbidden: true
  actual_profile_reporting_required: true

after_completion: "STOP_AND_REQUEST_GPT_SECOND_PASS"
boundary: "PRE_CANARY_TRUST_CLOSURE_ONLY / PUBLIC_READ_ONLY / NO_TRADE"
