schema_version: "1.1"
task_id: "CODEX-BRAINOPS-TRUSTED-PROVIDER-ATTESTATION-CORRECT-GIT-GRAPH-CLEAN-CLONE-REPRODUCTION-AND-STRICT-RECEIPT-VALIDATION-CLOSURE-0046-E50"
route_epoch: 52
target_agent: "CODEX"
mode: "project_plan"
mode_label: "【Codex模式：项目计划模式】可信Provider证明、正确Git图、干净克隆复现与严格回执验证 E50"
mode_reason: >-
  E49 closed synthetic hard-process crash gaps and produced exact-head evidence, but its final verifier has a broken ancestry predicate, trusts a caller-authored EXTERNAL_READ_ONLY_API marker, only text-matches reproduction commands and does not enforce structured provider/receipt schemas. E50 must turn these paper gates into independently anchored executable gates.
active_issue: 160
parent_program: 31
status: "READY"
execution_allowed: true
automatic_dispatch_allowed: false
canary_execution_allowed: false
automatic_resume: false
planned_branch: "codex/brainops-trusted-provider-release-validation-0046-e50"
completion_signal: "CODEX_BRAINOPS_E50_TRUSTED_PROVIDER_RELEASE_VALIDATION_READY_FOR_GPT_REVIEW"

source:
  issue: 158
  pull_request: 159
  base_head: "8db26c0bcf5759425117061f8507dea31dac01e9"
  plan_head: "f3c7dda8dc1f953d1071fd4bd4796fbda7ac5f4f"
  tested_head: "a87fd73b473c8476259038e797e288521a2d804c"
  receipt_head: "ee702cc596fd4aec3a0f9940f63ba914d5cb1bbd"
  review_record: "coordination/ENGINEERING-LEARNING/REVIEWS/CODEX-E49-INDEPENDENT-REVIEW.yaml"
  review_record_commit: "6251366704a4052087da8d429b9bf64a23b2b163"
  classification: "E49_PLAN_TOPOLOGY_HARD_CRASH_SYNTHETIC_RECOVERY_TESTED_AND_RECEIPT_EXACT_CI_PROVIDER_FACTS_AND_PLACEHOLDER_FREE_RECEIPT_ACCEPTED_BUT_FINAL_GIT_ANCESTRY_PREDICATE_EXTERNAL_PROVIDER_AUTHORITY_CLEAN_CLONE_REPRODUCTION_RECEIPT_SCHEMA_ENFORCEMENT_AND_END_TO_END_MUTATION_FAILED"
  frozen: true

fundamental_rules:
  - "Provider authority cannot be created by a caller-authored marker or success-shaped JSON."
  - "Git predicates are interpreted by documented exit status and tested on real positive and negative graphs."
  - "A reproduction claim exists only when the exact documented command succeeds in an ephemeral clean clone/worktree."
  - "Every receipt family is parsed against a strict schema and cross-bound to route, topology, exact provider facts and completion signal."

required_results:
  - "Correct plan-to-tested ancestry validation using subprocess return code and preserve distinct false-ancestry versus git-unavailable results."
  - "Add a positive end-to-end final validation test on a real base-plan-tested-receipt graph that returns READY_FOR_INDEPENDENT_REVIEW."
  - "Replace caller-marker provider authority with a trusted read-only GitHub adapter or GPT canonical-main post-run attestation bound by source commit, blob and payload digest."
  - "Reject forged, branch-authored or unsigned EXTERNAL_READ_ONLY_API documents even when every field looks valid."
  - "Require exact tested and receipt run IDs, job IDs, Python versions, head SHAs, conclusions, artifact IDs, names, digests and expiry facts."
  - "Require and strictly parse PROVIDER-EVIDENCE-TESTED-HEAD.json and every standard receipt document."
  - "Cross-check task ID, route epoch, agent, source heads, provider facts, identity marker and byte-exact completion signal."
  - "Execute the documented final verifier command in an ephemeral clean clone/worktree with trusted provider material outside the repository."
  - "Prove the clean-clone command reaches READY on a valid graph and fails nonzero for substituted facts."
  - "Retain E49 hard-crash implementation through selected path/blob import only; no whole-branch merge or cherry-pick."

trusted_provider_contract:
  allowed_authority_patterns:
    - "Direct read-only provider retrieval with immutable response identities and canonical digest."
    - "GPT canonical-main post-run attestation with verified commit, blob and canonical payload digest."
  forbidden_patterns:
    - "Trust provider_source: EXTERNAL_READ_ONLY_API from an arbitrary file."
    - "Trust caller-declared run/job/artifact/remote-head fields without independent source binding."
    - "Use a task-branch fixture or JSON as final provider authority."
  same_job_self_certification_forbidden: true
  independent_remote_head_recheck_required: true

receipt_schema_contract:
  required_documents:
    - "AMED-EXECUTION-RECEIPT.yaml"
    - "TEST-RUN-RECEIPT.json"
    - "PROVIDER-EVIDENCE-TESTED-HEAD.json"
    - "UNKNOWN-REGISTRY.yaml"
    - "AI_HANDOFF.yaml"
    - "RESEARCH-LEDGER.md"
    - "UNPLANNED-IMPROVEMENT-LEDGER.md"
    - "SYSTEM-DISCOVERY-AND-OPPORTUNITY-REPORT.md"
    - "WORK-PROCESS-AND-COORDINATION-REPORT.md"
    - "RECEIPT-MANIFEST.json"
  structured_schema_validation: true
  cross_document_invariants: true
  exact_completion_signal_required: true
  placeholder_free: true
  self_commit_sha_forbidden: true
  external_post_commit_identity_marker_required: true
  tested_provider_cross_check_required: true
  receipt_provider_facts_owned_by_gpt_post_commit_review: true

clean_clone_reproduction_contract:
  ephemeral_clone_or_worktree_required: true
  repository_cleanliness_required: true
  exact_documented_command_executed: true
  trusted_provider_attestation_materialized_outside_repository: true
  expected_valid_result: "READY_FOR_INDEPENDENT_REVIEW"
  stale_or_obsolete_argument_fails: true
  missing_external_fact_fails: true

mandatory_mutations:
  - "Treat empty stdout from merge-base --is-ancestor as ancestry failure."
  - "Invert or ignore the ancestry command return code."
  - "Accept a fabricated EXTERNAL_READ_ONLY_API JSON with matching fields."
  - "Accept provider facts from the task branch rather than trusted source/main attestation."
  - "Omit PROVIDER-EVIDENCE-TESTED-HEAD.json."
  - "Change one run, job, artifact, head or digest field."
  - "Use a textual but unexecutable reproduction command."
  - "Skip clean-clone execution while retaining command strings."
  - "Accept the E49 mismatched completion signal."
  - "Append a post-receipt commit."

commit_topology:
  first_commit: "STRICT_PLAN_ONLY_SINGLE_E50_PLAN_FILE"
  implementation_commits: "ONE_OR_MORE_ALLOWED_BEFORE_TESTED_HEAD"
  tested_head_gate: "TRUSTED_PROVIDER_CORRECT_GRAPH_STRICT_SCHEMAS_CLEAN_CLONE_AND_MUTATIONS_COMPLETE"
  tested_remote_gate: "E50_EXACT_HEAD_PYTHON_3_11_AND_3_13_PROVIDER_ARTIFACT_SUCCESS"
  final_commit: "EXACTLY_ONE_NONEMPTY_EVIDENCE_ONLY_STRICT_SCHEMA_RECEIPT"
  receipt_must_be_final_head: true
  post_receipt_commit_forbidden: true

repository_scope:
  allowed_paths:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/BRAINOPS-APP-FIRST-CONTROL-PLANE/**"
    - ".github/workflows/brainops-e50.yml"
  task_branch_only: "codex/brainops-trusted-provider-release-validation-0046-e50"
  source_import_policy: "SELECTED_PATH_AND_BLOB_FROM_FROZEN_E49_ONLY"
  imported_source_manifest_required: true
  whole_branch_merge_or_cherry_pick_forbidden: true
  frozen_source_mutation_forbidden: true
  sole_draft_pr_required: true

ci_contract:
  workflow: ".github/workflows/brainops-e50.yml"
  exact_event_head_checkout: true
  python_versions: ["3.11", "3.13"]
  full_regression_and_e50_tests: true
  positive_and_negative_real_git_graph_tests: true
  trusted_provider_adapter_or_main_attestation_tests: true
  strict_receipt_schema_tests: true
  clean_clone_reproduction_test: true
  active_mutation_harness: true
  evidence_artifacts: true
  tested_head_success_required: true
  receipt_head_success_required: true

hard_boundaries:
  - "ENGINEERING_AND_SYNTHETIC_PUBLIC_SAFE_FIXTURES_ONLY."
  - "Read-only provider metadata only; no repository settings or branch-protection mutation."
  - "No live authority, Canary, App Automation or real CLI execution beyond isolated test fixtures."
  - "No model/provider/reasoning/default/thread/subagent/workspace/session/private-config access or mutation."
  - "No credentials, accounts, orders, positions, funds, market data or trading."
  - "No direct main write by CODEX, merge, force-push, rebase or other-agent route mutation."

command_execution_contract:
  on_trigger: "VERIFY_ISSUE_160_AND_FROZEN_E49_CREATE_FRESH_PLAN_ONLY_PR_THEN_EXECUTE_TRUSTED_PROVIDER_AND_FINAL_RELEASE_VALIDATION"
  second_user_start_command_required: false
  read_only_acknowledgement_forbidden: true
  minimum_progress_before_return: >-
    Verify Issue #160, frozen E49 exact heads and current canonical main; create the fresh branch; commit exactly one E50 plan file; open the sole Draft PR; add a failing positive real-graph final-verifier test, a forged-provider-authority test and a clean-clone reproduction test before implementation.
after_completion: "STOP_AND_REQUEST_GPT_SECOND_PASS"

boundary: "TRUSTED_PROVIDER / CORRECT_GIT_GRAPH / CLEAN_CLONE_EXECUTION / STRICT_RECEIPT_SCHEMA / E49_FROZEN / NO_LIVE_AUTHORITY / NO_TRADE"
