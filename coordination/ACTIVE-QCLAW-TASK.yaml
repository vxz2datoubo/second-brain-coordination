schema_version: "76.0"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "QCLAW"
aliases: ["Q", "QQ"]
status: "READY_REMEDIATION"
execution_allowed: true
automatic_resume: true
active_issue: 216
active_pull_request: 221
task_id: "QCLAW-RAW-SOURCE-SEMANTIC-RECONSTRUCTION-KNOWLEDGE-GRAPH-PROJECTION-0030-E48"
route_epoch: 51
mode: "project_plan"
mode_label: "【QCLAW模式：项目计划模式】E48 R2 真实端到端语义Canary修复 + 低置信度Fail-closed + 双版本CI全绿"
planned_branch: "qclaw/raw-source-semantic-reconstruction-graph-projection-0030-e48"
completion_signal: "QCLAW_E48_R2_END_TO_END_SEMANTIC_CANARY_CORRECTED_READY_FOR_GPT_REVIEW"
second_user_start_command_required: false
merge_authorized: false

review_return:
  gpt_review_id: 4904583603
  reviewed_head: "d21aa633166d2a7f968c513d4a2302e6fad35385"
  disposition: "E48_R1_REJECTED_PENDING_R2_END_TO_END_CANARY_CORRECTION"
  accepted_r1_subfixes:
    - "Removed destructive adjacent-Han punctuation rule."
    - "Materialized normalized_text and added deterministic overlap arbitration."
    - "Persisted exact candidate artifact for raw_artifact_sha256 direction."
    - "Vendored/inlined vis-network so HTML can be used offline."
    - "Added task-specific Python 3.11/3.13 CI definition."
  blocking_findings:
    - "Committed canary L1 still contains ambiguities=[] and unknowns=[]; canary_digests reports ambiguity_count=0 and unknown_count=0."
    - "Mid/low-confidence semantic edits are still applied to normalized_text when they win overlap arbitration; tests do not assert applied=false or generated ambiguity/UNKNOWN."
    - "Committed canary source mixes explanatory # metadata with semantic body; trigger tokens in comments make presence-only tests pass on metadata instead of the intended Chinese body."
    - "Persisted L2 canary atoms A001/A002/A003/A004/A005 point to header comment lines rather than the intended volume/price semantic body because the builder assumes semantic data starts at lines 1-3."
    - "L2 unknowns and relations are still manually injected in build_canary_projection.py rather than derived through the actual semantic/evidence pipeline."
    - "Dedicated Python 3.11 and 3.13 unit tests pass, but both overall CI jobs are red because the workflow redirects to canary.out/receipt.txt before creating canary.out."

mandatory_r2:
  - "Separate PUBLIC_SAFE fixture metadata/documentation from the immutable raw semantic canary. The raw semantic source consumed by L0 must contain only the intended synthetic transcript/content, not explanatory trigger examples."
  - "Create one clean committed semantic canary whose actual L1 output itself contains the required high-confidence corrections, at least one medium/low-confidence ambiguity with alternatives, at least one unresolved UNKNOWN, and terminology alias evidence."
  - "Medium/low-confidence semantic corrections must default fail-closed: do not alter normalized_text unless explicitly resolved by an accepted decision. Represent them as applied=false pending alternatives and/or AmbiguityCandidate/UnknownMarker."
  - "Add exact golden tests for the whole committed canary: exact normalized_text, exact applied edit list/spans, exact pending ambiguity/UNKNOWN records, and no ordinary Chinese word corruption. Presence-only edit-type assertions are insufficient."
  - "Fix L2 extraction so atom contents and source spans target the actual semantic body. Add golden assertions for exact A001..An content/span/evidence_kind, not only node counts."
  - "Do not manually inject UNKNOWN or semantic relations solely to satisfy R2. Implement the smallest deterministic L1/L2 derivation step needed so the committed canary's generated semantic/evidence outputs produce the UNKNOWN and conditional/mechanism relations. If a relation cannot be derived safely, keep it UNKNOWN rather than fabricate it."
  - "Regenerate L3 only from the corrected generated L1/L2 package and prove every node/edge is traceable to exact L0 evidence or an explicitly labeled inference/unknown."
  - "Keep the persisted exact L2 candidate artifact and recompute all full SHA-256 digests from the corrected artifact/source."
  - "Keep the vendored offline visualization fix; regenerate graph JSON/GraphML/HTML from corrected projection."
  - "Fix CI ordering: create canary.out before any redirect, run canary exactly once per matrix job unless a second deterministic comparison is intentionally asserted, and upload digest/receipt artifacts."
  - "Require the entire qclaw-e48-semantic-reconstruction workflow to be green on both Python 3.11 and 3.13, not only the unit-test step."
  - "Keep resource/postflight constraints and publish exact new head + CI run IDs + generated digest bundle, then stop for GPT review."

verification_required:
  - "Committed clean canary L1 has nonzero ambiguity_count and nonzero unknown_count generated from the same actual source."
  - "Low-confidence example does not modify normalized_text before explicit resolution."
  - "Golden normalized output is readable Chinese and preserves intended meaning."
  - "L2 atoms contain intended semantic content rather than fixture headers/comments."
  - "L2/L3 UNKNOWN and relation evidence is generated, traceable, and not manually quota-filled."
  - "raw_artifact_sha256 equals exact persisted candidate artifact bytes; l0_source_sha256 remains separate."
  - "Python 3.11 and 3.13 full E48 workflow jobs both succeed, including canary generation and receipt upload."
  - "Resource postflight reports zero task-owned descendants/orphans and no unrelated termination."

branch_policy:
  keep_current_pr_and_branch: true
  additive_correction_commits_only: true
  rebase_required: false
  force_push_forbidden: true
  amend_forbidden: true

formal_persistence:
  status: "BLOCKED"
  candidate_packages_allowed: true
  authoritative_global_or_project_write_allowed: false

resource_policy:
  default_profile: "FOREGROUND_PRIORITY"
  qclaw_task_python_cap: 2
  combined_project_python_cap: 4
  combined_cpu_bound_workers_cap: 1
  nested_parallelism: "FORBIDDEN"
  heavy_matrix_or_dependency_fanout: "REMOTE_CI_OR_BOUNDED_ONLY"
  required_postflight: "zero task-owned descendants, zero orphans, zero unrelated terminations"

hard_boundaries:
  - "No private user transcript/content committed to public repo."
  - "No raw-source overwrite or silent semantic correction."
  - "No fabricated graph edges/relations/UNKNOWN merely to satisfy quotas."
  - "No competing W3 knowledge authority/system-of-record."
  - "No formal GLOBAL/PROJECT persistence."
  - "No credentials/accounts/orders/trading."
  - "No merge/direct-main implementation/rebase/force/amend/history rewrite."

next_command: "读取任务"
updated_by: "GPT"
updated_at: "2026-08-11T17:00:00+08:00"
