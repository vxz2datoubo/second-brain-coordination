schema_version: "75.0"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "QCLAW"
aliases: ["Q", "QQ"]
status: "READY_REMEDIATION"
execution_allowed: true
automatic_resume: true
active_issue: 216
active_pull_request: 221
task_id: "QCLAW-RAW-SOURCE-SEMANTIC-RECONSTRUCTION-KNOWLEDGE-GRAPH-PROJECTION-0030-E48"
route_epoch: 50
mode: "project_plan"
mode_label: "【QCLAW模式：项目计划模式】E48 R1 语义重建正确性修复 + 真实端到端Canary + 双版本CI"
planned_branch: "qclaw/raw-source-semantic-reconstruction-graph-projection-0030-e48"
completion_signal: "QCLAW_E48_R1_SEMANTIC_RECONSTRUCTION_CORRECTED_READY_FOR_GPT_REVIEW"
second_user_start_command_required: false
merge_authorized: false

review_return:
  gpt_review_id: 4904086170
  reviewed_head: "64bf7103121811f1e9b73a61d7b64fbc0413c6c3"
  disposition:
    remote_control_plane_recovery: "ACCEPTED"
    graph_projection_direction: "REUSABLE_CANDIDATE"
    full_digest_direction: "REUSABLE_CANDIDATE"
    l1_semantic_reconstruction: "REJECTED_PENDING_CORRECTION"
  blocking_findings:
    - "Generic Han-to-Han punctuation rule corrupts ordinary Chinese words (e.g. 讨论→讨。论, 如果→如。果, 成交→成。交)."
    - "normalized_text remains identical to raw L0 and overlapping edits are not arbitrated, so L1 is not an applied normalized semantic view."
    - "Committed canary does not end-to-end produce required ambiguity/UNKNOWN/alias/mid-confidence/cross-sentence semantic behavior; tests substitute manual objects/custom strings."
    - "raw_artifact_sha256 is computed over ad-hoc serialized bytes that are not persisted as the exact certification candidate artifact."
    - "Visualization claims self-contained while loading vis-network from CDN."
    - "No E48-specific Python 3.11/3.13 CI evidence observed; only unrelated Phase 3 workflow ran."

mandatory_r1:
  - "Remove the generic adjacent-Han punctuation rule. Default automatic punctuation must be conservative/fail-safe and must not split ordinary Chinese words."
  - "Add golden normalized-output tests for representative Chinese clauses/words, including 讨论/关系/如果/成交/应该 and the full committed canary."
  - "Implement deterministic edit conflict/overlap arbitration. No two applied edits may silently mutate overlapping L0 ranges. Ambiguous overlaps become alternatives/UNKNOWN or fail closed."
  - "Materialize NormalizedSegment.normalized_text as the actual accepted normalized display text while retaining exact immutable L0 raw_text and byte provenance."
  - "Make the single committed noisy-Chinese canary itself exercise all eight required categories through actual generated artifacts: filler/repetition, punctuation, typo, high-confidence homophone, medium-confidence alternative, unresolved UNKNOWN, terminology alias, cross-sentence mechanism/conditional relation."
  - "Do not satisfy required canary behavior by manually constructing UnknownMarker/alias/relation objects in tests while the reconstructor output remains empty."
  - "Persist the exact serialized L2 candidate artifact/blob intended for certification and compute raw_artifact_sha256 over those exact bytes. Keep l0_source_sha256 separately."
  - "Correct all documentation that incorrectly calls raw_artifact_sha256 the L0/raw-source digest."
  - "Either make the graph HTML actually offline/self-contained or truthfully label it CDN-dependent; GraphML/JSON remain valid offline exports."
  - "Add bounded task-specific GitHub Actions CI for the exact E48 suite on Python 3.11 and 3.13, with no nested parallelism/heavy local fanout."
  - "Regenerate all canary outputs and digests after semantic fixes; old generated outputs are not acceptance evidence."

allowed_paths:
  - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/QCLAW-RAW-SOURCE-SEMANTIC-RECONSTRUCTION-KNOWLEDGE-GRAPH-PROJECTION-0030-E48/**"
  - "coordination/PROJECT-PLANS/QCLAW-E48-RAW-SOURCE-SEMANTIC-RECONSTRUCTION-AND-GRAPH-PROJECTION-PLAN-v1.0.md"
  - ".github/workflows/qclaw-e48-semantic-reconstruction.yml"

branch_policy:
  keep_current_pr_and_branch: true
  additive_correction_commits_only: true
  rebase_required: false
  force_push_forbidden: true
  amend_forbidden: true
  current_pr_mergeable_observed: true

verification_required:
  - "Exact E48 tests pass on GitHub Actions Python 3.11 and 3.13."
  - "Golden normalized canary output is semantically readable and contains no word-splitting corruption."
  - "L0 byte hash remains unchanged."
  - "Every applied normalized edit maps to exact L0 span and applied edit ranges are non-overlapping."
  - "Actual canary L1 contains explicit ambiguity/UNKNOWN/alias evidence where required."
  - "Actual L3 relations/UNKNOWN nodes derive from the generated pipeline, not manually substituted test-only objects."
  - "raw_artifact_sha256 matches exact persisted candidate artifact bytes."
  - "Resource postflight: zero task-owned descendants/orphans and no unrelated process termination."

formal_persistence:
  status: "BLOCKED"
  candidate_packages_allowed: true
  authoritative_global_or_project_write_allowed: false

resource_policy:
  default_profile: "FOREGROUND_PRIORITY"
  qclaw_task_python_cap: 2
  combined_project_python_cap: 4
  combined_cpu_bound_workers_cap: 1
  nested_parallelism: "FORBIDDEN"
  heavy_matrix_or_dependency_fanout: "REMOTE_CI_OR_BOUNDED_ONLY"
  required_postflight: "zero task-owned descendants, zero orphans, zero unrelated terminations"

hard_boundaries:
  - "No private user transcript/content committed to public repo."
  - "No raw-source overwrite or silent semantic correction."
  - "No fabricated graph edges/relations/UNKNOWN merely to satisfy quotas."
  - "No competing W3 knowledge authority/system-of-record."
  - "No formal GLOBAL/PROJECT persistence."
  - "No credentials/accounts/orders/trading."
  - "No merge/direct-main implementation/rebase/force/amend/history rewrite."

next_command: "读取任务"
updated_by: "GPT"
updated_at: "2026-08-11T15:51:00+08:00"
