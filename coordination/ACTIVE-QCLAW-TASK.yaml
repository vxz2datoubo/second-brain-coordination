schema_version: "77.0"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "QCLAW"
aliases: ["Q", "QQ"]
status: "READY_REMEDIATION"
execution_allowed: true
automatic_resume: true
active_issue: 216
active_pull_request: 221
task_id: "QCLAW-RAW-SOURCE-SEMANTIC-RECONSTRUCTION-KNOWLEDGE-GRAPH-PROJECTION-0030-E48"
route_epoch: 52
mode: "project_plan"
mode_label: "【QCLAW模式：项目计划模式】E48 R3 知识原子语义完整性 + SOURCE_EXTRACT provenance + 条件关系修正"
planned_branch: "qclaw/raw-source-semantic-reconstruction-graph-projection-0030-e48"
completion_signal: "QCLAW_E48_R3_SEMANTIC_ATOMS_AND_PROVENANCE_CORRECTED_READY_FOR_GPT_REVIEW"
second_user_start_command_required: false
merge_authorized: false

review_return:
  gpt_review_id: 4904878067
  reviewed_head: "ebc822d791bb845d6c73be3631a29bbc5764937e"
  disposition: "E48_R2_L1_AND_CI_ACCEPTED_L2_L3_SEMANTICS_REJECTED_PENDING_R3"
  accepted_r2:
    - "Clean raw semantic canary is separated from explanatory fixture metadata."
    - "Same committed L1 produces nonzero ambiguity and UNKNOWN evidence."
    - "Mid/low-confidence edits fail closed and remain unapplied by default."
    - "Adjacent-Han punctuation corruption remains fixed and normalized_text materialization/overlap arbitration remain accepted."
    - "Persisted exact candidate artifact and full SHA-256 digest pipeline remain accepted in direction."
    - "Offline vendored visualization remains accepted."
    - "Task-specific GitHub Actions run 31478249185 is fully green on Python 3.11 and 3.13 including canary generation and receipt upload."
  blocking_findings:
    - "Normalization operations are promoted as L2 knowledge atoms: filler_removal (including empty content), punctuation, typo and ASR correction nodes pollute the semantic graph."
    - "Several atoms are marked SOURCE_EXTRACT while atom.content differs from the exact cited L0 byte slice (e.g. 部分 vs 部份, 试试 vs 式式, inserted punctuation/alias replacements), violating direct-evidence semantics."
    - "Conditional derivation truncates the effect to `价格` because the regex stops at the first whitespace after `那么`; this is not a complete mechanism/effect clause."
    - "The generated relation `CONDITION -> MECHANISM : REFINES` does not truthfully represent an if/then dependency."
    - "R2 handoff claims exact golden normalized/atom assertions, but current tests mostly assert presence/count/span-membership; EXPECTED.txt references a nonexistent exact-normalized test."
    - "The canary is space-separated between nearly every Chinese token, which overfits boundary regexes and is not representative enough of ordinary Chinese ASR/transcript formatting."

mandatory_r3:
  - "Separate L1 normalization provenance from L2 knowledge semantics. Filler removal, punctuation insertion, typo correction and ASR correction operations must remain L1 audit/provenance records and must not automatically become ordinary CandidateKnowledgeAtoms/L3 knowledge nodes."
  - "Define/retain only meaningful semantic L2 atoms such as concept/condition/mechanism/claim/inference/unknown according to the E47 candidate-knowledge pattern. Do not create atom quotas."
  - "Add a hard SOURCE_EXTRACT invariant: for every atom with evidence_kind=SOURCE_EXTRACT, atom.content must exactly equal the decoded L0 byte slice for its cited source span."
  - "If a useful semantic atom uses normalized wording that differs from L0, classify it as derived/inference/normalized semantic content and carry explicit L0 span + relevant L1 edit provenance; never mislabel it as direct SOURCE_EXTRACT."
  - "Fix conditional extraction so `如果 成交量 上升 那么 价格 就 倾向 于 上升` yields a complete semantic effect/mechanism span such as `价格 就 倾向 于 上升`, bounded before the next contrast/conditional clause."
  - "Use an existing truthful relation type/direction from the accepted E47 vocabulary; preferred target is MECHANISM/effect DEPENDS_ON CONDITION when semantically justified. Do not invent a new canonical relation solely for the canary."
  - "Add exact golden assertions for the full committed canary normalized_text, the complete expected semantic L2 atom list (type/content/evidence_kind/exact span), and the complete semantic relation list (type/direction)."
  - "Add a regression assertion that no normalization-only edit event becomes an L3 knowledge_atom node."
  - "Correct EXPECTED.txt and handoff documentation so every referenced test actually exists and every claim matches committed behavior."
  - "Add at least one small natural-Chinese regression input without word-by-word artificial spaces. It may conservatively retain uncertain filler; the mandatory behavior is no semantic corruption and fail-safe handling."
  - "Regenerate L2/L3, visualization artifacts and all digests after the semantic fix; preserve exact L0 immutability and the accepted full-digest contracts."
  - "Run the full task-specific Python 3.11/3.13 workflow green again and publish exact head/run IDs/postflight before GPT re-review."

verification_required:
  - "No filler_removal/punctuation/typo/asr edit-operation nodes appear as ordinary semantic knowledge atoms in the committed canary graph."
  - "Every SOURCE_EXTRACT content is byte-identical to its exact L0 span."
  - "Normalized/derived wording that differs from L0 is explicitly typed as derived and traceable to L1 edit provenance."
  - "Canary CONDITION and MECHANISM/effect atoms have exact complete expected content and spans."
  - "Conditional relation type and direction express the actual dependency, not REFINES-by-default."
  - "Exact whole-canary normalized_text golden assertion exists and passes."
  - "Exact semantic atom/relation golden assertions exist and pass."
  - "Natural-Chinese no-space regression is fail-safe and uncorrupted."
  - "Full Python 3.11 and 3.13 E48 workflow jobs succeed."
  - "Resource postflight reports zero task-owned descendants/orphans and no unrelated termination."

branch_policy:
  keep_current_pr_and_branch: true
  additive_correction_commits_only: true
  rebase_required: false
  force_push_forbidden: true
  amend_forbidden: true

formal_persistence:
  status: "BLOCKED"
  candidate_packages_allowed: true
  authoritative_global_or_project_write_allowed: false

resource_policy:
  default_profile: "FOREGROUND_PRIORITY"
  qclaw_task_python_cap: 2
  combined_project_python_cap: 4
  combined_cpu_bound_workers_cap: 1
  nested_parallelism: "FORBIDDEN"
  heavy_matrix_or_dependency_fanout: "REMOTE_CI_OR_BOUNDED_ONLY"
  required_postflight: "zero task-owned descendants, zero orphans, zero unrelated terminations"

hard_boundaries:
  - "No private user transcript/content committed to public repo."
  - "No raw-source overwrite or silent semantic correction."
  - "No fabricated graph edges/relations/knowledge atoms merely to satisfy quotas."
  - "No competing W3 knowledge authority/system-of-record."
  - "No formal GLOBAL/PROJECT persistence."
  - "No credentials/accounts/orders/trading."
  - "No merge/direct-main implementation/rebase/force/amend/history rewrite."

next_command: "读取任务"
updated_by: "GPT"
updated_at: "2026-08-11T17:37:00+08:00"
