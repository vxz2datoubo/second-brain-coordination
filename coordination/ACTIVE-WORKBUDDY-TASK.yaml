schema_version: "2.6"
router_protocol: "coordination/WORKBUDDY-TASK-ROUTER.md"
task_lease_protocol: "coordination/GOVERNANCE/AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS-PROTOCOL-v1.0.yaml"
discovery_protocol: "coordination/GOVERNANCE/AGENT-PROACTIVE-DISCOVERY-AND-REALTIME-ESCALATION-PROTOCOL-v1.0.yaml"
amed_policy: "coordination/GOVERNANCE/AMED-ENTERPRISE-POLICY-v1.0.yaml"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "WORKBUDDY"
trigger_phrases: ["读取任务", "执行任务", "开始任务"]

active_issue: 89
task_id: "WORKBUDDY-PR91-R4-FAIL-CLOSED-VERIFIER-AND-FINAL-RECEIPT-TRUTH-CLOSURE"
mode: "target"
mode_label: "【WorkBuddy模式：目标模式】PR #91验证器失败闭锁、真实删除与最终回执收口R4"
mode_reason: >-
  PR #91 R3已经建立真实tested-head/receipt-head链，并使用完整QQ冻结head复现15文件；
  当前只剩删除语义、fail-closed退出行为、最终head扫描和回执真值四类确定性缺口，
  不需要重做H0、QQ语义或系统架构，因此继续使用目标模式。
status: "READY"
execution_allowed: true
blocked_by: "none"
parent_program: 31
source_issue: 89
pull_request: 91
branch: "workbuddy/qclaw-p1-cleanroom-handoff-verification-0023-h0"
reviewed_or_base_head: "486c42e19c3f4689080bc9ff0c8fac093bb4d951"
r3_tested_head: "d6d2df686e8cb5c7b87334371fa05e4eca35de91"
r3_receipt_head: "486c42e19c3f4689080bc9ff0c8fac093bb4d951"
direct_main_write: false
auto_merge: false

route_epoch: 4
route_issued_at: "2026-07-24T15:15:00Z"
completion_signal: "WORKBUDDY_PR91_R4_FAIL_CLOSED_AND_FINAL_RECEIPTS_READY_FOR_GPT_REVIEW"
supersedes:
  - "WORKBUDDY-PR91-R3-SELF-AUDITING-SCANNER-AND-TRUE-TWO-HEAD-CLOSURE"
  - "WORKBUDDY-PR91-R2-MACHINE-RECEIPT-AND-PATH-SCAN-CLOSURE"

lease_claim_required:
  protocol: "AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS"
  exact_echo_fields: [repository, remote_main_head_at_claim, task_id, route_epoch, active_issue, pull_request, branch, status, execution_allowed, completion_signal, reviewed_or_base_head]
  mismatch_result: "STOP_STALE_OR_INVALID_ROUTE"
pre_completion_revalidation:
  required: true
  reread_latest_remote_main: true
  exact_match_fields: [task_id, route_epoch, active_issue, pull_request, branch, completion_signal, execution_allowed]
  mismatch_result: "STALE_COMPLETION_ABORTED_AND_STALEROUTEPACKET_REQUIRED"

amed_task_contract:
  task_weight: "LIGHT"
  research_trigger: "L1_INTERNAL_ENGINEERING_VALIDATION"
  research_reason: "No external research is required; this is a bounded verifier and receipt correctness repair."
  exploration_budget:
    main_delivery_percent: 90
    proactive_discovery_percent: 5
    system_evolution_proposal_percent: 5
  unplanned_improvement_permissions:
    A_DIRECT: "Mechanical changes in the explicitly allowed PR #91 files."
    B_IMPLEMENT_AND_REPORT: "Small deterministic negative tests for verifier exit behavior."
    C_PROPOSAL_ONLY: "Repository-wide scanner service, shared CI or cross-agent governance expansion."
    D_STOP_AND_ESCALATE: "Credential exposure, destructive action, semantic rescore, source activation, account/order/trade or cross-branch edits."
  primary_delivery_priority: "R4 fail-closed verifier and truthful final receipt"

task_impact_forecast:
  first_principles_goal: >-
    Ensure a PR #91 CLEAN/PASS claim can exist only when the scanner and QCLAW reproduction actually succeed,
    the obsolete scanner file is absent from the Git tree, and every receipt names the real final heads without placeholders.
  expected_positive_effects:
    - "Remove the remaining false-success path where internal failure still exits zero."
    - "Make deletion and receipt-head claims match the Git tree exactly."
    - "Close PR #91 machine evidence without reopening accepted semantic work."
  expected_negative_effects:
    - "One additional tested-head/receipt-head cycle."
  execution_cost: "LOW"
  opportunity_cost: "Issue #92 remains blocked until R4 review."
  net_value_assessment: "POSITIVE_HIGH"
  validation_signals:
    - "_r2_scanner.py is absent from the receipt-head Git tree."
    - "Injected scan finding, extraction failure and validator failure each produce non-zero process exit."
    - "All four receipt files contain full tested and receipt heads with no TBD or short SHA."
    - "Final receipt-head scan and lease revalidation are posted with exact hashes."
  stop_conditions:
    - "Latest remote main no longer matches route_epoch 4."
    - "Any secret, destructive action or unauthorized scope is encountered."

review_disposition:
  classification: "R3_CORE_CHAIN_ACCEPTED_FINAL_TRUTH_AND_FAIL_CLOSED_R4_REQUIRED"
  accepted_facts:
    - "R3 is exactly two commits ahead of the rejected R2 head."
    - "R3 tested head d6d2df686e8cb5c7b87334371fa05e4eca35de91 contains the executable verifier changes."
    - "R3 receipt head 486c42e19c3f4689080bc9ff0c8fac093bb4d951 changes exactly the four authorized receipt files."
    - "The lease claim for route_epoch 3 was posted before execution."
    - "The verifier uses the full QCLAW head 63c344084d9af86cb26c1cc65a30d409fefa872f and an explicit 15-file manifest with real extensions."
    - "No QCLAW semantic decision, blocker status, 0.491 score or verdict was changed."
  blocking_defects:
    - id: "WB91-R4-D01"
      fact: >-
        _r2_scanner.py remains in the final Git tree as a one-line tombstone saying it was deleted.
        The receipts claim deletion, so the repository state and receipt statement conflict.
    - id: "WB91-R4-D02"
      fact: >-
        INDEPENDENT-VERIFICATION-SCRIPT.py never exits non-zero when findings exist, a QCLAW file extraction fails,
        the QCLAW validator exits non-zero, repository discovery fails or files are unreadable. It can print FAIL and still return process exit 0.
    - id: "WB91-R4-D03"
      fact: >-
        AI_HANDOFF.yaml and PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml still contain receipt_head TBD at the actual receipt head,
        while the PR completion comment claims the two-head chain is closed.
    - id: "WB91-R4-D04"
      fact: >-
        The final four receipt files do not record the required final receipt-head scan command, final full head, exit code,
        normalized stdout/stderr hashes, tested-to-receipt file list, route_epoch 3 pre-completion remote-main head or exact lease revalidation result.
    - id: "WB91-R4-D05"
      fact: >-
        The scanner omits required Google-token and credential-assignment patterns and hardcodes OS as Windows rather than recording the detected platform.

required_actions:
  - "Claim route_epoch 4 from latest remote main before editing and post the exact lease acknowledgement."
  - "Reuse the existing branch and Draft PR #91; create no replacement PR."
  - "Create a new R4 tested_head that changes only INDEPENDENT-VERIFICATION-SCRIPT.py and deletes _r2_scanner.py through Git deletion."
  - "Make repository discovery failure, unreadable scanned file, any safety finding, any missing/extraction-failed QCLAW file, manifest mismatch, validator absence, timeout or non-zero validator result produce a non-zero verifier exit."
  - "Verify the exact 15-file set against git ls-tree at the full QCLAW frozen head, not only a hardcoded list; reject missing, duplicate or extra expected P1 audit files."
  - "Add Google token-shape and credential-assignment detection using safe-fragment construction; detect the actual OS/platform rather than printing a constant."
  - "Add deterministic negative tests proving at minimum: injected absolute path -> non-zero; missing QCLAW file -> non-zero; forced validator non-zero -> non-zero. Do not alter QCLAW frozen files."
  - "Run the full verifier at one concrete full R4 tested_head and record exact command, environment, timestamps, exit code, counts, manifest hash and normalized stdout/stderr SHA-256."
  - "Create one later receipt_head changing exactly PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml, TEST-RUN-RECEIPT.md, AI_HANDOFF.yaml and WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml."
  - "In all four receipt files record the complete R4 tested_head and receipt_head; remove every TBD, placeholder and short authoritative SHA."
  - "After the receipt commit, run scan-only against the final receipt head and publish full final head, exact command, file count, findings, exit code, manifest hash and normalized stdout/stderr hashes without another commit."
  - "Before posting completion, reread latest remote main and publish remote_main_head_at_pre_completion_revalidation plus exact route_epoch/task/completion-signal match."
  - "Preserve R2 and R3 failures as history; do not rewrite them as passed."
  - "Post completion to PR #91 and Issues #89/#31, then stop."

allowed_changed_files:
  tested_head:
    - "INDEPENDENT-VERIFICATION-SCRIPT.py"
    - "_r2_scanner.py deletion only"
  receipt_head_only:
    - "PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml"
    - "TEST-RUN-RECEIPT.md"
    - "AI_HANDOFF.yaml"
    - "WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml"

preserve_unchanged:
  - "PR #84/#77/#79/#90 and all fixed heads"
  - "QCLAW 42-question statuses, four blocker decisions, 0.491 score and verdict"
  - "Accepted H0/R1 machine facts and R3 two-head structure"
  - "All other PR #91 files"

acceptance_gates:
  - "Lease acknowledgement exactly matches route_epoch 4."
  - "_r2_scanner.py is absent from the final Git tree."
  - "The verifier is fail-closed for every scan, extraction, manifest and validator error path."
  - "Required safety pattern families include local paths, UNC, GitHub/OpenAI/Google tokens, private keys and credential assignments."
  - "The full frozen QCLAW head contains exactly the expected 15 P1 audit artifacts and the unchanged validator passes 37/0/0."
  - "The R4 tested_head precedes a receipt_head changing exactly four authorized receipt files."
  - "All receipts contain full concrete heads and no TBD, placeholders or short authoritative SHA."
  - "The post-receipt final-head scan and route_epoch 4 pre-completion revalidation are published with complete machine hashes."
  - "Every failure, SKIP, UNKNOWN and negative result remains visible."
  - "No semantic rescore, Issue #92 execution, source access, merge, account, order or trade occurs."

forbidden:
  - "No tombstone file in place of an actual Git deletion."
  - "No success exit when an internal required check fails."
  - "No placeholder or short SHA in authoritative completion receipts."
  - "No exclusion of verifier source from safety scanning."
  - "No modification of QCLAW semantics or PR #84/#77/#79/#90."
  - "No Issue #92 execution, source access, labels, replay, backtest or trading."
  - "No merge or auto-merge."

after_completion: "STOP_AND_WAIT_FOR_GPT_REVIEW"
boundary: "PUBLIC_SAFE / CANDIDATE_ONLY / research_only / NO_TRADE"
updated_by: "GPT"
updated_at: "2026-07-24T15:15:00Z"
