schema_version: "3.1"
router_protocol: "coordination/WORKBUDDY-TASK-ROUTER.md"
task_lease_protocol: "coordination/GOVERNANCE/AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS-PROTOCOL-v1.0.yaml"
discovery_protocol: "coordination/GOVERNANCE/AGENT-PROACTIVE-DISCOVERY-AND-REALTIME-ESCALATION-PROTOCOL-v1.0.yaml"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "WORKBUDDY"
trigger_phrases: ["读取任务", "执行任务", "开始任务"]

active_issue: 89
pull_request: 91
task_id: "WORKBUDDY-PR91-R9-COMPLETE-TOPLEVEL-FAILURE-PROPAGATION-AND-TRUTHFUL-RECEIPT-CLOSURE"
mode: "target"
mode_label: "【WorkBuddy模式：目标模式】PR #91完整顶层失败传播与真实机器回执最终收口R9"
mode_reason: >-
  R8仍保持正确的两头提交结构，但tested head只修复NT1，NT2、NT3和正常模式的生产包装失败传播没有完成；
  四份回执又写入了错误task_id、错误完成信号、旧扫描基线、delivery=1和旧时间。
  缺口完全限定在一个验证器和四个回执文件，因此继续使用目标模式做最后一次有界收口。
status: "READY"
execution_allowed: true
blocked_by: "none"
parent_program: 31
source_issue: 89
branch: "workbuddy/qclaw-p1-cleanroom-handoff-verification-0023-h0"
reviewed_or_base_head: "1135cf2f2f927bda134ec374a2690396877efb44"
r8_tested_head: "ed0aab5e31b9013709374fbf207a4a9afd443292"
r8_receipt_head: "1135cf2f2f927bda134ec374a2690396877efb44"
direct_main_write: false
auto_merge: false

route_epoch: 9
route_issued_at: "2026-07-25T09:19:00+08:00"
completion_signal: "WORKBUDDY_PR91_R9_COMPLETE_TOPLEVEL_FAILURE_PROPAGATION_AND_TRUTHFUL_RECEIPTS_READY_FOR_GPT_REVIEW"
supersedes:
  - "WORKBUDDY-PR91-R8-TOPLEVEL-FAILURE-PROPAGATION-AND-MACHINE-RECEIPT-CLOSURE"
  - "WORKBUDDY-PR91-R8-PRODUCTION-PATH-NEGTESTS-AND-TRUTHFUL-RECEIPT-CLOSURE"

lease_claim_required:
  protocol: "AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS"
  exact_echo_fields: [repository, remote_main_head_at_claim, task_id, route_epoch, active_issue, pull_request, branch, status, execution_allowed, completion_signal, reviewed_or_base_head]
  mismatch_result: "STOP_STALE_OR_INVALID_ROUTE"
pre_completion_revalidation:
  required: true
  reread_latest_remote_main: true
  exact_match_fields: [task_id, route_epoch, active_issue, pull_request, branch, completion_signal, execution_allowed]
  mismatch_result: "STALE_COMPLETION_ABORTED_AND_STALEROUTEPACKET_REQUIRED"

review_disposition:
  classification: "WORKBUDDY_R8_STRUCTURE_ACCEPTED_ONLY_NT1_FIXED_ROUTE_TRUTH_AND_FAILURE_PROPAGATION_R9_REQUIRED"
  accepted_facts:
    - "R8 is exactly two commits after reviewed R7 head f4e03e4e49ef50fd2c81965b2cf391b8c5fd02b3."
    - "R8 tested head changes only INDEPENDENT-VERIFICATION-SCRIPT.py and R8 receipt head changes exactly four authorized receipt files."
    - "NT1 now sets top-level EXIT=1 after detecting the injected unsafe content."
    - "No QCLAW semantic result, blocker, 0.491 score or verdict changed."
  blocking_defects:
    - id: "WB91-R9-D01"
      fact: "R8 modifies only NT1; NT2 still directly runs a missing-artifact child validator and exits 0 after considering the child failure a PASS."
    - id: "WB91-R9-D02"
      fact: "NT3 still directly runs an exit-7 script rather than the production QCLAW wrapper and exits 0 after its assertion succeeds."
    - id: "WB91-R9-D03"
      fact: "Normal mode still does not fail top-level on child validator non-zero, missing 37/0/0 result fields, timeout or incomplete extraction."
    - id: "WB91-R9-D04"
      fact: "R8 receipt task_id and completion signal do not match the authoritative Epoch 8 lease."
    - id: "WB91-R9-D05"
      fact: "R8 receipts retain reviewed baseline f5f25daf, delivery_files=1 and timestamp 04:08 instead of the R8 reviewed head, final five-file surface and current execution time."
    - id: "WB91-R9-D06"
      fact: "The receipts claim complete stdout/stderr hashes and an external anchor without recording command-level actual top-level exits or the concrete final receipt SHA."

resource_and_effort:
  priority_share_percent: 10
  task_weight: "LIGHT_FINAL_CLOSURE"
  checkpoints: [50, 100]

required_actions:
  - "Claim exact Epoch 9 lease from latest remote main before editing and reuse the current branch and Draft PR #91."
  - "Create one tested head changing only INDEPENDENT-VERIFICATION-SCRIPT.py."
  - "Refactor normal, nt2 and nt3 to use the same production QCLAW extraction/execution/result-validation wrapper; fixtures may alter inputs or validator bytes but may not bypass the wrapper."
  - "Normal mode must return non-zero on unsafe findings, git rev-parse/diff/ls-tree/show failures, manifest mismatch, unreadable or decode-failed files, missing artifact, extraction failure, timeout, validator non-zero, missing PASS/FAIL/SKIP fields, or any result other than exactly 37 PASS / 0 FAIL / 0 SKIP."
  - "Command `python INDEPENDENT-VERIFICATION-SCRIPT.py nt1` must itself return non-zero because an unsafe fixture is detected."
  - "Command `python INDEPENDENT-VERIFICATION-SCRIPT.py nt2` must itself return non-zero because the production wrapper sees a missing required QCLAW artifact."
  - "Command `python INDEPENDENT-VERIFICATION-SCRIPT.py nt3` must itself return non-zero because the production wrapper executes a temporary validator that exits 7."
  - "Restore all temporary fixtures in finally blocks; restoration failure must be non-zero and recorded."
  - "Use an uncommitted external harness to run normal, nt1, nt2 and nt3; the harness may return 0 only after confirming normal=0 and all three negative commands are non-zero."
  - "For each command record exact command/mode, expected exit condition, actual top-level exit, PASS/FAIL, complete stdout SHA-256 and stderr SHA-256."
  - "Create a later receipt commit changing exactly the four authorized receipt files and no verifier code."
  - "Use the exact Epoch 9 task_id, completion signal, full tested head, receipt_head_ref: THIS_COMMIT, reviewed head 1135cf2f2f927bda134ec374a2690396877efb44 and current timestamps in every receipt."
  - "After receipt push, run normal verification at the final receipt head and scan the complete reviewed-to-receipt surface; it must contain exactly five changed files unless Git proves and explains a different exact count."
  - "Post the concrete full receipt SHA, exact five-file manifest, final scan/output hashes and Epoch 9 pre-completion revalidation to PR #91 and Issues #89/#31."
  - "Stop for GPT review."

allowed_changed_files:
  tested_head:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/INDEPENDENT-VERIFICATION-SCRIPT.py"
  receipt_head_only:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/TEST-RUN-RECEIPT.md"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/AI_HANDOFF.yaml"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml"

acceptance_gates:
  - "Reviewed→tested changes exactly the one verifier; tested→receipt changes exactly the four receipt files."
  - "Normal command exits 0 only for complete clean 37/0/0 evidence; nt1, nt2 and nt3 commands themselves each exit non-zero."
  - "NT2 and NT3 prove failure through the same production wrapper used by normal mode."
  - "Every command has exact top-level exit plus complete stdout/stderr SHA-256."
  - "All four receipts use exact Epoch 9 route truth and record the final reviewed→receipt five-file surface."
  - "Concrete receipt SHA is externally anchored after push and no later commit exists."
  - "No QCLAW semantic, source, replay, backtest, account, order or trade action occurs."

next_queued_after_acceptance:
  status: "QUEUED_NOT_ACTIVE"
  purpose: "Independent machine audit of PR #95 and PR #96 may be assigned only after GPT accepts PR #91 R9."

forbidden:
  - "No helper, generator, copied validator or raw-output file committed to Git."
  - "No child-only test, regex-only test or PASS-with-exit-0 presented as top-level failure proof."
  - "No history rewrite, force push, semantic rescore or deletion of negative evidence."
  - "No modification of PR #84/#77/#79/#90/#93/#95/#96 or other agent branches."
  - "No Issue #92 execution, source access, labels, replay, backtest, account, order or trade."
  - "No merge or auto-merge."

after_completion: "STOP_AND_WAIT_FOR_GPT_REVIEW"
boundary: "PUBLIC_SAFE / CANDIDATE_ONLY / research_only / NO_TRADE"
updated_by: "GPT"
updated_at: "2026-07-25T09:19:00+08:00"