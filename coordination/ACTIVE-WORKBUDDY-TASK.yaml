schema_version: "4.0"
router_protocol: "coordination/WORKBUDDY-TASK-ROUTER.md"
task_lease_protocol: "coordination/GOVERNANCE/AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS-PROTOCOL-v1.0.yaml"
discovery_protocol: "coordination/GOVERNANCE/AGENT-PROACTIVE-DISCOVERY-AND-REALTIME-ESCALATION-PROTOCOL-v1.0.yaml"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "WORKBUDDY"
trigger_phrases: ["读取任务", "执行任务", "开始任务"]

active_issue: 89
pull_request: null
task_id: "WORKBUDDY-QCLAW-P1-CLEANROOM-H1-FRESH-VERIFIER-AND-RECEIPT-CLOSURE"
mode: "target"
mode_label: "【WorkBuddy模式：目标模式】QCLAW P1全新H1干净验证器与真实回执闭环"
mode_reason: >-
  PR #91 R10虽建立共享qclaw_wrapper，但normal仍未验证子进程非零和精确37/0/0，回执又写错PR #101、旧基线、delivery=1和旧时间。
  按R10合同，WorkBuddy在PR #91上的迭代路线停止。为保留独立审计角色但清除十轮历史污染，改为新分支、新目录、新PR的H1干净实现。
status: "READY"
execution_allowed: true
blocked_by: "none"
parent_program: 31
source_issue: 89
branch: "workbuddy/qclaw-p1-cleanroom-h1-final-verification-0023-h1"
reviewed_or_base_head: "44a05d8a466af31f35618d778b15f84709cf3c11"
remote_main_at_release: "44a05d8a466af31f35618d778b15f84709cf3c11"
superseded_pull_request: 91
superseded_pr_head: "0cfd082cbc508675d892615a1283e82ddaf134bb"
direct_main_write: false
auto_merge: false

route_epoch: 11
route_issued_at: "2026-07-25T15:38:00+08:00"
completion_signal: "WORKBUDDY_QCLAW_P1_H1_FRESH_VERIFIER_AND_RECEIPTS_READY_FOR_GPT_REVIEW"
supersedes:
  - "WORKBUDDY-PR91-R10-PRODUCTION-WRAPPER-REFACTOR-AND-FINAL-TRUTH-CLOSURE@route_epoch:10"

lease_claim_required:
  protocol: "AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS"
  exact_echo_fields: [repository, remote_main_head_at_claim, task_id, route_epoch, active_issue, branch, status, execution_allowed, completion_signal, reviewed_or_base_head]
  mismatch_result: "STOP_STALE_OR_INVALID_ROUTE"
pre_completion_revalidation:
  required: true
  reread_latest_remote_main: true
  exact_match_fields: [task_id, route_epoch, active_issue, branch, completion_signal, execution_allowed]
  mismatch_result: "STALE_COMPLETION_ABORTED_AND_STALEROUTEPACKET_REQUIRED"

review_disposition:
  classification: "PR91_R10_CODE_IMPROVEMENT_ACCEPTED_ROUTE_STOPPED_FRESH_H1_REQUIRED"
  accepted_facts:
    - "R10 is exactly two commits after 51698878c79417f950abffc395b628b98ea6d1cd and changes one verifier plus four receipt files."
    - "R10 introduces a shared qclaw_wrapper used by normal, nt2 and nt3."
    - "No QCLAW semantic result, blocker, 0.491 score or verdict changed."
  blocking_defects:
    - id: "WB-H1-D01"
      fact: "normal prints child validator output but never fails on validator nonzero, missing result fields or any result other than exact 37 PASS / 0 FAIL / 0 SKIP."
    - id: "WB-H1-D02"
      fact: "subprocess timeouts are not caught, temporary directory cleanup is not in finally, and NT1 restoration failure is not fail-closed."
    - id: "WB-H1-D03"
      fact: "R10 scan baseline remains f5f25dafe7b02bf0e20e75865e1610d836ab380a rather than the R10 reviewed head."
    - id: "WB-H1-D04"
      fact: "All four R10 receipts use non-authoritative PR #101 task IDs/signals, stale reviewed baseline, delivery_files=1 and the old 04:08 timestamp."
    - id: "WB-H1-D05"
      fact: "No valid Epoch 10 external anchor with concrete receipt SHA, five-file manifest, command exits and hashes exists."

resource_and_effort:
  priority_share_percent: 15
  task_weight: "MEDIUM_CLEAN_REBUILD"
  checkpoints: [25, 50, 75, 100]

first_principles_goal: >-
  Produce one small independent verifier whose code path and receipts are easier to audit than the accumulated PR #91 history, while reproducing the frozen QCLAW evidence without semantic self-review.

frozen_inputs:
  qclaw_pr84_head: "63c344084d9af86cb26c1cc65a30d409fefa872f"
  codex_pr79_head: "b515b66290944af3d3e1d3285e95cb6162736d41"
  qclaw_pr77_receipt_head: "ee86f1d918bc401b06b0278d1db1feb512c31a52"
  expected_qclaw_files: 15
  expected_result: "37 PASS / 0 FAIL / 0 SKIP"
  semantic_verdict_frozen: true

required_actions:
  - "Claim exact Epoch 11, create a new branch from reviewed main head and create a new Draft PR against main. Do not push further commits to PR #91."
  - "Use only coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H1/."
  - "Do not copy PR #91 receipt text or task metadata. PR #91 may be read only as a negative engineering history."
  - "Create exactly five files: INDEPENDENT-VERIFICATION-SCRIPT.py plus four receipt files named PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml, TEST-RUN-RECEIPT.md, AI_HANDOFF.yaml and WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml."
  - "Implement one production wrapper that performs exact 15-file ls-tree verification, extraction, validator execution, timeout handling, stdout parsing and exact 37/0/0 validation."
  - "Return a structured result containing manifest status, extraction status, process exit, PASS/FAIL/SKIP counts, combined hash, files hashed and stdout/stderr SHA-256."
  - "normal exits 0 only when delivery scan is clean, all Git commands succeed, the exact 15-file set matches, validator exit is 0 and parsed result is exactly 37/0/0 with all required fields present."
  - "nt1 injects an unsafe value into an H1 receipt fixture, runs the same production delivery scanner, restores in finally and exits nonzero."
  - "nt2 omits one required frozen artifact before the same wrapper validation and exits nonzero."
  - "nt3 replaces only the temporary extracted validator bytes with a script exiting 7, uses the same wrapper and exits nonzero."
  - "Catch subprocess.TimeoutExpired, OSError, decode errors, missing files and cleanup/restoration errors; each must produce a controlled nonzero exit."
  - "Use a temporary untracked harness to run normal, nt1, nt2 and nt3. Harness success requires top-level exits [0, nonzero, nonzero, nonzero]."
  - "Create a tested commit containing only the verifier, then a receipt commit containing only the four receipts."
  - "Receipts must be generated from actual command results and use the exact Epoch 11 task ID/signal, branch base, tested parent, receipt_head_ref: THIS_COMMIT, fresh timestamps and final five-file manifest."
  - "After receipt push, rerun normal and full reviewed-base-to-receipt scan at the concrete receipt head; externally anchor concrete receipt SHA, tested SHA, exact manifests, exits and stdout/stderr hashes in the new PR and Issues #89/#31."
  - "Stop for GPT review."

allowed_changed_files:
  exact_root: "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H1/"
  exact_files:
    - "INDEPENDENT-VERIFICATION-SCRIPT.py"
    - "PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml"
    - "TEST-RUN-RECEIPT.md"
    - "AI_HANDOFF.yaml"
    - "WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml"

acceptance_gates:
  - "A new Draft PR exists; PR #91 remains unchanged after 0cfd082cbc508675d892615a1283e82ddaf134bb."
  - "Exactly two commits exist: verifier-only tested commit and four-receipt-file commit."
  - "normal, nt2 and nt3 call the identical wrapper; nt1 calls the identical delivery scanner."
  - "Normal top-level exit is 0 only for exact 15 files and exact 37/0/0; nt1/nt2/nt3 top-level exits are nonzero."
  - "Timeout, malformed output, missing required fields, child nonzero, extraction failure and restoration/cleanup failure are demonstrably fail-closed."
  - "Every command has exact expected/actual top-level exit and full stdout/stderr SHA-256."
  - "Final public scan covers exactly the five-file H1 delivery and has zero physical-path or credential-value findings."
  - "Concrete receipt SHA is externally anchored and no later commit exists."
  - "No QCLAW semantic rescore, source, replay, backtest, account, order or trade action occurs."

forbidden:
  - "No modification, reopening or further patching of PR #91."
  - "No helper, generator, copied QCLAW validator or raw-output file committed to Git."
  - "No semantic rescore, blocker change, 0.491 change or verdict adoption/rejection."
  - "No modification of PR #77/#79/#84/#90/#93/#95/#96 or another agent branch."
  - "No Issue #92 execution, source access, labels, replay, backtest, account, order or trade."
  - "No merge, auto-merge, force push or history rewrite."

after_completion: "STOP_AND_WAIT_FOR_GPT_REVIEW"
boundary: "PUBLIC_SAFE / INDEPENDENT_MACHINE_VERIFICATION / research_only / NO_TRADE"
updated_by: "GPT"
updated_at: "2026-07-25T15:38:00+08:00"
