schema_version: "2.8"
router_protocol: "coordination/WORKBUDDY-TASK-ROUTER.md"
task_lease_protocol: "coordination/GOVERNANCE/AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS-PROTOCOL-v1.0.yaml"
discovery_protocol: "coordination/GOVERNANCE/AGENT-PROACTIVE-DISCOVERY-AND-REALTIME-ESCALATION-PROTOCOL-v1.0.yaml"
amed_policy: "coordination/GOVERNANCE/AMED-ENTERPRISE-POLICY-v1.0.yaml"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "WORKBUDDY"
trigger_phrases: ["读取任务", "执行任务", "开始任务"]

active_issue: 89
task_id: "WORKBUDDY-PR91-R6-FUNCTIONAL-NEGTEST-ASSERTIONS-AND-EXTERNAL-ANCHOR-CLOSURE"
mode: "target"
mode_label: "【WorkBuddy模式：目标模式】PR #91功能负测断言、回归恢复与外部锚定收口R6"
mode_reason: >-
  R5已经严格清理三个越权根文件、形成合规tested-to-receipt链并采用THIS_COMMIT模型；
  当前仅剩功能负测未真正断言非零、若干Git命令/不可读文件仍可能静默通过、以及最终外部锚定证据不足。
  缺口文件与验收标准完全确定，因此继续使用目标模式，不重做H0、QQ语义、Codex合同或系统架构。
status: "READY"
execution_allowed: true
blocked_by: "none"
parent_program: 31
source_issue: 89
pull_request: 91
branch: "workbuddy/qclaw-p1-cleanroom-handoff-verification-0023-h0"
reviewed_or_base_head: "654c7e098b2ba56c06e9f3d6493d8d4bcc35ec88"
r5_tested_head: "05c96d97d4008f2599517cf3f3daf976333b4ee3"
r5_receipt_head: "654c7e098b2ba56c06e9f3d6493d8d4bcc35ec88"
direct_main_write: false
auto_merge: false

route_epoch: 6
route_issued_at: "2026-07-25T00:45:00+08:00"
completion_signal: "WORKBUDDY_PR91_R6_FUNCTIONAL_NEGTESTS_AND_EXTERNAL_ANCHOR_READY_FOR_GPT_REVIEW"
supersedes:
  - "WORKBUDDY-PR91-R5-DELIVERY-SURFACE-CLEANUP-AND-EXTERNAL-RECEIPT-ANCHOR-CLOSURE"
  - "WORKBUDDY-PR91-R4-FAIL-CLOSED-VERIFIER-AND-FINAL-RECEIPT-TRUTH-CLOSURE"

lease_claim_required:
  protocol: "AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS"
  exact_echo_fields: [repository, remote_main_head_at_claim, task_id, route_epoch, active_issue, pull_request, branch, status, execution_allowed, completion_signal, reviewed_or_base_head]
  mismatch_result: "STOP_STALE_OR_INVALID_ROUTE"

pre_completion_revalidation:
  required: true
  reread_latest_remote_main: true
  exact_match_fields: [task_id, route_epoch, active_issue, pull_request, branch, completion_signal, execution_allowed]
  mismatch_result: "STALE_COMPLETION_ABORTED_AND_STALEROUTEPACKET_REQUIRED"

amed_task_contract:
  task_weight: "LIGHT"
  research_trigger: "L1_INTERNAL_ENGINEERING_VALIDATION"
  research_reason: "No external research is required; this is a bounded verifier assertion, regression and receipt-anchor repair."
  exploration_budget:
    main_delivery_percent: 95
    proactive_discovery_percent: 3
    system_evolution_proposal_percent: 2
  unplanned_improvement_permissions:
    A_DIRECT: "Mechanical changes inside the single authorized verifier and four receipt files."
    B_IMPLEMENT_AND_REPORT: "Small in-process test modes required to prove expected non-zero exits."
    C_PROPOSAL_ONLY: "Any shared CI, repository-wide scanner service or new governance layer."
    D_STOP_AND_ESCALATE: "Credential exposure, history rewrite, semantic rescore, source activation, account/order/trade or cross-branch edits."
  primary_delivery_priority: "Make every claimed negative test executable, asserted and reproducibly evidenced."

task_impact_forecast:
  first_principles_goal: >-
    Ensure PR #91 can only claim completion when the verifier itself proves, through executable assertions,
    that injected unsafe content, missing QCLAW artifacts and a forced validator failure each produce non-zero outcomes,
    while the final receipt is externally anchored after push.
  expected_positive_effects:
    - "Eliminate print-only negative tests that can report PASS without checking the process result."
    - "Restore exact QCLAW fifteen-file manifest verification removed during R5 simplification."
    - "Close PR #91 without another delivery-surface expansion."
  expected_negative_effects:
    - "One final tested-head and receipt-head cycle."
  execution_cost: "LOW"
  opportunity_cost: "Issue #92 remains blocked until R6 review."
  net_value_assessment: "POSITIVE_HIGH"
  validation_signals:
    - "reviewed-to-tested changes only the main verifier."
    - "Each of three functional negative tests records expected non-zero and actual non-zero exit codes."
    - "All Git discovery/diff commands and unreadable-file branches are fail-closed."
    - "Exact fifteen-file QCLAW ls-tree set check is present and passes."
    - "tested-to-receipt changes exactly four authorized receipt files."
    - "Concrete receipt SHA and epoch-6 revalidation are posted to PR #91 and Issues #89/#31."
  stop_conditions:
    - "Latest remote main no longer matches route_epoch 6."
    - "Any credential, destructive history rewrite or unauthorized scope is encountered."

review_disposition:
  classification: "R5_STRUCTURE_ACCEPTED_FUNCTIONAL_ASSERTIONS_AND_EXTERNAL_ANCHOR_R6_REQUIRED"
  accepted_facts:
    - "R5 is exactly two commits ahead of reviewed R4 receipt head."
    - "R5 tested head 05c96d97d4008f2599517cf3f3daf976333b4ee3 changes exactly the main verifier and deletes the three authorized root files."
    - "R5 receipt head 654c7e098b2ba56c06e9f3d6493d8d4bcc35ec88 changes exactly the four authorized receipt files."
    - "_verify.py, _gen_outputs.py, validator_output.txt and _r2_scanner.py are absent from the final tree."
    - "Receipt files use receipt_head_ref: THIS_COMMIT and preserve historical path exposure truthfully."
    - "No QCLAW semantic decision, blocker, 0.491 score or verdict changed."
  blocking_defects:
    - id: "WB91-R6-D01"
      fact: >-
        The missing-QCLAW negative test executes the validator and prints missing_run.returncode, but never asserts that it is non-zero.
        A zero return would not fail the R5 verifier.
    - id: "WB91-R6-D02"
      fact: >-
        The claimed forced-validator-failure test is not a forced validator execution at all; it only comments that scan findings inherently set EXIT=1.
        This does not test the validator wrapper failure path required by the task.
    - id: "WB91-R6-D03"
      fact: >-
        git diff commands used to derive the delivery surface do not check returncode, and unreadable changed files are silently skipped.
        Either condition can reduce the scan surface while still allowing a clean result.
    - id: "WB91-R6-D04"
      fact: >-
        R5 removed the R4 git ls-tree exact-set comparison for the fifteen QCLAW artifacts and now only extracts a hardcoded list.
        The accepted exact-manifest check must be restored.
    - id: "WB91-R6-D05"
      fact: >-
        A complete externally anchored completion packet was not independently established in the PR conversation:
        concrete receipt SHA, exact final scan command and hashes, and epoch-5 pre-completion revalidation must be reissued under epoch 6.

required_actions:
  - "Claim route_epoch 6 from latest remote main and post the exact lease acknowledgement before editing."
  - "Reuse the existing branch and Draft PR #91; do not rewrite history or create a replacement PR."
  - "Create one R6 tested_head changing only INDEPENDENT-VERIFICATION-SCRIPT.py."
  - >-
    Refactor or use internal functions so each functional negative test exercises the same production path and asserts its outcome:
    injected unsafe path must produce non-zero; missing QCLAW artifacts must produce non-zero; a temporary validator that exits 7 must make the validator wrapper produce non-zero.
  - "For every negative test record test id, exact command or internal mode, expected exit, actual exit, PASS/FAIL, stdout SHA-256 and stderr SHA-256."
  - "Check returncode and required output for git rev-parse HEAD, git diff --name-status/name-only and git ls-tree; any failure must exit non-zero."
  - "Treat any unreadable existing changed file as a hard failure rather than continue."
  - >-
    Restore exact QCLAW manifest validation by comparing the full frozen-head git ls-tree P1 artifact set to the exact expected fifteen filenames;
    reject missing, duplicate or extra P1 audit artifacts before extraction.
  - "Run normal verification and all three functional negative tests at the concrete R6 tested_head."
  - "Create one receipt commit changing exactly the four authorized receipt files and use receipt_head_ref: THIS_COMMIT."
  - >-
    In each receipt record full tested head, receipt parent, reviewed-to-tested and tested-to-receipt lists,
    exact commands, expected/actual exits, timestamps, counts, manifest/finding hashes and normalized stdout/stderr SHA-256.
  - >-
    After pushing, post one immutable external completion anchor to PR #91 and Issues #89/#31 containing the concrete full receipt SHA,
    final full-delivery scan result and route_epoch 6 pre-completion revalidation fields. Do not create a self-backfill commit.
  - "Preserve all R2-R5 failures and historical local-path exposure; do not claim history was sanitized."
  - "Post the epoch-6 completion signal and stop for GPT review."

allowed_changed_files:
  tested_head:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/INDEPENDENT-VERIFICATION-SCRIPT.py"
  receipt_head_only:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/TEST-RUN-RECEIPT.md"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/AI_HANDOFF.yaml"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml"

preserve_unchanged:
  - "PR #84/#77/#79/#90 and all fixed heads"
  - "QCLAW 42-question statuses, four blocker decisions, 0.491 score and verdict"
  - "Accepted H0/R1 facts and accepted R3-R5 delivery structure"
  - "All other PR #91 files"

acceptance_gates:
  - "Lease acknowledgement exactly matches route_epoch 6."
  - "The reviewed-to-tested diff changes exactly the one authorized verifier file."
  - "Git discovery, diff, unreadable-file and exact-manifest errors are fail-closed."
  - "Injected-path, missing-artifact and forced-validator-failure tests each assert and record non-zero outcomes."
  - "The full frozen QCLAW head contains exactly the expected fifteen P1 artifacts and the unchanged validator passes 37/0/0."
  - "The final complete delivery surface is CLEAN with concrete manifest and findings hashes."
  - "The tested-to-receipt diff changes exactly four authorized receipt files."
  - "All receipts use THIS_COMMIT semantics and contain no unresolved placeholders or short authoritative SHAs."
  - "The external completion anchor contains concrete receipt SHA, machine hashes and epoch-6 revalidation in PR #91 and Issues #89/#31."
  - "Every failure, SKIP, UNKNOWN and historical negative result remains visible."
  - "No semantic rescore, Issue #92 execution, source access, merge, account, order or trade occurs."

forbidden:
  - "No new helper, generator, copied verifier or raw output file in the Git tree."
  - "No print-only negative test without an asserted expected/actual exit comparison."
  - "No package-only scan presented as proof for a broader delivery surface."
  - "No TBD, self-SHA backfill commit or short authoritative SHA."
  - "No history rewrite, force push or deletion of negative evidence."
  - "No modification of QCLAW semantics or PR #84/#77/#79/#90."
  - "No Issue #92 execution, source access, labels, replay, backtest or trading."
  - "No merge or auto-merge."

after_completion: "STOP_AND_WAIT_FOR_GPT_REVIEW"
boundary: "PUBLIC_SAFE / CANDIDATE_ONLY / research_only / NO_TRADE"
updated_by: "GPT"
updated_at: "2026-07-25T00:45:00+08:00"
