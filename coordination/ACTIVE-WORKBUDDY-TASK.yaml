schema_version: "3.0"
router_protocol: "coordination/WORKBUDDY-TASK-ROUTER.md"
task_lease_protocol: "coordination/GOVERNANCE/AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS-PROTOCOL-v1.0.yaml"
discovery_protocol: "coordination/GOVERNANCE/AGENT-PROACTIVE-DISCOVERY-AND-REALTIME-ESCALATION-PROTOCOL-v1.0.yaml"
repository: "vxz2datoubo/second-brain-coordination"
target_agent: "WORKBUDDY"
trigger_phrases: ["读取任务", "执行任务", "开始任务"]

active_issue: 89
pull_request: 91
task_id: "WORKBUDDY-PR91-R8-TOPLEVEL-FAILURE-PROPAGATION-AND-MACHINE-RECEIPT-CLOSURE"
mode: "target"
mode_label: "【WorkBuddy模式：目标模式】PR #91顶层失败传播与机器回执最终收口R8"
mode_reason: >-
  R7已经形成严格的tested-to-receipt两头结构并修正了旧任务标识，但负测模式在检测到预期危险后仍返回0，
  nt2/nt3没有经过同一个生产QCLAW包装函数，正常模式也未把子验证器非零传播为顶层非零。
  当前缺口只涉及一个验证器和四个回执文件，路径和成功标准完全确定，因此继续使用目标模式。
status: "READY"
execution_allowed: true
blocked_by: "none"
parent_program: 31
source_issue: 89
branch: "workbuddy/qclaw-p1-cleanroom-handoff-verification-0023-h0"
reviewed_or_base_head: "f4e03e4e49ef50fd2c81965b2cf391b8c5fd02b3"
r7_tested_head: "73e4ff9a889c6f37c6bd26aee7d5309c9f53af3d"
r7_receipt_head: "f4e03e4e49ef50fd2c81965b2cf391b8c5fd02b3"
direct_main_write: false
auto_merge: false
route_epoch: 8
route_issued_at: "2026-07-25T05:10:00+08:00"
completion_signal: "WORKBUDDY_PR91_R8_TOPLEVEL_FAILURE_PROPAGATION_AND_MACHINE_RECEIPTS_READY_FOR_GPT_REVIEW"

lease_claim_required:
  protocol: "AGENT-TASK-LEASE-AND-COMPLETION-FRESHNESS"
  exact_echo_fields: [repository, remote_main_head_at_claim, task_id, route_epoch, active_issue, pull_request, branch, status, execution_allowed, completion_signal, reviewed_or_base_head]
  mismatch_result: "STOP_STALE_OR_INVALID_ROUTE"
pre_completion_revalidation:
  required: true
  reread_latest_remote_main: true
  exact_match_fields: [task_id, route_epoch, active_issue, pull_request, branch, completion_signal, execution_allowed]
  mismatch_result: "STALE_COMPLETION_ABORTED_AND_STALEROUTEPACKET_REQUIRED"

review_disposition:
  classification: "R7_STRUCTURE_ACCEPTED_TOPLEVEL_FAILURE_PROPAGATION_AND_RECEIPT_EVIDENCE_R8_REQUIRED"
  accepted_facts:
    - "R7 is exactly two commits after the reviewed R6 receipt head."
    - "R7 tested head changes only INDEPENDENT-VERIFICATION-SCRIPT.py."
    - "R7 receipt head changes exactly the four authorized receipt files."
    - "Exact fifteen-file QCLAW manifest checking, strict UTF-8 reads, full task id and exact completion signal are present."
    - "No QCLAW semantic result, blocker, 0.491 score or verdict changed."
  blocking_defects:
    - id: "WB91-R8-D01"
      fact: "nt1 detects a finding but treats it as PASS and exits 0; the required top-level non-zero behavior is not proven."
    - id: "WB91-R8-D02"
      fact: "nt2 directly executes the child validator and exits 0 when the child fails; it does not exercise production wrapper propagation."
    - id: "WB91-R8-D03"
      fact: "nt3 directly executes an exit-7 child and exits 0 when the assertion succeeds; it does not prove the production wrapper and top-level process fail."
    - id: "WB91-R8-D04"
      fact: "qclaw() records child returncode but normal mode never sets EXIT when the child validator is non-zero or required result fields are absent."
    - id: "WB91-R8-D05"
      fact: "The receipts claim per-test stdout/stderr hashes, but TEST-RUN-RECEIPT does not contain them and records findings/child exits rather than actual top-level process exits."
    - id: "WB91-R8-D06"
      fact: "The receipt files report delivery=1 from the tested head, not the final reviewed-to-receipt five-file delivery surface."

required_actions:
  - "Claim route_epoch 8 from latest remote main before editing."
  - "Reuse the current branch and Draft PR #91; do not rewrite history or create another PR."
  - "Create one tested head changing only INDEPENDENT-VERIFICATION-SCRIPT.py."
  - "Use one production scan function and one production QCLAW wrapper for normal and fixture modes."
  - "In normal mode, any manifest mismatch, git failure, missing extraction, validator timeout, validator non-zero, missing 37/0/0 fields or unsafe finding must make the top-level process non-zero."
  - "Command `python INDEPENDENT-VERIFICATION-SCRIPT.py nt1` must itself return non-zero after the production scanner sees an injected unsafe fixture."
  - "Command `python INDEPENDENT-VERIFICATION-SCRIPT.py nt2` must itself return non-zero after the production wrapper receives a missing-artifact validator failure."
  - "Command `python INDEPENDENT-VERIFICATION-SCRIPT.py nt3` must itself return non-zero after the production wrapper receives validator exit 7."
  - "Use an uncommitted external harness or shell commands to assert those three commands are non-zero; the harness may return 0 only after verifying expected failures."
  - "Restore every temporary fixture in finally blocks; restoration failure must be non-zero."
  - "Record exact commands, expected and actual top-level exits, PASS/FAIL, stdout SHA-256 and stderr SHA-256 for normal, nt1, nt2 and nt3."
  - "Create one receipt commit changing exactly the four authorized receipt files."
  - "All receipt files must use exact R8 task_id, completion signal, full tested head, receipt_head_ref: THIS_COMMIT and current timestamps."
  - "After receipt push, run normal verification at the final receipt head; reviewed-to-receipt scan must cover exactly five changed files unless Git independently proves another exact count."
  - "Post the concrete receipt SHA, full final scan hashes, command hashes and epoch-8 pre-completion revalidation to PR #91 and Issues #89/#31."
  - "Stop for GPT review."

allowed_changed_files:
  tested_head:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/INDEPENDENT-VERIFICATION-SCRIPT.py"
  receipt_head_only:
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/PUBLIC-SAFETY-AND-LOCAL-PATH-HYGIENE-REPORT.yaml"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/TEST-RUN-RECEIPT.md"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/AI_HANDOFF.yaml"
    - "coordination/PROGRAMS/SECOND-BRAIN-A-SHARE-ENTERPRISE-SYSTEM-0001/WORKBUDDY-QCLAW-P1-CLEANROOM-VERIFICATION/0023-H0/WORKBUDDY-EXECUTION-FEEDBACK-v2.yaml"

forbidden:
  - "No helper, generator, copied validator or raw-output file committed to Git."
  - "No child-only test or PASS-with-exit-0 presented as top-level failure propagation proof."
  - "No history rewrite, force push, semantic rescore or deletion of negative evidence."
  - "No modification of PR #84/#77/#79/#90/#93 or Codex/QCLAW branches."
  - "No Issue #92 execution, source access, labels, replay, backtest, account, order or trade."
  - "No merge or auto-merge."

after_completion: "STOP_AND_WAIT_FOR_GPT_REVIEW"
boundary: "PUBLIC_SAFE / CANDIDATE_ONLY / research_only / NO_TRADE"
updated_by: "GPT"
updated_at: "2026-07-25T05:10:00+08:00"
