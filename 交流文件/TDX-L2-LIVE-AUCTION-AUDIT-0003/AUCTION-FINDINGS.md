# AUCTION-FINDINGS — 集合竞价阶段 (A/B/C: 09:15–09:25)

> ⚠️ 本会话从 09:24:15 起开始采集。09:15–09:24 段未在本会话直接捕获(任务下发时已过)。以下 A/B 阶段记录基于 09:24 起的现场 + R1/R2/R3 轮次证据, 不补造 09:15–09:24 数据。

## A. 09:15–09:20 (可撤单阶段)
- **本会话未覆盖** (任务下发时已 09:24, 该段已过)。
- 用户陈述: 客户端 UI 在该阶段显示虚拟参考价/匹配量/未匹配量/十档 → UI_VERIFIED_BY_USER。
- MCP 侧无该阶段快照, 不补造。

## B. 09:20–09:25 (不可撤单阶段) — R1 撮合点窗口捕获
**采集轮次 R1**: 本地接收锚点见 `raw/_poll_anchor_r1.txt` (09:25:21), HQTime 跨度 **92500–92519** (即交易所时间 09:25:00–09:25:19), 正好覆盖 **C 撮合点 09:24:55–09:25:10**。

### R1 关键观察 (6标的 + 指数)
| 标的 | HQTime | Now(现价) | OpenStatus | BspInfo档数 | ProInfo.InOut | ProInfo.InOutHB |
|---|---|---|---|---|---|---|
| 300418 | 92500-92519 | 45.5(开盘价) | 3 | 5档(后5空) | 无(R1) | 无(R1) |
| 300058 | 同窗口 | 12.76 | 3 | 5档 | 无 | 无 |
| 600519 | 同窗口 | 1252 | 3 | 5档 | 无 | 无 |
| 510300 | 同窗口 | 4.775 | 3 | 5档 | 无 | 无 |
| 000609 | 同窗口 | 10.84 | 3 | 5档 | 无 | 无 |
| 999999 | 同窗口 | 3912.38(开盘) | — | — | — | — |

### B/C 阶段发现
1. **撮合点字段切换**: R1(撮合点窗口)中, 多数标的的 `ProInfo` **尚未出现** `InOut`/`InOutHB` 聚合字段; 到 R2(09:26:03 撮合后过渡) 这些字段才出现 (如 300418 R2 出现 InOut:3084872/InOutHB:3116957)。→ **L2 聚合字段在撮合后/连续竞价开启后才由 MCP 吐出**, 竞价撮合瞬间不暴露。
2. **OpenStatus**: 撮合点已为 3(连续状态), MCP 未单独标记"撮合瞬间"状态。
3. **BspInfo 始终 5 档**: 即便 bspNum=10, 竞价阶段后5档为空对象 `{}`。→ **MCP 不暴露 L2 十档**。
4. **WeStock 竞价期滞后**: R1 中 300418 WeStock 价 45.28 vs TDX 45.5, 000609 WeStock 10.88 vs TDX 10.84, 且深市 volume=0。→ **WeStock 在集合竞价阶段价格/成交量滞后**, 不可用于竞价逐笔推断, 仅连续竞价后对齐。
5. **撮合价 = 开盘价**: 300418 开盘 45.5, 300058 开盘 12.76, 600519 开盘 1252, 510300 开盘 4.775, 000609 开盘 10.84, 上证开盘 3912.38。

## C. 09:25 撮合点 (09:24:55–09:25:10)
- **撮合瞬间字段**: MCP 未返回"最后虚拟价/最后匹配量/最后未匹配量"独立字段。撮合点以 `Open`(开盘价) + `OpenStatus` 切换体现。
- **字段是否瞬间清零/冻结**: R1→R2 未见字段清零; `InOut/InOutHB` 是撮合后**新增出现**而非清零。
- **本地是否产生新文件**: 撮合点未观察到 vipdoc 新增 .l2d/.tick; T0002/hq_cache 压缩缓存持续更新(非撮合点特有)。

## 竞价阶段结论
- **MCP 在集合竞价阶段能力极有限**: 只有五档快照 + OpenStatus + HQTime + 开盘价, **拿不到**虚拟参考价/匹配量/未匹配量/十档/逐笔。
- **L2 竞价细节(虚拟价/匹配量/未匹配量)只能从客户端 UI 看, 不能从 MCP 读**。
- 要程序化获取竞价虚拟价/匹配量, 须走 **TdxQuant subscribe_hq 推送**(待非交易时段验证)或 **eltdx 等第三方**(本次未测)。
